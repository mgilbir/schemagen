{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Issue #205, the issue's first repro. Draft 7 asserts `format`, and the object-level `then` was read leniently: objectPropertyChecksLenient carries nine scalar keywords and `format` is not one, so the branch demanded only that `p` be a string.",
  "type": "object",
  "properties": {
    "t": {
      "type": "integer"
    }
  },
  "if": {
    "properties": {
      "t": {
        "const": 1
      }
    },
    "required": [
      "t"
    ]
  },
  "then": {
    "properties": {
      "p": {
        "type": "string",
        "format": "email"
      }
    },
    "required": [
      "p"
    ]
  }
}
