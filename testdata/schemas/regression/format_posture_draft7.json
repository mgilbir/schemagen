{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FormatPostureDraft7",
  "description": "The same shapes as format_posture_2020, written in a dialect that leaves format assertion to the implementation. Draft 7 says an implementation SHOULD validate a format it recognises, and this generator does.",
  "type": "object",
  "properties": {
    "typed": {"type": "string", "format": "ipv4"},
    "untyped": {"format": "ipv4"},
    "nullable": {"type": ["string", "null"], "format": "ipv4"},
    "stamp": {"type": "string", "format": "date-time"},
    "mail": {"type": "string", "format": "email"}
  }
}
