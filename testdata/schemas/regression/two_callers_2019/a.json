{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "https://example.com/a.json",
  "x-unmodelled": true,
  "$comment": "x-unmodelled is load-bearing: it is a keyword the runtime evaluator cannot compile, which keeps this resource on the static path where #332 is visible.",
  "$recursiveAnchor": true,
  "type": "object",
  "required": ["x"],
  "properties": {
    "x": { "type": "integer" },
    "r": { "$ref": "r.json" }
  }
}
