{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Issue #205 at a condition every object satisfies. `if: {}` matches everything, so `then` applies to every document -- but objectConditionalDef returns nil for a vacuous condition, on the reading that the unconditional `then` belongs to whatever flattens it into the struct, and nothing does. So `then` was enforced by neither route and {} was accepted against a branch requiring \"p\".",
  "type": "object",
  "if": {},
  "then": {
    "properties": { "p": { "type": "string", "format": "email" } },
    "required": ["p"]
  }
}
