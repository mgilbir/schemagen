{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BooleanDefsKeepAny",
  "$comment": "A bare boolean definition named from a property. `true` stays `any`: it accepts everything, and wrapping it once cost the alias over it its decode, a false rejection. `false` gets the forbidding wrapper instead, because it must reject every instance -- including an explicit null, which the parent's old field != nil rule could not see.",
  "$defs": {
    "yes": true,
    "no": false
  },
  "type": "object",
  "properties": {
    "a": {
      "$ref": "#/$defs/yes"
    },
    "b": {
      "$ref": "#/$defs/no"
    }
  }
}
