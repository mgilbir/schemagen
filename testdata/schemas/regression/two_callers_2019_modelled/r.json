{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "https://example.com/modelled/r.json",
  "$comment": "The shared resource. Its $recursiveRef must mean a.json when r is reached through a and b.json when it is reached through b. See main.json beside this, and issue #332.",
  "$recursiveAnchor": true,
  "type": "object",
  "required": ["c"],
  "properties": {
    "c": { "type": "integer" },
    "v": { "$recursiveRef": "#" }
  }
}
