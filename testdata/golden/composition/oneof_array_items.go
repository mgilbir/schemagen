// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"bytes"
	"encoding/json"
	"fmt"
)

type CircleKind string

const (
	CircleKindCircle CircleKind = "circle"
)

// Validate checks CircleKind against its JSON Schema constraints.
func (c CircleKind) Validate() error {
	switch c {
	case CircleKindCircle:
		return nil
	default:
		return jsonValueErrorf("invalid CircleKind value: %v", c)
	}
}

type Circle struct {
	Kind                 CircleKind                 `json:"kind"`
	Radius               float64                    `json:"radius"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (c *Circle) UnmarshalJSON(data []byte) error {
	c.AdditionalProperties = nil
	c._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"kind",
			"radius",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias Circle
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(c),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"kind", jsonDecodeValue[CircleKind]},
			{"radius", jsonDecodeValue[float64]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"kind",
			"radius",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		c._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			c._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"kind":   true,
			"radius": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if c.AdditionalProperties == nil {
				c.AdditionalProperties = make(map[string]json.RawMessage)
			}
			c.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (c Circle) MarshalJSON() ([]byte, error) {
	type Alias Circle
	aux := struct {
		Alias
	}{
		Alias: (Alias)(c),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range c.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks Circle against its JSON Schema constraints.
func (c Circle) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if c._jsonKeys != nil {
		for _, _req := range []string{"kind", "radius"} {
			if !c._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	if err := c.Kind.Validate(); err != nil {
		return jsonPathf(err, "kind")
	}
	return nil
}

type RectangleKind string

const (
	RectangleKindRectangle RectangleKind = "rectangle"
)

// Validate checks RectangleKind against its JSON Schema constraints.
func (r RectangleKind) Validate() error {
	switch r {
	case RectangleKindRectangle:
		return nil
	default:
		return jsonValueErrorf("invalid RectangleKind value: %v", r)
	}
}

type Rectangle struct {
	Height               float64                    `json:"height"`
	Kind                 RectangleKind              `json:"kind"`
	Width                float64                    `json:"width"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (r *Rectangle) UnmarshalJSON(data []byte) error {
	r.AdditionalProperties = nil
	r._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"height",
			"kind",
			"width",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias Rectangle
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(r),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"height", jsonDecodeValue[float64]},
			{"kind", jsonDecodeValue[RectangleKind]},
			{"width", jsonDecodeValue[float64]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"height",
			"kind",
			"width",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		r._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			r._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"height": true,
			"kind":   true,
			"width":  true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if r.AdditionalProperties == nil {
				r.AdditionalProperties = make(map[string]json.RawMessage)
			}
			r.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (r Rectangle) MarshalJSON() ([]byte, error) {
	type Alias Rectangle
	aux := struct {
		Alias
	}{
		Alias: (Alias)(r),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range r.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks Rectangle against its JSON Schema constraints.
func (r Rectangle) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if r._jsonKeys != nil {
		for _, _req := range []string{"height", "kind", "width"} {
			if !r._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	if err := r.Kind.Validate(); err != nil {
		return jsonPathf(err, "kind")
	}
	return nil
}

type CanvasShapesItem struct {
	Value         isCanvasShapesItem_Value   `json:"-"`
	_jsonKeys     map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

// isCanvasShapesItem_Value is a sealed interface for the Value field of CanvasShapesItem.
// Exactly one variant must be set.
type isCanvasShapesItem_Value interface {
	isCanvasShapesItem_Value()
}

// CanvasShapesItem_Circle wraps *Circle as a Value variant.
type CanvasShapesItem_Circle struct {
	Circle *Circle
}

func (*CanvasShapesItem_Circle) isCanvasShapesItem_Value() {}

// CanvasShapesItem_Rectangle wraps *Rectangle as a Value variant.
type CanvasShapesItem_Rectangle struct {
	Rectangle *Rectangle
}

func (*CanvasShapesItem_Rectangle) isCanvasShapesItem_Value() {}

// GetCircle returns the Circle variant value, or the zero value if not set.
func (c *CanvasShapesItem) GetCircle() *Circle {
	if c != nil {
		if v, ok := c.Value.(*CanvasShapesItem_Circle); ok {
			return v.Circle
		}
	}
	var zero *Circle
	return zero
}

// GetRectangle returns the Rectangle variant value, or the zero value if not set.
func (c *CanvasShapesItem) GetRectangle() *Rectangle {
	if c != nil {
		if v, ok := c.Value.(*CanvasShapesItem_Rectangle); ok {
			return v.Rectangle
		}
	}
	var zero *Rectangle
	return zero
}

func (c *CanvasShapesItem) UnmarshalJSON(data []byte) error {
	c._jsonKeys = nil
	c._jsonRawProps = nil
	c.Value = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias CanvasShapesItem
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(c),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return jsonDecodeRefusal(err)
	}

	{
		// Top-level oneOf: the entire JSON object is the variant data.
		oneofData := json.RawMessage(data)
		if len(oneofData) > 0 && string(oneofData) != "null" {
			// Discriminator-based dispatch on "kind"
			discVal, discErr := oneofDiscriminatorValue(oneofData, "kind")
			if discErr != nil {
				return fmt.Errorf("CanvasShapesItem.Value: %w", discErr)
			}
			switch discVal {
			case "circle":
				var candidate *Circle
				if err := json.Unmarshal(oneofData, &candidate); err != nil {
					return fmt.Errorf("CanvasShapesItem.Value (variant Circle): %w", err)
				}
				c.Value = &CanvasShapesItem_Circle{Circle: candidate}
			case "rectangle":
				var candidate *Rectangle
				if err := json.Unmarshal(oneofData, &candidate); err != nil {
					return fmt.Errorf("CanvasShapesItem.Value (variant Rectangle): %w", err)
				}
				c.Value = &CanvasShapesItem_Rectangle{Rectangle: candidate}
			default:
				return fmt.Errorf("CanvasShapesItem.Value: unknown discriminator value %q for field \"kind\"", discVal)
			}
		}
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		c._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			c._jsonKeys[_k] = true
		}
		c._jsonRawProps = raw
	}

	return nil
}
func (c CanvasShapesItem) MarshalJSON() ([]byte, error) {
	// Top-level oneOf: marshal the selected variant directly as the root object.
	if c.Value != nil {
		switch v := c.Value.(type) {
		case *CanvasShapesItem_Circle:
			return json.Marshal(v.Circle)
		case *CanvasShapesItem_Rectangle:
			return json.Marshal(v.Rectangle)
		}
	}
	return []byte("null"), nil
}

// Validate checks CanvasShapesItem against its JSON Schema constraints.
func (c CanvasShapesItem) Validate() error {
	// object-level oneOf: exactly one flattened variant must match.
	// This check depends on JSON key presence (_jsonKeys), so it is skipped
	// for hand-constructed values (nil _jsonKeys), consistent with how the
	// required-property check above treats untracked presence.
	if c._jsonKeys != nil {
		_oneOfMatches := 0
		{
			_branchMatches := true
			if !c._jsonKeys["kind"] {
				_branchMatches = false
			}
			if !c._jsonKeys["radius"] {
				_branchMatches = false
			}
			if _branchMatches {
				if _raw, _ok := c._jsonRawProps["kind"]; _ok {
					if _branchMatches {
						_valueMatches := false
						switch string(bytes.TrimSpace(_raw)) {
						case "\"circle\"":
							_valueMatches = true
						}
						if !_valueMatches {
							_branchMatches = false
						}
					}
				}
			}
			if _branchMatches {
				if _raw, _ok := c._jsonRawProps["radius"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "number" && _jt != "integer" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				_oneOfMatches++
			}
		}
		{
			_branchMatches := true
			if !c._jsonKeys["height"] {
				_branchMatches = false
			}
			if !c._jsonKeys["kind"] {
				_branchMatches = false
			}
			if !c._jsonKeys["width"] {
				_branchMatches = false
			}
			if _branchMatches {
				if _raw, _ok := c._jsonRawProps["height"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "number" && _jt != "integer" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				if _raw, _ok := c._jsonRawProps["kind"]; _ok {
					if _branchMatches {
						_valueMatches := false
						switch string(bytes.TrimSpace(_raw)) {
						case "\"rectangle\"":
							_valueMatches = true
						}
						if !_valueMatches {
							_branchMatches = false
						}
					}
				}
			}
			if _branchMatches {
				if _raw, _ok := c._jsonRawProps["width"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "number" && _jt != "integer" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				_oneOfMatches++
			}
		}
		if _oneOfMatches != 1 {
			return fmt.Errorf("oneOf: matched %d variants, expected exactly 1", _oneOfMatches)
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := c.Value.(type) {
	case *CanvasShapesItem_Circle:
		if _oneOfSel.Circle != nil {
			if err := _oneOfSel.Circle.Validate(); err != nil {
				return err
			}
		}
	case *CanvasShapesItem_Rectangle:
		if _oneOfSel.Rectangle != nil {
			if err := _oneOfSel.Rectangle.Validate(); err != nil {
				return err
			}
		}
	}
	return nil
}

type Canvas struct {
	Name                 *string                    `json:"name,omitempty"`
	Shapes               []CanvasShapesItem         `json:"shapes"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (c *Canvas) UnmarshalJSON(data []byte) error {
	c.AdditionalProperties = nil
	c._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"name",
			"shapes",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias Canvas
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(c),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"name", jsonDecodeValue[*string]},
			{"shapes", jsonDecodeItems(jsonDecodeValue[CanvasShapesItem])},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"name",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		if _v, ok := raw["shapes"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "shapes")
			}
		}
		c._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			c._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"name":   true,
			"shapes": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if c.AdditionalProperties == nil {
				c.AdditionalProperties = make(map[string]json.RawMessage)
			}
			c.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (c Canvas) MarshalJSON() ([]byte, error) {
	type Alias Canvas
	aux := struct {
		Alias
	}{
		Alias: (Alias)(c),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range c.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks Canvas against its JSON Schema constraints.
func (c Canvas) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if c._jsonKeys != nil {
		for _, _req := range []string{"shapes"} {
			if !c._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	for _i, _item := range c.Shapes {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "shapes[%d]", _i)
		}
	}
	return nil
}
