// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"bytes"
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
	"math"
	"unicode/utf8"
)

type Anything any

type D int64

func (d *D) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type D")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type D")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*d = D(_i)
		return nil
	}
	// Try float with zero fractional part (e.g., 1.0).
	if _f, _fErr := _n.Float64(); _fErr == nil {
		if _f == math.Trunc(_f) && !math.IsInf(_f, 0) && _f >= -9.223372036854776e+18 && _f <= 9.223372036854776e+18 {
			*d = D(int64(_f))
			return nil
		}
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks D against its JSON Schema constraints.
func (d D) Validate() error {
	if float64(d) < 5 {
		return fmt.Errorf("value: %v is less than minimum 5", d)
	}
	return nil
}

type PatternValueSubschemasPattern0 string

const (
	PatternValueSubschemasPattern0Aaa PatternValueSubschemasPattern0 = "aaa"
	PatternValueSubschemasPattern0Bbb PatternValueSubschemasPattern0 = "bbb"
)

// Validate checks PatternValueSubschemasPattern0 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern0) Validate() error {
	switch p {
	case PatternValueSubschemasPattern0Aaa, PatternValueSubschemasPattern0Bbb:
		return nil
	default:
		return fmt.Errorf("invalid PatternValueSubschemasPattern0 value: %v", p)
	}
}

type PatternValueSubschemasPattern1 string

const (
	PatternValueSubschemasPattern1Aaa PatternValueSubschemasPattern1 = "aaa"
)

// Validate checks PatternValueSubschemasPattern1 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern1) Validate() error {
	switch p {
	case PatternValueSubschemasPattern1Aaa:
		return nil
	default:
		return fmt.Errorf("invalid PatternValueSubschemasPattern1 value: %v", p)
	}
}

type PatternValueSubschemasPattern2 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (p *PatternValueSubschemasPattern2) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern2")
	}
	type Alias PatternValueSubschemasPattern2
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		p._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			p._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern2) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern2
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern2 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern2) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if p._jsonKeys != nil {
		for _, _req := range []string{"x"} {
			if !p._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type PatternValueSubschemasPattern3 struct {
	X                    *string                    `json:"x,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (p *PatternValueSubschemasPattern3) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern3")
	}
	type Alias PatternValueSubschemasPattern3
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"x",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		p._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			p._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"x": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern3) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern3
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern3 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern3) Validate() error {
	if p._jsonKeys["x"] {
		if p.X != nil && utf8.RuneCountInString(*p.X) < 5 {
			return fmt.Errorf("x: length %d is less than minimum 5", utf8.RuneCountInString(*p.X))
		}
	}
	return nil
}

type PatternValueSubschemasPattern5 []string

func (p *PatternValueSubschemasPattern5) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern5")
	}
	if _err := checkJSONNulls(data, "value", &jsonNullRule{Elem: &jsonNullRule{Reject: true}}); _err != nil {
		return _err
	}
	type Alias PatternValueSubschemasPattern5
	return json.Unmarshal(data, (*Alias)(p))
}

// Validate checks PatternValueSubschemasPattern5 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern5) Validate() error {
	for _i0, _e0 := range p {
		if utf8.RuneCountInString(string(_e0)) < 5 {
			return fmt.Errorf("items[%d]: length %d is less than minimum 5", _i0, utf8.RuneCountInString(string(_e0)))
		}
	}
	return nil
}

type PatternValueSubschemasPattern6 []any

func (p *PatternValueSubschemasPattern6) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern6")
	}
	type Alias PatternValueSubschemasPattern6
	return json.Unmarshal(data, (*Alias)(p))
}

// Validate checks PatternValueSubschemasPattern6 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern6) Validate() error {
	{
		seen := make(map[string]bool, len(p))
		for i, item := range p {
			b, err := json.Marshal(item)
			if err != nil {
				return fmt.Errorf("value: uniqueItems check: marshal error at index %d: %w", i, err)
			}
			key := string(b)
			if seen[key] {
				return fmt.Errorf("value: items are not unique (duplicate at index %d)", i)
			}
			seen[key] = true
		}
	}
	return nil
}

// PatternValueSubschemasPattern7 accepts any JSON value and validates a root-level "not" constraint.
type PatternValueSubschemasPattern7 struct {
	_raw json.RawMessage
}

func (p *PatternValueSubschemasPattern7) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PatternValueSubschemasPattern7) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PatternValueSubschemasPattern7) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PatternValueSubschemasPattern7) IsZero() bool { return len(p._raw) == 0 }

func (p PatternValueSubschemasPattern7) String() string { return string(p._raw) }

// Validate checks PatternValueSubschemasPattern7 against its JSON Schema "not" constraint.
func (p PatternValueSubschemasPattern7) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	// Decode raw JSON to determine the value's type.
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("not: cannot decode value: %w", _err)
	}
	if _, _sOk := _v.(string); _sOk {
		return fmt.Errorf("not: value must not be string")
	}
	return nil
}

type PatternValueSubschemasPattern8 string

func (p *PatternValueSubschemasPattern8) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern8")
	}
	type Alias PatternValueSubschemasPattern8
	return json.Unmarshal(data, (*Alias)(p))
}

// Validate checks PatternValueSubschemasPattern8 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern8) Validate() error {
	if utf8.RuneCountInString(string(p)) < 5 {
		return fmt.Errorf("value: length %d is less than minimum 5", utf8.RuneCountInString(string(p)))
	}
	return nil
}

type PatternValueSubschemasPattern9 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (p *PatternValueSubschemasPattern9) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern9")
	}
	type Alias PatternValueSubschemasPattern9
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		p._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			p._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern9) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern9
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern9 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern9) Validate() error {
	{
		// Count properties actually present in the source JSON. _jsonKeys is
		// populated by UnmarshalJSON; when nil (value not built from JSON) fall
		// back to the overflow map count.
		totalProps := len(p.AdditionalProperties)
		if p._jsonKeys != nil {
			totalProps = len(p._jsonKeys)
		}
		if totalProps > 1 {
			return fmt.Errorf("too many properties: %d exceeds maximum 1", totalProps)
		}
	}
	return nil
}

type PatternValueSubschemasPattern10 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (p *PatternValueSubschemasPattern10) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p.PatternProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern10")
	}
	type Alias PatternValueSubschemasPattern10
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^z", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if p.PatternProperties == nil {
					p.PatternProperties = make(map[string]json.RawMessage)
				}
				p.PatternProperties[rawKey] = rawVal
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern10) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern10
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.PatternProperties {
		obj[k] = v
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern10 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern10) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(p.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^z", ecmaflags.Unicode),
		}
		for k, v := range p.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					b := bytes.TrimSpace(v)
					var jt string
					if len(b) == 0 {
						jt = "unknown"
					} else {
						switch b[0] {
						case '"':
							jt = "string"
						case '{':
							jt = "object"
						case '[':
							jt = "array"
						case 't', 'f':
							jt = "boolean"
						case 'n':
							jt = "null"
						default:
							jt = "number"
							// Draft 6 onward reads the value: 1.0 is an integer, so
							// the number is parsed rather than scanned. Every arm
							// below that accepts "number" also accepts "integer", so
							// widening this classification narrows nothing.
							var _ppNum float64
							if json.Unmarshal(b, &_ppNum) == nil &&
								!math.IsInf(_ppNum, 0) && _ppNum == math.Trunc(_ppNum) {
								jt = "integer"
							}
						}
					}
					if jt != "string" {
						return fmt.Errorf("patternProperties %s: key %q value must be string", "^z", k)
					}
				}
				{
					var s string
					if err := json.Unmarshal(v, &s); err == nil {
						if utf8.RuneCountInString(s) < 5 {
							return fmt.Errorf("patternProperties %s: key %q string length is less than minLength 5", "^z", k)
						}
					}
				}
			}
		}
	}
	return nil
}

type PatternValueSubschemasPattern11 struct {
	AdditionalProperties map[string]string `json:"-"`
}

func (p *PatternValueSubschemasPattern11) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern11")
	}
	type Alias PatternValueSubschemasPattern11
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if string(rawVal) == "null" {
				return fmt.Errorf("additionalProperties[%q]: null is not allowed", rawKey)
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]string)
			}
			var val string
			if err := json.Unmarshal(rawVal, &val); err != nil {
				return fmt.Errorf("additionalProperties[%s]: %w", rawKey, err)
			}
			p.AdditionalProperties[rawKey] = val
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern11) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern11
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		raw, err := json.Marshal(v)
		if err != nil {
			return nil, fmt.Errorf("marshaling additional property %q: %w", k, err)
		}
		obj[k] = raw
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern11 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern11) Validate() error {
	for _k0, _e0 := range p.AdditionalProperties {
		if utf8.RuneCountInString(string(_e0)) < 5 {
			return fmt.Errorf("additionalProperties[%q]: length %d is less than minimum 5", _k0, utf8.RuneCountInString(string(_e0)))
		}
	}
	return nil
}

// PatternValueSubschemasPattern12 accepts any JSON value. The schema declares no type of its own, so
// the constraints come entirely from its applicators and are checked against
// the decoded value by Validate.
type PatternValueSubschemasPattern12 struct {
	_raw json.RawMessage
}

func (p *PatternValueSubschemasPattern12) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PatternValueSubschemasPattern12) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PatternValueSubschemasPattern12) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PatternValueSubschemasPattern12) IsZero() bool { return len(p._raw) == 0 }

func (p PatternValueSubschemasPattern12) String() string { return string(p._raw) }

// Validate checks PatternValueSubschemasPattern12 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern12) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	{
		_matches := 0
		if _dynIsString(_v) {
			_matches++
		}
		if _dynIsInteger(_v) {
			_matches++
		}
		if _matches != 1 {
			return fmt.Errorf("oneOf: value matches %d variants, expected exactly 1", _matches)
		}
	}
	return nil
}

// PatternValueSubschemasPattern13 accepts any JSON value. The schema declares no type of its own, so
// the constraints come entirely from its applicators and are checked against
// the decoded value by Validate.
type PatternValueSubschemasPattern13 struct {
	_raw json.RawMessage
}

func (p *PatternValueSubschemasPattern13) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PatternValueSubschemasPattern13) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PatternValueSubschemasPattern13) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PatternValueSubschemasPattern13) IsZero() bool { return len(p._raw) == 0 }

func (p PatternValueSubschemasPattern13) String() string { return string(p._raw) }

// Validate checks PatternValueSubschemasPattern13 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern13) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _dynIsString(_v) {
		if !(_dynStrOK(_v, func(s string) bool { return utf8.RuneCountInString(s) >= 5 })) {
			return fmt.Errorf("then: value does not satisfy the then schema")
		}
	} else {
	}
	return nil
}

type PatternValueSubschemasPattern14 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (p *PatternValueSubschemasPattern14) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern14")
	}
	type Alias PatternValueSubschemasPattern14
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		p._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			p._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern14) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern14
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern14 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern14) Validate() error {
	// propertyNames: validate that all property names satisfy the constraint.
	for _pnKey := range p._jsonKeys {
		if !ecma262.MustCompile("^z", ecmaflags.Unicode).MatchString(_pnKey) {
			return fmt.Errorf("propertyNames: property name %q does not match pattern %q", _pnKey, "^z")
		}
	}
	return nil
}

type PatternValueSubschemasPattern15 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (p *PatternValueSubschemasPattern15) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern15")
	}
	type Alias PatternValueSubschemasPattern15
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		p._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			p._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern15) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern15
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern15 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern15) Validate() error {
	// dependentRequired: when a trigger key is present, listed properties must also be present.
	if p._jsonKeys["x"] {
		if !p._jsonKeys["y"] {
			return fmt.Errorf("dependentRequired %q: property %q is required", "x", "y")
		}
	}
	return nil
}

type PatternValueSubschemasPattern16 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (p *PatternValueSubschemasPattern16) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemasPattern16")
	}
	type Alias PatternValueSubschemasPattern16
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemasPattern16) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemasPattern16
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemasPattern16 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern16) Validate() error {
	if len(p.AdditionalProperties) > 0 {
		for k := range p.AdditionalProperties {
			return fmt.Errorf("additional property %q is not allowed", k)
		}
	}
	return nil
}

// PatternValueSubschemasPattern20 accepts any JSON value. Constraints apply only when the value is string.
type PatternValueSubschemasPattern20 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (p *PatternValueSubschemasPattern20) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		p._raw = append(p._raw[:0], data...)
		p._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &p._value); _err == nil {
		p._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	p._raw = append(p._raw[:0], data...)
	p._isRaw = true
	return nil
}
func (p PatternValueSubschemasPattern20) MarshalJSON() ([]byte, error) {
	if p._isRaw {
		if len(p._raw) == 0 {
			return []byte("null"), nil
		}
		return p._raw, nil
	}
	return json.Marshal(p._value)
}
func (p PatternValueSubschemasPattern20) StringValue() string { return p._value }
func (p PatternValueSubschemasPattern20) IsString() bool      { return !p._isRaw }
func (p PatternValueSubschemasPattern20) Raw() json.RawMessage {
	if p._isRaw {
		return p._raw
	}
	_b, _ := json.Marshal(p._value)
	return _b
}
func (p PatternValueSubschemasPattern20) String() string {
	if p._isRaw {
		return string(p._raw)
	}
	return fmt.Sprintf("%v", p._value)
}

// Validate checks PatternValueSubschemasPattern20 against its JSON Schema constraints.
func (p PatternValueSubschemasPattern20) Validate() error {
	if p._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

type PatternValueSubschemas struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (p *PatternValueSubschemas) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p.PatternProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PatternValueSubschemas")
	}
	type Alias PatternValueSubschemas
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^a", ecmaflags.Unicode),
			ecma262.MustCompile("^b", ecmaflags.Unicode),
			ecma262.MustCompile("^c", ecmaflags.Unicode),
			ecma262.MustCompile("^d", ecmaflags.Unicode),
			ecma262.MustCompile("^e", ecmaflags.Unicode),
			ecma262.MustCompile("^f", ecmaflags.Unicode),
			ecma262.MustCompile("^g", ecmaflags.Unicode),
			ecma262.MustCompile("^h", ecmaflags.Unicode),
			ecma262.MustCompile("^i", ecmaflags.Unicode),
			ecma262.MustCompile("^j", ecmaflags.Unicode),
			ecma262.MustCompile("^k", ecmaflags.Unicode),
			ecma262.MustCompile("^l", ecmaflags.Unicode),
			ecma262.MustCompile("^m", ecmaflags.Unicode),
			ecma262.MustCompile("^n", ecmaflags.Unicode),
			ecma262.MustCompile("^o", ecmaflags.Unicode),
			ecma262.MustCompile("^p", ecmaflags.Unicode),
			ecma262.MustCompile("^q", ecmaflags.Unicode),
			ecma262.MustCompile("^r", ecmaflags.Unicode),
			ecma262.MustCompile("^s", ecmaflags.Unicode),
			ecma262.MustCompile("^t", ecmaflags.Unicode),
			ecma262.MustCompile("^u", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if p.PatternProperties == nil {
					p.PatternProperties = make(map[string]json.RawMessage)
				}
				p.PatternProperties[rawKey] = rawVal
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PatternValueSubschemas) MarshalJSON() ([]byte, error) {
	type Alias PatternValueSubschemas
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.PatternProperties {
		obj[k] = v
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PatternValueSubschemas against its JSON Schema constraints.
func (p PatternValueSubschemas) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(p.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^a", ecmaflags.Unicode),
			ecma262.MustCompile("^b", ecmaflags.Unicode),
			ecma262.MustCompile("^c", ecmaflags.Unicode),
			ecma262.MustCompile("^d", ecmaflags.Unicode),
			ecma262.MustCompile("^e", ecmaflags.Unicode),
			ecma262.MustCompile("^f", ecmaflags.Unicode),
			ecma262.MustCompile("^g", ecmaflags.Unicode),
			ecma262.MustCompile("^h", ecmaflags.Unicode),
			ecma262.MustCompile("^i", ecmaflags.Unicode),
			ecma262.MustCompile("^j", ecmaflags.Unicode),
			ecma262.MustCompile("^k", ecmaflags.Unicode),
			ecma262.MustCompile("^l", ecmaflags.Unicode),
			ecma262.MustCompile("^m", ecmaflags.Unicode),
			ecma262.MustCompile("^n", ecmaflags.Unicode),
			ecma262.MustCompile("^o", ecmaflags.Unicode),
			ecma262.MustCompile("^p", ecmaflags.Unicode),
			ecma262.MustCompile("^q", ecmaflags.Unicode),
			ecma262.MustCompile("^r", ecmaflags.Unicode),
			ecma262.MustCompile("^s", ecmaflags.Unicode),
			ecma262.MustCompile("^t", ecmaflags.Unicode),
			ecma262.MustCompile("^u", ecmaflags.Unicode),
		}
		for k, v := range p.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern0
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^a", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^a", k, _vErr)
					}
				}
			}
			if ppRegexps[1].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern1
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^b", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^b", k, _vErr)
					}
				}
			}
			if ppRegexps[2].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern2
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^c", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^c", k, _vErr)
					}
				}
			}
			if ppRegexps[3].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern3
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^d", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^d", k, _vErr)
					}
				}
			}
			if ppRegexps[4].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv D
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^e", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^e", k, _vErr)
					}
				}
			}
			if ppRegexps[5].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern5
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^f", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^f", k, _vErr)
					}
				}
			}
			if ppRegexps[6].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern6
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^g", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^g", k, _vErr)
					}
				}
			}
			if ppRegexps[7].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern7
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^h", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^h", k, _vErr)
					}
				}
			}
			if ppRegexps[8].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern8
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^i", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^i", k, _vErr)
					}
				}
			}
			if ppRegexps[9].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern9
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^j", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^j", k, _vErr)
					}
				}
			}
			if ppRegexps[10].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern10
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^k", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^k", k, _vErr)
					}
				}
			}
			if ppRegexps[11].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern11
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^l", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^l", k, _vErr)
					}
				}
			}
			if ppRegexps[12].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern12
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^m", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^m", k, _vErr)
					}
				}
			}
			if ppRegexps[13].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern13
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^n", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^n", k, _vErr)
					}
				}
			}
			if ppRegexps[14].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern14
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^o", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^o", k, _vErr)
					}
				}
			}
			if ppRegexps[15].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern15
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^p", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^p", k, _vErr)
					}
				}
			}
			if ppRegexps[16].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern16
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^q", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^q", k, _vErr)
					}
				}
			}
			if ppRegexps[17].MatchString(k) {
				{
					b := bytes.TrimSpace(v)
					var jt string
					if len(b) == 0 {
						jt = "unknown"
					} else {
						switch b[0] {
						case '"':
							jt = "string"
						case '{':
							jt = "object"
						case '[':
							jt = "array"
						case 't', 'f':
							jt = "boolean"
						case 'n':
							jt = "null"
						default:
							jt = "number"
							// Draft 6 onward reads the value: 1.0 is an integer, so
							// the number is parsed rather than scanned. Every arm
							// below that accepts "number" also accepts "integer", so
							// widening this classification narrows nothing.
							var _ppNum float64
							if json.Unmarshal(b, &_ppNum) == nil &&
								!math.IsInf(_ppNum, 0) && _ppNum == math.Trunc(_ppNum) {
								jt = "integer"
							}
						}
					}
					if jt != "integer" {
						return fmt.Errorf("patternProperties %s: key %q value must be integer", "^r", k)
					}
				}
			}
			if ppRegexps[18].MatchString(k) {
				{
					b := bytes.TrimSpace(v)
					var jt string
					if len(b) == 0 {
						jt = "unknown"
					} else {
						switch b[0] {
						case '"':
							jt = "string"
						case '{':
							jt = "object"
						case '[':
							jt = "array"
						case 't', 'f':
							jt = "boolean"
						case 'n':
							jt = "null"
						default:
							jt = "number"
							// Draft 6 onward reads the value: 1.0 is an integer, so
							// the number is parsed rather than scanned. Every arm
							// below that accepts "number" also accepts "integer", so
							// widening this classification narrows nothing.
							var _ppNum float64
							if json.Unmarshal(b, &_ppNum) == nil &&
								!math.IsInf(_ppNum, 0) && _ppNum == math.Trunc(_ppNum) {
								jt = "integer"
							}
						}
					}
					if jt != "string" {
						return fmt.Errorf("patternProperties %s: key %q value must be string", "^s", k)
					}
				}
				{
					var s string
					if err := json.Unmarshal(v, &s); err == nil {
						if utf8.RuneCountInString(s) < 5 {
							return fmt.Errorf("patternProperties %s: key %q string length is less than minLength 5", "^s", k)
						}
					}
				}
			}
			if ppRegexps[20].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv PatternValueSubschemasPattern20
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^u", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^u", k, _vErr)
					}
				}
			}
		}
	}
	return nil
}
