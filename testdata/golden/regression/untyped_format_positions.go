// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
	"math"
)

// BareV4 accepts any JSON value. Constraints apply only when the value is string.
type BareV4 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (b *BareV4) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		b._raw = append(b._raw[:0], data...)
		b._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &b._value); _err == nil {
		b._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	b._raw = append(b._raw[:0], data...)
	b._isRaw = true
	return nil
}
func (b BareV4) MarshalJSON() ([]byte, error) {
	if b._isRaw {
		if len(b._raw) == 0 {
			return []byte("null"), nil
		}
		return b._raw, nil
	}
	return json.Marshal(b._value)
}
func (b BareV4) StringValue() string { return b._value }
func (b BareV4) IsString() bool      { return !b._isRaw }
func (b BareV4) Raw() json.RawMessage {
	if b._isRaw {
		return b._raw
	}
	_b, _ := json.Marshal(b._value)
	return _b
}
func (b BareV4) String() string {
	if b._isRaw {
		return string(b._raw)
	}
	return fmt.Sprintf("%v", b._value)
}

// Validate checks BareV4 against its JSON Schema constraints.
func (b BareV4) Validate() error {
	if b._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedChainInner accepts any JSON value. Constraints apply only when the value is string.
type UntypedChainInner struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedChainInner) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedChainInner) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedChainInner) StringValue() string { return u._value }
func (u UntypedChainInner) IsString() bool      { return !u._isRaw }
func (u UntypedChainInner) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedChainInner) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedChainInner against its JSON Schema constraints.
func (u UntypedChainInner) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedChainOuter accepts any JSON value. Constraints apply only when the value is string.
type UntypedChainOuter struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedChainOuter) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedChainOuter) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedChainOuter) StringValue() string { return u._value }
func (u UntypedChainOuter) IsString() bool      { return !u._isRaw }
func (u UntypedChainOuter) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedChainOuter) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedChainOuter against its JSON Schema constraints.
func (u UntypedChainOuter) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsBranchOption0 accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsBranchOption0 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsBranchOption0) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsBranchOption0) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsBranchOption0) StringValue() string { return u._value }
func (u UntypedFormatPositionsBranchOption0) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsBranchOption0) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsBranchOption0) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsBranchOption0 against its JSON Schema constraints.
func (u UntypedFormatPositionsBranchOption0) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsBucketsPattern0 accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsBucketsPattern0 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsBucketsPattern0) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsBucketsPattern0) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsBucketsPattern0) StringValue() string { return u._value }
func (u UntypedFormatPositionsBucketsPattern0) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsBucketsPattern0) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsBucketsPattern0) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsBucketsPattern0 against its JSON Schema constraints.
func (u UntypedFormatPositionsBucketsPattern0) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

type UntypedFormatPositionsBuckets struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (u *UntypedFormatPositionsBuckets) UnmarshalJSON(data []byte) error {
	u.AdditionalProperties = nil
	u.PatternProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type UntypedFormatPositionsBuckets")
	}
	type Alias UntypedFormatPositionsBuckets
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(u),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^p", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if u.PatternProperties == nil {
					u.PatternProperties = make(map[string]json.RawMessage)
				}
				u.PatternProperties[rawKey] = rawVal
				continue
			}
			if u.AdditionalProperties == nil {
				u.AdditionalProperties = make(map[string]json.RawMessage)
			}
			u.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (u UntypedFormatPositionsBuckets) MarshalJSON() ([]byte, error) {
	type Alias UntypedFormatPositionsBuckets
	aux := struct {
		Alias
	}{
		Alias: (Alias)(u),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range u.PatternProperties {
		obj[k] = v
	}
	for k, v := range u.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks UntypedFormatPositionsBuckets against its JSON Schema constraints.
func (u UntypedFormatPositionsBuckets) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(u.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^p", ecmaflags.Unicode),
		}
		for k, v := range u.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv UntypedFormatPositionsBucketsPattern0
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^p", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^p", k, _vErr)
					}
				}
			}
		}
	}
	return nil
}

// UntypedFormatPositionsInline accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsInline struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsInline) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsInline) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsInline) StringValue() string { return u._value }
func (u UntypedFormatPositionsInline) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsInline) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsInline) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsInline against its JSON Schema constraints.
func (u UntypedFormatPositionsInline) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsListItem accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsListItem struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsListItem) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsListItem) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsListItem) StringValue() string { return u._value }
func (u UntypedFormatPositionsListItem) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsListItem) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsListItem) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsListItem against its JSON Schema constraints.
func (u UntypedFormatPositionsListItem) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsMail accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsMail struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsMail) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsMail) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsMail) StringValue() string { return u._value }
func (u UntypedFormatPositionsMail) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsMail) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsMail) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsMail against its JSON Schema constraints.
func (u UntypedFormatPositionsMail) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsMapValue accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsMapValue struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsMapValue) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsMapValue) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsMapValue) StringValue() string { return u._value }
func (u UntypedFormatPositionsMapValue) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsMapValue) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsMapValue) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsMapValue against its JSON Schema constraints.
func (u UntypedFormatPositionsMapValue) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsStamp accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsStamp struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsStamp) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsStamp) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsStamp) StringValue() string { return u._value }
func (u UntypedFormatPositionsStamp) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsStamp) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsStamp) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsStamp against its JSON Schema constraints.
func (u UntypedFormatPositionsStamp) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsWrapped accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsWrapped struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsWrapped) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsWrapped) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsWrapped) StringValue() string { return u._value }
func (u UntypedFormatPositionsWrapped) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsWrapped) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsWrapped) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsWrapped against its JSON Schema constraints.
func (u UntypedFormatPositionsWrapped) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositionsTupleItem0 accepts any JSON value. Constraints apply only when the value is string.
type UntypedFormatPositionsTupleItem0 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (u *UntypedFormatPositionsTupleItem0) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		u._raw = append(u._raw[:0], data...)
		u._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &u._value); _err == nil {
		u._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	u._raw = append(u._raw[:0], data...)
	u._isRaw = true
	return nil
}
func (u UntypedFormatPositionsTupleItem0) MarshalJSON() ([]byte, error) {
	if u._isRaw {
		if len(u._raw) == 0 {
			return []byte("null"), nil
		}
		return u._raw, nil
	}
	return json.Marshal(u._value)
}
func (u UntypedFormatPositionsTupleItem0) StringValue() string { return u._value }
func (u UntypedFormatPositionsTupleItem0) IsString() bool      { return !u._isRaw }
func (u UntypedFormatPositionsTupleItem0) Raw() json.RawMessage {
	if u._isRaw {
		return u._raw
	}
	_b, _ := json.Marshal(u._value)
	return _b
}
func (u UntypedFormatPositionsTupleItem0) String() string {
	if u._isRaw {
		return string(u._raw)
	}
	return fmt.Sprintf("%v", u._value)
}

// Validate checks UntypedFormatPositionsTupleItem0 against its JSON Schema constraints.
func (u UntypedFormatPositionsTupleItem0) Validate() error {
	if u._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	return nil
}

// UntypedFormatPositions - A format stated without a type, in every position it can be written. Each resolved to `any`, which Go forbids methods on, so the format was asserted nowhere. The type it deserves is not a string: format is scoped to string instances, so a number, an object, an array, a boolean and a null all satisfy the schema outright.
type UntypedFormatPositions struct {
	Buckets              *UntypedFormatPositionsBuckets            `json:"buckets,omitempty"`
	Chain                *UntypedChainOuter                        `json:"chain,omitempty"`
	Inline               *UntypedFormatPositionsInline             `json:"inline,omitempty"`
	List                 []UntypedFormatPositionsListItem          `json:"list,omitzero"`
	Mail                 *UntypedFormatPositionsMail               `json:"mail,omitempty"`
	Map                  map[string]UntypedFormatPositionsMapValue `json:"map,omitzero"`
	Ref                  *BareV4                                   `json:"ref,omitempty"`
	Stamp                *UntypedFormatPositionsStamp              `json:"stamp,omitempty"`
	Tuple                []any                                     `json:"tuple,omitzero"`
	Wrapped              *UntypedFormatPositionsWrapped            `json:"wrapped,omitempty"`
	Branch               isUntypedFormatPositions_Branch           `json:"-"`
	AdditionalProperties map[string]json.RawMessage                `json:"-"`
	_jsonNulls           map[string]bool                           // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

// isUntypedFormatPositions_Branch is a sealed interface for the Branch field of UntypedFormatPositions.
// Exactly one variant must be set.
type isUntypedFormatPositions_Branch interface {
	isUntypedFormatPositions_Branch()
}

// UntypedFormatPositions_UntypedFormatPositionsBranchOption0 wraps UntypedFormatPositionsBranchOption0 as a Branch variant.
type UntypedFormatPositions_UntypedFormatPositionsBranchOption0 struct {
	UntypedFormatPositionsBranchOption0 UntypedFormatPositionsBranchOption0
}

func (*UntypedFormatPositions_UntypedFormatPositionsBranchOption0) isUntypedFormatPositions_Branch() {
}

// UntypedFormatPositions_Boolean wraps bool as a Branch variant.
type UntypedFormatPositions_Boolean struct {
	Boolean bool
}

func (*UntypedFormatPositions_Boolean) isUntypedFormatPositions_Branch() {}

// GetUntypedFormatPositionsBranchOption0 returns the UntypedFormatPositionsBranchOption0 variant value, or the zero value if not set.
func (u *UntypedFormatPositions) GetUntypedFormatPositionsBranchOption0() UntypedFormatPositionsBranchOption0 {
	if u != nil {
		if v, ok := u.Branch.(*UntypedFormatPositions_UntypedFormatPositionsBranchOption0); ok {
			return v.UntypedFormatPositionsBranchOption0
		}
	}
	var zero UntypedFormatPositionsBranchOption0
	return zero
}

// GetBoolean returns the Boolean variant value, or the zero value if not set.
func (u *UntypedFormatPositions) GetBoolean() bool {
	if u != nil {
		if v, ok := u.Branch.(*UntypedFormatPositions_Boolean); ok {
			return v.Boolean
		}
	}
	var zero bool
	return zero
}

func (u *UntypedFormatPositions) UnmarshalJSON(data []byte) error {
	u.AdditionalProperties = nil
	u._jsonNulls = nil
	u.Branch = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type UntypedFormatPositions")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"buckets",
			"chain",
			"inline",
			"list",
			"mail",
			"map",
			"ref",
			"stamp",
			"tuple",
			"wrapped",
			"branch",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias UntypedFormatPositions
	aux := &struct {
		*Alias
		Branch json.RawMessage `json:"branch"`
	}{
		Alias: (*Alias)(u),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	{
		oneofData := aux.Branch
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isUntypedFormatPositions_Branch
			var oneofStrictErr error

			// Try variant: UntypedFormatPositionsBranchOption0
			{
				var candidate UntypedFormatPositionsBranchOption0
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					u.Branch = &UntypedFormatPositions_UntypedFormatPositionsBranchOption0{UntypedFormatPositionsBranchOption0: candidate}
					oneofMatched++
					if _vErr := candidate.Validate(); _vErr == nil {
						oneofStrict++
						oneofStrictSel = &UntypedFormatPositions_UntypedFormatPositionsBranchOption0{UntypedFormatPositionsBranchOption0: candidate}
					} else {
						oneofStrictErr = fmt.Errorf("variant UntypedFormatPositionsBranchOption0: %w", _vErr)
					}
				} else {
					oneofLastErr = err
				}
			}

			// Try variant: Boolean
			{
				var candidate bool
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					u.Branch = &UntypedFormatPositions_Boolean{Boolean: candidate}
					oneofMatched++
					// Nothing this branch says is left for a Validate to
					// carry, so matching it is satisfying it.
					oneofStrict++
					oneofStrictSel = &UntypedFormatPositions_Boolean{Boolean: candidate}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("UntypedFormatPositions.Branch: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					u.Branch = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return fmt.Errorf("UntypedFormatPositions.Branch: no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return fmt.Errorf("UntypedFormatPositions.Branch: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"buckets",
			"list",
			"map",
			"tuple",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"branch",
			"chain",
			"inline",
			"mail",
			"ref",
			"stamp",
			"wrapped",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if u._jsonNulls == nil {
					u._jsonNulls = make(map[string]bool, 1)
				}
				u._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"buckets": true,
			"chain":   true,
			"inline":  true,
			"list":    true,
			"mail":    true,
			"map":     true,
			"ref":     true,
			"stamp":   true,
			"tuple":   true,
			"wrapped": true,
			"branch":  true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if u.AdditionalProperties == nil {
				u.AdditionalProperties = make(map[string]json.RawMessage)
			}
			u.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (u UntypedFormatPositions) MarshalJSON() ([]byte, error) {
	type Alias UntypedFormatPositions
	aux := struct {
		Alias
		Branch json.RawMessage `json:"branch,omitempty"`
	}{
		Alias: (Alias)(u),
	}

	if u.Branch != nil {
		switch v := u.Branch.(type) {
		case *UntypedFormatPositions_UntypedFormatPositionsBranchOption0:
			raw, err := json.Marshal(v.UntypedFormatPositionsBranchOption0)
			if err != nil {
				return nil, fmt.Errorf("marshaling UntypedFormatPositions.Branch: %w", err)
			}
			aux.Branch = raw
		case *UntypedFormatPositions_Boolean:
			raw, err := json.Marshal(v.Boolean)
			if err != nil {
				return nil, fmt.Errorf("marshaling UntypedFormatPositions.Branch: %w", err)
			}
			aux.Branch = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(u._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range u._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range u.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks UntypedFormatPositions against its JSON Schema constraints.
func (u UntypedFormatPositions) Validate() error {
	if u.Buckets != nil {
		if err := u.Buckets.Validate(); err != nil {
			return jsonPathf(err, "buckets")
		}
	}
	if u.Chain != nil {
		if err := u.Chain.Validate(); err != nil {
			return jsonPathf(err, "chain")
		}
	}
	if u.Inline != nil {
		if err := u.Inline.Validate(); err != nil {
			return jsonPathf(err, "inline")
		}
	}
	for _i, _item := range u.List {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "list[%d]", _i)
		}
	}
	if u.Mail != nil {
		if err := u.Mail.Validate(); err != nil {
			return jsonPathf(err, "mail")
		}
	}
	for _k, _val := range u.Map {
		if err := _val.Validate(); err != nil {
			return jsonPathf(err, "map[%q]", _k)
		}
	}
	if u.Ref != nil {
		if err := u.Ref.Validate(); err != nil {
			return jsonPathf(err, "ref")
		}
	}
	if u.Stamp != nil {
		if err := u.Stamp.Validate(); err != nil {
			return jsonPathf(err, "stamp")
		}
	}
	if u.Wrapped != nil {
		if err := u.Wrapped.Validate(); err != nil {
			return jsonPathf(err, "wrapped")
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := u.Branch.(type) {
	case *UntypedFormatPositions_UntypedFormatPositionsBranchOption0:
		if err := _oneOfSel.UntypedFormatPositionsBranchOption0.Validate(); err != nil {
			return jsonPathf(err, "branch")
		}
	}
	// Tuple items: validate each position against its schema type.
	for _idx, _elem := range u.Tuple {
		_ = _elem
		if _idx == 0 {
			_raw, _mErr := json.Marshal(_elem)
			if _mErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _mErr)
			}
			var _typed UntypedFormatPositionsTupleItem0
			if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _uErr)
			}
			if _vErr := _typed.Validate(); _vErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _vErr)
			}
		}
		if _idx == 1 {
			_cv, _cvOk := _elem.(float64)
			if !_cvOk || _cv != math.Trunc(_cv) {
				return fmt.Errorf("tuple: items[%d]: expected integer, got %T", _idx, _elem)
			}
		}
	}
	return nil
}
