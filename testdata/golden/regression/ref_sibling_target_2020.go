// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	"unicode/utf8"
)

type AnyWord string

func (a *AnyWord) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnyWord")
	}
	type Alias AnyWord
	return json.Unmarshal(data, (*Alias)(a))
}

// Validate checks AnyWord against its JSON Schema constraints.
func (a AnyWord) Validate() error {
	return nil
}

type Long string

func (l *Long) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type Long")
	}
	type Alias Long
	return json.Unmarshal(data, (*Alias)(l))
}

// Validate checks Long against its JSON Schema constraints.
func (l Long) Validate() error {
	if utf8.RuneCountInString(string(l)) < 5 {
		return fmt.Errorf("value: length %d is less than minimum 5", utf8.RuneCountInString(string(l)))
	}
	return nil
}

// NamedConst accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NamedConst struct {
	_raw json.RawMessage
}

func (n *NamedConst) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NamedConst) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NamedConst) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NamedConst) IsZero() bool { return len(n._raw) == 0 }

func (n NamedConst) String() string { return string(n._raw) }

// NamedConstSchema is the schema NamedConst validates against.
var NamedConstSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Const: _strPtr("\"abc\""),
}

// Validate checks NamedConst against its JSON Schema constraints.
func (n NamedConst) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NamedConstSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// NamedEnum accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NamedEnum struct {
	_raw json.RawMessage
}

func (n *NamedEnum) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NamedEnum) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NamedEnum) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NamedEnum) IsZero() bool { return len(n._raw) == 0 }

func (n NamedEnum) String() string { return string(n._raw) }

// NamedEnumSchema is the schema NamedEnum validates against.
var NamedEnumSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Enum: []string{"\"abc\"", "\"abcde\""},
}

// Validate checks NamedEnum against its JSON Schema constraints.
func (n NamedEnum) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NamedEnumSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// RefSiblingTarget2020ConstForbidden accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type RefSiblingTarget2020ConstForbidden struct {
	_raw json.RawMessage
}

func (r *RefSiblingTarget2020ConstForbidden) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefSiblingTarget2020ConstForbidden) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefSiblingTarget2020ConstForbidden) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefSiblingTarget2020ConstForbidden) IsZero() bool { return len(r._raw) == 0 }

func (r RefSiblingTarget2020ConstForbidden) String() string { return string(r._raw) }

// RefSiblingTarget2020ConstForbiddenSchema is the schema RefSiblingTarget2020ConstForbidden validates against.
var RefSiblingTarget2020ConstForbiddenSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Const: _strPtr("\"abc\""),
}

// Validate checks RefSiblingTarget2020ConstForbidden against its JSON Schema constraints.
func (r RefSiblingTarget2020ConstForbidden) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(r._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&RefSiblingTarget2020ConstForbiddenSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// RefSiblingTarget2020ConstSatisfied accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type RefSiblingTarget2020ConstSatisfied struct {
	_raw json.RawMessage
}

func (r *RefSiblingTarget2020ConstSatisfied) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefSiblingTarget2020ConstSatisfied) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefSiblingTarget2020ConstSatisfied) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefSiblingTarget2020ConstSatisfied) IsZero() bool { return len(r._raw) == 0 }

func (r RefSiblingTarget2020ConstSatisfied) String() string { return string(r._raw) }

// RefSiblingTarget2020ConstSatisfiedSchema is the schema RefSiblingTarget2020ConstSatisfied validates against.
var RefSiblingTarget2020ConstSatisfiedSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Const: _strPtr("\"abcde\""),
}

// Validate checks RefSiblingTarget2020ConstSatisfied against its JSON Schema constraints.
func (r RefSiblingTarget2020ConstSatisfied) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(r._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&RefSiblingTarget2020ConstSatisfiedSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type RefSiblingTarget2020DynSibling string

const (
	RefSiblingTarget2020DynSiblingAbc RefSiblingTarget2020DynSibling = "abc"
)

// Validate checks RefSiblingTarget2020DynSibling against its JSON Schema constraints.
func (r RefSiblingTarget2020DynSibling) Validate() error {
	switch r {
	case RefSiblingTarget2020DynSiblingAbc:
		return nil
	default:
		return fmt.Errorf("invalid RefSiblingTarget2020DynSibling value: %v", r)
	}
}

// RefSiblingTarget2020EnumSibling accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type RefSiblingTarget2020EnumSibling struct {
	_raw json.RawMessage
}

func (r *RefSiblingTarget2020EnumSibling) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefSiblingTarget2020EnumSibling) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefSiblingTarget2020EnumSibling) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefSiblingTarget2020EnumSibling) IsZero() bool { return len(r._raw) == 0 }

func (r RefSiblingTarget2020EnumSibling) String() string { return string(r._raw) }

// RefSiblingTarget2020EnumSiblingSchema is the schema RefSiblingTarget2020EnumSibling validates against.
var RefSiblingTarget2020EnumSiblingSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Enum: []string{"\"abc\"", "\"abcde\""},
}

// Validate checks RefSiblingTarget2020EnumSibling against its JSON Schema constraints.
func (r RefSiblingTarget2020EnumSibling) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(r._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&RefSiblingTarget2020EnumSiblingSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// RefSiblingTarget2020ListSiblingItem accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type RefSiblingTarget2020ListSiblingItem struct {
	_raw json.RawMessage
}

func (r *RefSiblingTarget2020ListSiblingItem) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefSiblingTarget2020ListSiblingItem) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefSiblingTarget2020ListSiblingItem) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefSiblingTarget2020ListSiblingItem) IsZero() bool { return len(r._raw) == 0 }

func (r RefSiblingTarget2020ListSiblingItem) String() string { return string(r._raw) }

// RefSiblingTarget2020ListSiblingItemSchema is the schema RefSiblingTarget2020ListSiblingItem validates against.
var RefSiblingTarget2020ListSiblingItemSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Enum: []string{"\"abc\"", "\"abcde\""},
}

// Validate checks RefSiblingTarget2020ListSiblingItem against its JSON Schema constraints.
func (r RefSiblingTarget2020ListSiblingItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(r._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&RefSiblingTarget2020ListSiblingItemSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// RefSiblingTarget2020MapSiblingValue accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type RefSiblingTarget2020MapSiblingValue struct {
	_raw json.RawMessage
}

func (r *RefSiblingTarget2020MapSiblingValue) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefSiblingTarget2020MapSiblingValue) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefSiblingTarget2020MapSiblingValue) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefSiblingTarget2020MapSiblingValue) IsZero() bool { return len(r._raw) == 0 }

func (r RefSiblingTarget2020MapSiblingValue) String() string { return string(r._raw) }

// RefSiblingTarget2020MapSiblingValueSchema is the schema RefSiblingTarget2020MapSiblingValue validates against.
var RefSiblingTarget2020MapSiblingValueSchema = _schemaNode{
	AllOf: []_schemaNode{
		_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(5),
					Type:      []string{"string"},
				},
			},
		},
	},
	Enum: []string{"\"abc\"", "\"abcde\""},
}

// Validate checks RefSiblingTarget2020MapSiblingValue against its JSON Schema constraints.
func (r RefSiblingTarget2020MapSiblingValue) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(r._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&RefSiblingTarget2020MapSiblingValueSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type RefSiblingTarget2020NoRef string

const (
	RefSiblingTarget2020NoRefAbc   RefSiblingTarget2020NoRef = "abc"
	RefSiblingTarget2020NoRefAbcde RefSiblingTarget2020NoRef = "abcde"
)

// Validate checks RefSiblingTarget2020NoRef against its JSON Schema constraints.
func (r RefSiblingTarget2020NoRef) Validate() error {
	switch r {
	case RefSiblingTarget2020NoRefAbc, RefSiblingTarget2020NoRefAbcde:
		return nil
	default:
		return fmt.Errorf("invalid RefSiblingTarget2020NoRef value: %v", r)
	}
}

// RefSiblingTarget2020 - Issue #153, the other half of #151's split. From 2019-09 on a $ref is an ordinary applicator, so an `enum` or a `const` written beside it applies *with* the reference and not instead of it. The enum arm of each type ladder claimed the schema first, so the sibling decided the type and the target was never followed: with the target {"type":"string","minLength":5}, "abc" was accepted at every position although the target forbids it. Every position here states both halves and every one has an accept row: "abcde" satisfies target and sibling alike, "abc" is refused by the target, "zzzzz" by the sibling. noSibling is the control that the target alone still binds, and noRef the control that an enum with no reference beside it is untouched -- it keeps its own type and admits "abc". ref_sibling_values_draft7.json is the control on the other side of the split: there the same shape must keep admitting the document the sibling forbids.
type RefSiblingTarget2020 struct {
	ConstForbidden       RefSiblingTarget2020ConstForbidden             `json:"constForbidden,omitzero"`
	ConstSatisfied       RefSiblingTarget2020ConstSatisfied             `json:"constSatisfied,omitzero"`
	DynSibling           *RefSiblingTarget2020DynSibling                `json:"dynSibling,omitempty"`
	EnumSibling          RefSiblingTarget2020EnumSibling                `json:"enumSibling,omitzero"`
	ListSibling          []RefSiblingTarget2020ListSiblingItem          `json:"listSibling,omitzero"`
	MapSibling           map[string]RefSiblingTarget2020MapSiblingValue `json:"mapSibling,omitzero"`
	NamedConst           NamedConst                                     `json:"namedConst,omitzero"`
	NamedEnum            NamedEnum                                      `json:"namedEnum,omitzero"`
	NoRef                *RefSiblingTarget2020NoRef                     `json:"noRef,omitempty"`
	NoSibling            *Long                                          `json:"noSibling,omitempty"`
	AdditionalProperties map[string]json.RawMessage                     `json:"-"`
	_jsonKeys            map[string]bool                                // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (r *RefSiblingTarget2020) UnmarshalJSON(data []byte) error {
	r.AdditionalProperties = nil
	r._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type RefSiblingTarget2020")
	}
	type Alias RefSiblingTarget2020
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(r),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"constForbidden",
			"constSatisfied",
			"dynSibling",
			"enumSibling",
			"namedConst",
			"namedEnum",
			"noRef",
			"noSibling",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		if _v, ok := raw["listSibling"]; ok {
			if err := checkJSONNulls(_v, "listSibling", &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		if _v, ok := raw["mapSibling"]; ok {
			if err := checkJSONNulls(_v, "mapSibling", &jsonNullRule{Reject: true, IsMap: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		r._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			r._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"constForbidden": true,
			"constSatisfied": true,
			"dynSibling":     true,
			"enumSibling":    true,
			"listSibling":    true,
			"mapSibling":     true,
			"namedConst":     true,
			"namedEnum":      true,
			"noRef":          true,
			"noSibling":      true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if r.AdditionalProperties == nil {
				r.AdditionalProperties = make(map[string]json.RawMessage)
			}
			r.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (r RefSiblingTarget2020) MarshalJSON() ([]byte, error) {
	type Alias RefSiblingTarget2020
	aux := struct {
		Alias
	}{
		Alias: (Alias)(r),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range r.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks RefSiblingTarget2020 against its JSON Schema constraints.
func (r RefSiblingTarget2020) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["constForbidden"] {
		if err := r.ConstForbidden.Validate(); err != nil {
			return fmt.Errorf("constForbidden.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["constSatisfied"] {
		if err := r.ConstSatisfied.Validate(); err != nil {
			return fmt.Errorf("constSatisfied.%w", err)
		}
	}
	if r.DynSibling != nil {
		if err := r.DynSibling.Validate(); err != nil {
			return fmt.Errorf("dynSibling.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["enumSibling"] {
		if err := r.EnumSibling.Validate(); err != nil {
			return fmt.Errorf("enumSibling.%w", err)
		}
	}
	for _i, _item := range r.ListSibling {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("listSibling[%d].%w", _i, err)
		}
	}
	for _k, _val := range r.MapSibling {
		if err := _val.Validate(); err != nil {
			return fmt.Errorf("mapSibling[%q].%w", _k, err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["namedConst"] {
		if err := r.NamedConst.Validate(); err != nil {
			return fmt.Errorf("namedConst.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["namedEnum"] {
		if err := r.NamedEnum.Validate(); err != nil {
			return fmt.Errorf("namedEnum.%w", err)
		}
	}
	if r.NoRef != nil {
		if err := r.NoRef.Validate(); err != nil {
			return fmt.Errorf("noRef.%w", err)
		}
	}
	if r.NoSibling != nil {
		if err := r.NoSibling.Validate(); err != nil {
			return fmt.Errorf("noSibling.%w", err)
		}
	}
	return nil
}
