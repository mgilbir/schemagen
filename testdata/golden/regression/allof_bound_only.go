// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	"unicode/utf8"
)

// Bound accepts any JSON value. Constraints apply only when the value is string.
type Bound struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (b *Bound) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		b._raw = append(b._raw[:0], data...)
		b._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &b._value); _err == nil {
		b._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	b._raw = append(b._raw[:0], data...)
	b._isRaw = true
	return nil
}
func (b Bound) MarshalJSON() ([]byte, error) {
	if b._isRaw {
		if len(b._raw) == 0 {
			return []byte("null"), nil
		}
		return b._raw, nil
	}
	return json.Marshal(b._value)
}
func (b Bound) StringValue() string { return b._value }
func (b Bound) IsString() bool      { return !b._isRaw }
func (b Bound) Raw() json.RawMessage {
	if b._isRaw {
		return b._raw
	}
	_b, _ := json.Marshal(b._value)
	return _b
}
func (b Bound) String() string {
	if b._isRaw {
		return string(b._raw)
	}
	return fmt.Sprintf("%v", b._value)
}

// Validate checks Bound against its JSON Schema constraints.
func (b Bound) Validate() error {
	if b._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(b._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(b._value)))
	}
	return nil
}

// AllOfBoundOnlyArr accepts any JSON value. Constraints apply only when the value is array.
type AllOfBoundOnlyArr struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyArr) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyArr) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyArr) Slice() []any  { return a._value }
func (a AllOfBoundOnlyArr) IsArray() bool { return !a._isRaw }
func (a AllOfBoundOnlyArr) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyArr) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyArr against its JSON Schema constraints.
func (a AllOfBoundOnlyArr) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if len(a._value) < 2 {
		return jsonValueErrorf("has %d items, minimum is 2", len(a._value))
	}
	return nil
}

// AllOfBoundOnlyListItem accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyListItem struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyListItem) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyListItem) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyListItem) StringValue() string { return a._value }
func (a AllOfBoundOnlyListItem) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyListItem) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyListItem) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyListItem against its JSON Schema constraints.
func (a AllOfBoundOnlyListItem) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnlyMapValue accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyMapValue struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyMapValue) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyMapValue) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyMapValue) StringValue() string { return a._value }
func (a AllOfBoundOnlyMapValue) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyMapValue) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyMapValue) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyMapValue against its JSON Schema constraints.
func (a AllOfBoundOnlyMapValue) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnlyNested accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyNested struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyNested) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyNested) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyNested) StringValue() string { return a._value }
func (a AllOfBoundOnlyNested) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyNested) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyNested) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyNested against its JSON Schema constraints.
func (a AllOfBoundOnlyNested) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnlyNum accepts any JSON value. Constraints apply only when the value is number.
type AllOfBoundOnlyNum struct {
	_value float64
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyNum) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyNum) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyNum) Float64() float64 { return a._value }
func (a AllOfBoundOnlyNum) IsNumber() bool   { return !a._isRaw }
func (a AllOfBoundOnlyNum) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyNum) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyNum against its JSON Schema constraints.
func (a AllOfBoundOnlyNum) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if float64(a._value) < 5 {
		return jsonValueErrorf("%v is less than minimum 5", a._value)
	}
	return nil
}

// AllOfBoundOnlyProp accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyProp struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyProp) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyProp) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyProp) StringValue() string { return a._value }
func (a AllOfBoundOnlyProp) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyProp) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyProp) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyProp against its JSON Schema constraints.
func (a AllOfBoundOnlyProp) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnlyUnionOption0 accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyUnionOption0 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyUnionOption0) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyUnionOption0) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyUnionOption0) StringValue() string { return a._value }
func (a AllOfBoundOnlyUnionOption0) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyUnionOption0) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyUnionOption0) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyUnionOption0 against its JSON Schema constraints.
func (a AllOfBoundOnlyUnionOption0) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnlyViaRef accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyViaRef struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyViaRef) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyViaRef) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyViaRef) StringValue() string { return a._value }
func (a AllOfBoundOnlyViaRef) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyViaRef) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyViaRef) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyViaRef against its JSON Schema constraints.
func (a AllOfBoundOnlyViaRef) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnlyTupleItem0 accepts any JSON value. Constraints apply only when the value is string.
type AllOfBoundOnlyTupleItem0 struct {
	_value string
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AllOfBoundOnlyTupleItem0) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AllOfBoundOnlyTupleItem0) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AllOfBoundOnlyTupleItem0) StringValue() string { return a._value }
func (a AllOfBoundOnlyTupleItem0) IsString() bool      { return !a._isRaw }
func (a AllOfBoundOnlyTupleItem0) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AllOfBoundOnlyTupleItem0) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AllOfBoundOnlyTupleItem0 against its JSON Schema constraints.
func (a AllOfBoundOnlyTupleItem0) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if utf8.RuneCountInString(string(a._value)) < 3 {
		return jsonValueErrorf("length %d is less than minimum 3", utf8.RuneCountInString(string(a._value)))
	}
	return nil
}

// AllOfBoundOnly - An allOf branch stating only a bound, in every position: the type is inferred from the keyword, so the value must still accept every other instance type
type AllOfBoundOnly struct {
	Arr                  *AllOfBoundOnlyArr                `json:"arr,omitempty"`
	List                 []AllOfBoundOnlyListItem          `json:"list,omitzero"`
	Map                  map[string]AllOfBoundOnlyMapValue `json:"map,omitzero"`
	Nested               *AllOfBoundOnlyNested             `json:"nested,omitempty"`
	Num                  *AllOfBoundOnlyNum                `json:"num,omitempty"`
	Prop                 *AllOfBoundOnlyProp               `json:"prop,omitempty"`
	Tuple                []any                             `json:"tuple,omitzero"`
	ViaRef               *AllOfBoundOnlyViaRef             `json:"viaRef,omitempty"`
	Union                isAllOfBoundOnly_Union            `json:"-"`
	AdditionalProperties map[string]json.RawMessage        `json:"-"`
	_jsonNulls           map[string]bool                   // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

// isAllOfBoundOnly_Union is a sealed interface for the Union field of AllOfBoundOnly.
// Exactly one variant must be set.
type isAllOfBoundOnly_Union interface {
	isAllOfBoundOnly_Union()
}

// AllOfBoundOnly_AllOfBoundOnlyUnionOption0 wraps AllOfBoundOnlyUnionOption0 as a Union variant.
type AllOfBoundOnly_AllOfBoundOnlyUnionOption0 struct {
	AllOfBoundOnlyUnionOption0 AllOfBoundOnlyUnionOption0
}

func (*AllOfBoundOnly_AllOfBoundOnlyUnionOption0) isAllOfBoundOnly_Union() {}

// AllOfBoundOnly_Boolean wraps bool as a Union variant.
type AllOfBoundOnly_Boolean struct {
	Boolean bool
}

func (*AllOfBoundOnly_Boolean) isAllOfBoundOnly_Union() {}

// GetAllOfBoundOnlyUnionOption0 returns the AllOfBoundOnlyUnionOption0 variant value, or the zero value if not set.
func (a *AllOfBoundOnly) GetAllOfBoundOnlyUnionOption0() AllOfBoundOnlyUnionOption0 {
	if a != nil {
		if v, ok := a.Union.(*AllOfBoundOnly_AllOfBoundOnlyUnionOption0); ok {
			return v.AllOfBoundOnlyUnionOption0
		}
	}
	var zero AllOfBoundOnlyUnionOption0
	return zero
}

// GetBoolean returns the Boolean variant value, or the zero value if not set.
func (a *AllOfBoundOnly) GetBoolean() bool {
	if a != nil {
		if v, ok := a.Union.(*AllOfBoundOnly_Boolean); ok {
			return v.Boolean
		}
	}
	var zero bool
	return zero
}

func (a *AllOfBoundOnly) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonNulls = nil
	a.Union = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"arr",
			"list",
			"map",
			"nested",
			"num",
			"prop",
			"tuple",
			"viaRef",
			"union",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBoundOnly
	aux := &struct {
		*Alias
		Union json.RawMessage `json:"union"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"arr", jsonDecodeValue[*AllOfBoundOnlyArr]},
			{"list", jsonDecodeItems(jsonDecodeValue[AllOfBoundOnlyListItem])},
			{"map", jsonDecodeValues(jsonDecodeValue[AllOfBoundOnlyMapValue])},
			{"nested", jsonDecodeValue[*AllOfBoundOnlyNested]},
			{"num", jsonDecodeValue[*AllOfBoundOnlyNum]},
			{"prop", jsonDecodeValue[*AllOfBoundOnlyProp]},
			{"tuple", jsonDecodeItems(jsonDecodeValue[any])},
			{"viaRef", jsonDecodeValue[*AllOfBoundOnlyViaRef]},
		})
	}

	{
		oneofData := aux.Union
		// Every refusal this union raises is a sentence about the value the union
		// holds, and the property that reaches it goes in front of that sentence
		// by the rule every other message is joined by (see jsonPathError).
		//
		// What used to go in front was a Go type and a Go field pasted in by hand:
		// AllOfBoundOnly.Union
		// A caller cannot find either in the document they sent, and at any depth
		// but the first the pair does not even resemble the path. Issue #289.
		oneofErrf := func(format string, args ...any) error {
			return jsonPathf(jsonValueErrorf(format, args...), "%s", "union")
		}
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isAllOfBoundOnly_Union
			var oneofStrictErr error

			// Try variant: AllOfBoundOnlyUnionOption0
			{
				var candidate AllOfBoundOnlyUnionOption0
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					a.Union = &AllOfBoundOnly_AllOfBoundOnlyUnionOption0{AllOfBoundOnlyUnionOption0: candidate}
					oneofMatched++
					if _vErr := candidate.Validate(); _vErr == nil {
						oneofStrict++
						oneofStrictSel = &AllOfBoundOnly_AllOfBoundOnlyUnionOption0{AllOfBoundOnlyUnionOption0: candidate}
					} else {
						oneofStrictErr = fmt.Errorf("variant AllOfBoundOnlyUnionOption0: %w", _vErr)
					}
				} else {
					oneofLastErr = err
				}
			}

			// Try variant: Boolean
			{
				var candidate bool
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					a.Union = &AllOfBoundOnly_Boolean{Boolean: candidate}
					oneofMatched++
					// Nothing this branch says is left for a Validate to
					// carry, so matching it is satisfying it.
					oneofStrict++
					oneofStrictSel = &AllOfBoundOnly_Boolean{Boolean: candidate}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				// A branch is only put to a decode once the keys it selects on are
				// present, so a value that answers none of them leaves no branch
				// reason behind at all -- and the %w below then had nothing to
				// wrap and printed "%!w(<nil>)", which says nothing true about
				// anything. The refusal stands on its own in that case. Issue #289.
				if oneofLastErr == nil {
					return oneofErrf("no matching oneOf variant")
				}
				return oneofErrf("no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					a.Union = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return oneofErrf("no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return oneofErrf("multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"list",
			"map",
			"tuple",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"arr",
			"nested",
			"num",
			"prop",
			"union",
			"viaRef",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if a._jsonNulls == nil {
					a._jsonNulls = make(map[string]bool, 1)
				}
				a._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"arr":    true,
			"list":   true,
			"map":    true,
			"nested": true,
			"num":    true,
			"prop":   true,
			"tuple":  true,
			"viaRef": true,
			"union":  true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBoundOnly) MarshalJSON() ([]byte, error) {
	type Alias AllOfBoundOnly
	aux := struct {
		Alias
		Union json.RawMessage `json:"union,omitempty"`
	}{
		Alias: (Alias)(a),
	}

	if a.Union != nil {
		switch v := a.Union.(type) {
		case *AllOfBoundOnly_AllOfBoundOnlyUnionOption0:
			raw, err := json.Marshal(v.AllOfBoundOnlyUnionOption0)
			if err != nil {
				return nil, fmt.Errorf("marshaling AllOfBoundOnly.Union: %w", err)
			}
			aux.Union = raw
		case *AllOfBoundOnly_Boolean:
			raw, err := json.Marshal(v.Boolean)
			if err != nil {
				return nil, fmt.Errorf("marshaling AllOfBoundOnly.Union: %w", err)
			}
			aux.Union = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(a._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range a._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBoundOnly against its JSON Schema constraints.
func (a AllOfBoundOnly) Validate() error {
	if a.Arr != nil {
		if err := a.Arr.Validate(); err != nil {
			return jsonPathf(err, "arr")
		}
	}
	for _i, _item := range a.List {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "list[%d]", _i)
		}
	}
	for _k, _val := range a.Map {
		if err := _val.Validate(); err != nil {
			return jsonPathf(err, "map[%q]", _k)
		}
	}
	if a.Nested != nil {
		if err := a.Nested.Validate(); err != nil {
			return jsonPathf(err, "nested")
		}
	}
	if a.Num != nil {
		if err := a.Num.Validate(); err != nil {
			return jsonPathf(err, "num")
		}
	}
	if a.Prop != nil {
		if err := a.Prop.Validate(); err != nil {
			return jsonPathf(err, "prop")
		}
	}
	if a.ViaRef != nil {
		if err := a.ViaRef.Validate(); err != nil {
			return jsonPathf(err, "viaRef")
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := a.Union.(type) {
	case *AllOfBoundOnly_AllOfBoundOnlyUnionOption0:
		if err := _oneOfSel.AllOfBoundOnlyUnionOption0.Validate(); err != nil {
			return jsonPathf(err, "union")
		}
	}
	// Tuple items: validate each position against its schema type.
	for _idx, _elem := range a.Tuple {
		_ = _elem
		if _idx == 0 {
			_raw, _mErr := json.Marshal(_elem)
			if _mErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _mErr)
			}
			var _typed AllOfBoundOnlyTupleItem0
			if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _uErr)
			}
			if _vErr := _typed.Validate(); _vErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _vErr)
			}
		}
	}
	return nil
}
