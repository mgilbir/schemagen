// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

type InferredArrayRootItem struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (i *InferredArrayRootItem) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias InferredArrayRootItem
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return jsonDecodeRefusal(err)
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InferredArrayRootItem) MarshalJSON() ([]byte, error) {
	type Alias InferredArrayRootItem
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InferredArrayRootItem against its JSON Schema constraints.
func (i InferredArrayRootItem) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if i._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !i._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// InferredArrayRoot - Issue #166 at the one position a document has only one of: the root. The whole schema is `items` and nothing else, so the root type is the wrapper that accepts any JSON value and applies the element check only to an array -- which is what the four non-array documents below are for.
// InferredArrayRoot accepts any JSON value. Constraints apply only when the value is array.
type InferredArrayRoot struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InferredArrayRoot) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InferredArrayRoot) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InferredArrayRoot) Slice() []any  { return i._value }
func (i InferredArrayRoot) IsArray() bool { return !i._isRaw }
func (i InferredArrayRoot) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InferredArrayRoot) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InferredArrayRoot against its JSON Schema constraints.
func (i InferredArrayRoot) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	for _i, _elem := range i._value {
		_raw, _mErr := json.Marshal(_elem)
		if _mErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _mErr)
		}
		var _typed InferredArrayRootItem
		if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _uErr)
		}
		if _vErr := _typed.Validate(); _vErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _vErr)
		}
	}
	return nil
}
