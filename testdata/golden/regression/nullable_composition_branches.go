// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	"unicode/utf8"
)

// NullableCompositionBranchesAnyBound accepts any JSON value. The schema declares no type of its own, so
// the constraints come entirely from its applicators and are checked against
// the decoded value by Validate.
type NullableCompositionBranchesAnyBound struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyBound) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyBound) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyBound) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyBound) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyBound) String() string { return string(n._raw) }

// Validate checks NullableCompositionBranchesAnyBound against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyBound) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	{
		_matches := 0
		if _dynIsInteger(_v) && _dynNumOK(_v, func(f float64) bool { return f >= 5.0 }) {
			_matches++
		}
		if _v == nil {
			_matches++
		}
		if _matches == 0 {
			return fmt.Errorf("anyOf: value matches no variant")
		}
	}
	return nil
}

// NullableCompositionBranchesAnyConst accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesAnyConst struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyConst) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyConst) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyConst) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyConst) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyConst) String() string { return string(n._raw) }

// NullableCompositionBranchesAnyConstSchema is the schema NullableCompositionBranchesAnyConst validates against.
var NullableCompositionBranchesAnyConstSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
			Type:  []string{"string"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesAnyConst against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyConst) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesAnyConstSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

// NullableCompositionBranchesAnyEmpty accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesAnyEmpty struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyEmpty) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyEmpty) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyEmpty) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyEmpty) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyEmpty) String() string { return string(n._raw) }

// NullableCompositionBranchesAnyEmptySchema is the schema NullableCompositionBranchesAnyEmpty validates against.
var NullableCompositionBranchesAnyEmptySchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{Boolean: _boolPtr(false)},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesAnyEmpty against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyEmpty) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesAnyEmptySchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

type NullableCompositionBranchesAnyEnum string

const (
	NullableCompositionBranchesAnyEnumA NullableCompositionBranchesAnyEnum = "a"
	NullableCompositionBranchesAnyEnumC NullableCompositionBranchesAnyEnum = "c"
)

// Validate checks NullableCompositionBranchesAnyEnum against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyEnum) Validate() error {
	switch n {
	case NullableCompositionBranchesAnyEnumA, NullableCompositionBranchesAnyEnumC:
		return nil
	default:
		return jsonValueErrorf("invalid NullableCompositionBranchesAnyEnum value: %v", n)
	}
}

// NullableCompositionBranchesAnyFalse accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesAnyFalse struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyFalse) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyFalse) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyFalse) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyFalse) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyFalse) String() string { return string(n._raw) }

// NullableCompositionBranchesAnyFalseSchema is the schema NullableCompositionBranchesAnyFalse validates against.
var NullableCompositionBranchesAnyFalseSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{Boolean: _boolPtr(false)},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesAnyFalse against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyFalse) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesAnyFalseSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

// NullableCompositionBranchesAnyItems accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesAnyItems struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyItems) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyItems) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyItems) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyItems) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyItems) String() string { return string(n._raw) }

// NullableCompositionBranchesAnyItemsSchema is the schema NullableCompositionBranchesAnyItems validates against.
var NullableCompositionBranchesAnyItemsSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Items: _node(_schemaNode{
				MinLength: _intPtr(3),
				Type:      []string{"string"},
			}),
			Type: []string{"array"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesAnyItems against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyItems) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesAnyItemsSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

// NullableCompositionBranchesAnyLen accepts any JSON value. The schema declares no type of its own, so
// the constraints come entirely from its applicators and are checked against
// the decoded value by Validate.
type NullableCompositionBranchesAnyLen struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyLen) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyLen) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyLen) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyLen) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyLen) String() string { return string(n._raw) }

// Validate checks NullableCompositionBranchesAnyLen against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyLen) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	{
		_matches := 0
		if _dynIsString(_v) && _dynStrOK(_v, func(s string) bool { return utf8.RuneCountInString(s) >= 3 }) {
			_matches++
		}
		if _v == nil {
			_matches++
		}
		if _matches == 0 {
			return fmt.Errorf("anyOf: value matches no variant")
		}
	}
	return nil
}

// NullableCompositionBranchesAnyMinIt accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesAnyMinIt struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesAnyMinIt) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesAnyMinIt) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesAnyMinIt) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesAnyMinIt) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesAnyMinIt) String() string { return string(n._raw) }

// NullableCompositionBranchesAnyMinItSchema is the schema NullableCompositionBranchesAnyMinIt validates against.
var NullableCompositionBranchesAnyMinItSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			MinItems: _intPtr(2),
			Type:     []string{"array"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesAnyMinIt against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyMinIt) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesAnyMinItSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

type NullableCompositionBranchesAnyObj struct {
	K                    string                     `json:"k"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (n *NullableCompositionBranchesAnyObj) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias NullableCompositionBranchesAnyObj
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"k", jsonDecodeValue[string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NullableCompositionBranchesAnyObj) MarshalJSON() ([]byte, error) {
	type Alias NullableCompositionBranchesAnyObj
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NullableCompositionBranchesAnyObj against its JSON Schema constraints.
func (n NullableCompositionBranchesAnyObj) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if n._jsonKeys != nil {
		for _, _req := range []string{"k"} {
			if !n._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// NullableCompositionBranchesOneConst accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesOneConst struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesOneConst) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesOneConst) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesOneConst) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesOneConst) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesOneConst) String() string { return string(n._raw) }

// NullableCompositionBranchesOneConstSchema is the schema NullableCompositionBranchesOneConst validates against.
var NullableCompositionBranchesOneConstSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
			Type:  []string{"string"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesOneConst against its JSON Schema constraints.
func (n NullableCompositionBranchesOneConst) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesOneConstSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

// NullableCompositionBranchesOneMapVal accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableCompositionBranchesOneMapVal struct {
	_raw json.RawMessage
}

func (n *NullableCompositionBranchesOneMapVal) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableCompositionBranchesOneMapVal) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableCompositionBranchesOneMapVal) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableCompositionBranchesOneMapVal) IsZero() bool { return len(n._raw) == 0 }

func (n NullableCompositionBranchesOneMapVal) String() string { return string(n._raw) }

// NullableCompositionBranchesOneMapValSchema is the schema NullableCompositionBranchesOneMapVal validates against.
var NullableCompositionBranchesOneMapValSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			AdditionalProperties: _node(_schemaNode{
				MinLength: _intPtr(3),
				Type:      []string{"string"},
			}),
			Type: []string{"object"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableCompositionBranchesOneMapVal against its JSON Schema constraints.
func (n NullableCompositionBranchesOneMapVal) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableCompositionBranchesOneMapValSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}

// NullableCompositionBranches - A composition written as {X, {"type":"null"}} collapsed to a pointer at X's own Go type, and nothing then read X again: the keywords a property carries on its parent's field, and the element checks an array carries beside it, are both taken from the property schema, which here is the wrapper and states none of them. So anyConst accepted "b", anyLen accepted "ab", anyItems accepted ["ab"], anyMinIt accepted [], anyBound accepted 1, oneMapVal accepted {"k":"a"}, and anyFalse and anyEmpty accepted documents no branch admits. anyEnum, anyObj, anyPlain and anyArr are the controls: the first two get a type of their own, whose Validate the field calls, and the last two state nothing their Go type does not already say.
type NullableCompositionBranches struct {
	AnyArr               *[]string                            `json:"anyArr,omitempty"`
	AnyBound             NullableCompositionBranchesAnyBound  `json:"anyBound,omitzero"`
	AnyConst             NullableCompositionBranchesAnyConst  `json:"anyConst,omitzero"`
	AnyEmpty             NullableCompositionBranchesAnyEmpty  `json:"anyEmpty,omitzero"`
	AnyEnum              *NullableCompositionBranchesAnyEnum  `json:"anyEnum,omitempty"`
	AnyFalse             NullableCompositionBranchesAnyFalse  `json:"anyFalse,omitzero"`
	AnyItems             NullableCompositionBranchesAnyItems  `json:"anyItems,omitzero"`
	AnyLen               NullableCompositionBranchesAnyLen    `json:"anyLen,omitzero"`
	AnyMinIt             NullableCompositionBranchesAnyMinIt  `json:"anyMinIt,omitzero"`
	AnyObj               *NullableCompositionBranchesAnyObj   `json:"anyObj,omitempty"`
	AnyPlain             *string                              `json:"anyPlain,omitempty"`
	OneConst             NullableCompositionBranchesOneConst  `json:"oneConst,omitzero"`
	OneMapVal            NullableCompositionBranchesOneMapVal `json:"oneMapVal,omitzero"`
	AdditionalProperties map[string]json.RawMessage           `json:"-"`
	_jsonKeys            map[string]bool                      // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool                      // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (n *NullableCompositionBranches) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	n._jsonNulls = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"anyArr",
			"anyBound",
			"anyConst",
			"anyEmpty",
			"anyEnum",
			"anyFalse",
			"anyItems",
			"anyLen",
			"anyMinIt",
			"anyObj",
			"anyPlain",
			"oneConst",
			"oneMapVal",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias NullableCompositionBranches
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"anyArr", jsonDecodeValue[*[]string]},
			{"anyBound", jsonDecodeValue[NullableCompositionBranchesAnyBound]},
			{"anyConst", jsonDecodeValue[NullableCompositionBranchesAnyConst]},
			{"anyEmpty", jsonDecodeValue[NullableCompositionBranchesAnyEmpty]},
			{"anyEnum", jsonDecodeValue[*NullableCompositionBranchesAnyEnum]},
			{"anyFalse", jsonDecodeValue[NullableCompositionBranchesAnyFalse]},
			{"anyItems", jsonDecodeValue[NullableCompositionBranchesAnyItems]},
			{"anyLen", jsonDecodeValue[NullableCompositionBranchesAnyLen]},
			{"anyMinIt", jsonDecodeValue[NullableCompositionBranchesAnyMinIt]},
			{"anyObj", jsonDecodeValue[*NullableCompositionBranchesAnyObj]},
			{"anyPlain", jsonDecodeValue[*string]},
			{"oneConst", jsonDecodeValue[NullableCompositionBranchesOneConst]},
			{"oneMapVal", jsonDecodeValue[NullableCompositionBranchesOneMapVal]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"anyArr",
			"anyEnum",
			"anyObj",
			"anyPlain",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if n._jsonNulls == nil {
					n._jsonNulls = make(map[string]bool, 1)
				}
				n._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"anyArr":    true,
			"anyBound":  true,
			"anyConst":  true,
			"anyEmpty":  true,
			"anyEnum":   true,
			"anyFalse":  true,
			"anyItems":  true,
			"anyLen":    true,
			"anyMinIt":  true,
			"anyObj":    true,
			"anyPlain":  true,
			"oneConst":  true,
			"oneMapVal": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NullableCompositionBranches) MarshalJSON() ([]byte, error) {
	type Alias NullableCompositionBranches
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(n._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range n._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NullableCompositionBranches against its JSON Schema constraints.
func (n NullableCompositionBranches) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyBound"] {
		if err := n.AnyBound.Validate(); err != nil {
			return jsonPathf(err, "anyBound")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyConst"] {
		if err := n.AnyConst.Validate(); err != nil {
			return jsonPathf(err, "anyConst")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyEmpty"] {
		if err := n.AnyEmpty.Validate(); err != nil {
			return jsonPathf(err, "anyEmpty")
		}
	}
	if n.AnyEnum != nil {
		if err := n.AnyEnum.Validate(); err != nil {
			return jsonPathf(err, "anyEnum")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyFalse"] {
		if err := n.AnyFalse.Validate(); err != nil {
			return jsonPathf(err, "anyFalse")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyItems"] {
		if err := n.AnyItems.Validate(); err != nil {
			return jsonPathf(err, "anyItems")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyLen"] {
		if err := n.AnyLen.Validate(); err != nil {
			return jsonPathf(err, "anyLen")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["anyMinIt"] {
		if err := n.AnyMinIt.Validate(); err != nil {
			return jsonPathf(err, "anyMinIt")
		}
	}
	if n.AnyObj != nil {
		if err := n.AnyObj.Validate(); err != nil {
			return jsonPathf(err, "anyObj")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["oneConst"] {
		if err := n.OneConst.Validate(); err != nil {
			return jsonPathf(err, "oneConst")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["oneMapVal"] {
		if err := n.OneMapVal.Validate(); err != nil {
			return jsonPathf(err, "oneMapVal")
		}
	}
	return nil
}
