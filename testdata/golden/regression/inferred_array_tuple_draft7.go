// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

type NeedsA struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (n *NeedsA) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type NeedsA")
	}
	type Alias NeedsA
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NeedsA) MarshalJSON() ([]byte, error) {
	type Alias NeedsA
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NeedsA against its JSON Schema constraints.
func (n NeedsA) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if n._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !n._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type InferredArrayTupleDraft7OneItem struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (i *InferredArrayTupleDraft7OneItem) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InferredArrayTupleDraft7OneItem")
	}
	type Alias InferredArrayTupleDraft7OneItem
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InferredArrayTupleDraft7OneItem) MarshalJSON() ([]byte, error) {
	type Alias InferredArrayTupleDraft7OneItem
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InferredArrayTupleDraft7OneItem against its JSON Schema constraints.
func (i InferredArrayTupleDraft7OneItem) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if i._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !i._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// InferredArrayTupleDraft7One accepts any JSON value. Constraints apply only when the value is array.
type InferredArrayTupleDraft7One struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InferredArrayTupleDraft7One) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InferredArrayTupleDraft7One) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InferredArrayTupleDraft7One) Slice() []any  { return i._value }
func (i InferredArrayTupleDraft7One) IsArray() bool { return !i._isRaw }
func (i InferredArrayTupleDraft7One) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InferredArrayTupleDraft7One) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InferredArrayTupleDraft7One against its JSON Schema constraints.
func (i InferredArrayTupleDraft7One) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	for _i, _elem := range i._value {
		_raw, _mErr := json.Marshal(_elem)
		if _mErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _mErr)
		}
		var _typed InferredArrayTupleDraft7OneItem
		if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _uErr)
		}
		if _vErr := _typed.Validate(); _vErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _vErr)
		}
	}
	return nil
}

// InferredArrayTupleDraft7Ref accepts any JSON value. Constraints apply only when the value is array.
type InferredArrayTupleDraft7Ref struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InferredArrayTupleDraft7Ref) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InferredArrayTupleDraft7Ref) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InferredArrayTupleDraft7Ref) Slice() []any  { return i._value }
func (i InferredArrayTupleDraft7Ref) IsArray() bool { return !i._isRaw }
func (i InferredArrayTupleDraft7Ref) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InferredArrayTupleDraft7Ref) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InferredArrayTupleDraft7Ref against its JSON Schema constraints.
func (i InferredArrayTupleDraft7Ref) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	for _i, _elem := range i._value {
		_raw, _mErr := json.Marshal(_elem)
		if _mErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _mErr)
		}
		var _typed NeedsA
		if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _uErr)
		}
		if _vErr := _typed.Validate(); _vErr != nil {
			return fmt.Errorf("items[%d]: %w", _i, _vErr)
		}
	}
	return nil
}

type InferredArrayTupleDraft7TupItem0 struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (i *InferredArrayTupleDraft7TupItem0) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InferredArrayTupleDraft7TupItem0")
	}
	type Alias InferredArrayTupleDraft7TupItem0
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InferredArrayTupleDraft7TupItem0) MarshalJSON() ([]byte, error) {
	type Alias InferredArrayTupleDraft7TupItem0
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InferredArrayTupleDraft7TupItem0 against its JSON Schema constraints.
func (i InferredArrayTupleDraft7TupItem0) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if i._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !i._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type InferredArrayTupleDraft7TupRest struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (i *InferredArrayTupleDraft7TupRest) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InferredArrayTupleDraft7TupRest")
	}
	type Alias InferredArrayTupleDraft7TupRest
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InferredArrayTupleDraft7TupRest) MarshalJSON() ([]byte, error) {
	type Alias InferredArrayTupleDraft7TupRest
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InferredArrayTupleDraft7TupRest against its JSON Schema constraints.
func (i InferredArrayTupleDraft7TupRest) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if i._jsonKeys != nil {
		for _, _req := range []string{"b"} {
			if !i._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// InferredArrayTupleDraft7Tup accepts any JSON value. Constraints apply only when the value is array.
type InferredArrayTupleDraft7Tup struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InferredArrayTupleDraft7Tup) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InferredArrayTupleDraft7Tup) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InferredArrayTupleDraft7Tup) Slice() []any  { return i._value }
func (i InferredArrayTupleDraft7Tup) IsArray() bool { return !i._isRaw }
func (i InferredArrayTupleDraft7Tup) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InferredArrayTupleDraft7Tup) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InferredArrayTupleDraft7Tup against its JSON Schema constraints.
func (i InferredArrayTupleDraft7Tup) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	{
		if len(i._value) > 0 {
			_raw, _mErr := json.Marshal(i._value[0])
			if _mErr != nil {
				return fmt.Errorf("items[0]: %w", _mErr)
			}
			var _typed InferredArrayTupleDraft7TupItem0
			if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
				return fmt.Errorf("items[0]: %w", _uErr)
			}
			if _vErr := _typed.Validate(); _vErr != nil {
				return fmt.Errorf("items[0]: %w", _vErr)
			}
		}
	}
	for _i := 1; _i < len(i._value); _i++ {
		_raw, _mErr := json.Marshal(i._value[_i])
		if _mErr != nil {
			return fmt.Errorf("additionalItems[%d]: %w", _i, _mErr)
		}
		var _typed InferredArrayTupleDraft7TupRest
		if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
			return fmt.Errorf("additionalItems[%d]: %w", _i, _uErr)
		}
		if _vErr := _typed.Validate(); _vErr != nil {
			return fmt.Errorf("additionalItems[%d]: %w", _i, _vErr)
		}
	}
	return nil
}

// InferredArrayTupleDraft7 - Issue #166 in the tuple spelling drafts 4 to 7 use: `items` as an array of schemas, with `additionalItems` governing everything past it. Both took the same reduction as the 2020-12 prefixItems form -- each position kept its declared JSON type and dropped the rest -- and neither is reachable from a 2020-12 document, so the draft matters.
type InferredArrayTupleDraft7 struct {
	One                  *InferredArrayTupleDraft7One `json:"one,omitempty"`
	Ref                  *InferredArrayTupleDraft7Ref `json:"ref,omitempty"`
	Tup                  *InferredArrayTupleDraft7Tup `json:"tup,omitempty"`
	AdditionalProperties map[string]json.RawMessage   `json:"-"`
	_jsonNulls           map[string]bool              // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (i *InferredArrayTupleDraft7) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonNulls = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InferredArrayTupleDraft7")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"one",
			"ref",
			"tup",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias InferredArrayTupleDraft7
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"one",
			"ref",
			"tup",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if i._jsonNulls == nil {
					i._jsonNulls = make(map[string]bool, 1)
				}
				i._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"one": true,
			"ref": true,
			"tup": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InferredArrayTupleDraft7) MarshalJSON() ([]byte, error) {
	type Alias InferredArrayTupleDraft7
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(i._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range i._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InferredArrayTupleDraft7 against its JSON Schema constraints.
func (i InferredArrayTupleDraft7) Validate() error {
	if i.One != nil {
		if err := i.One.Validate(); err != nil {
			return jsonPathf(err, "one")
		}
	}
	if i.Ref != nil {
		if err := i.Ref.Validate(); err != nil {
			return jsonPathf(err, "ref")
		}
	}
	if i.Tup != nil {
		if err := i.Tup.Validate(); err != nil {
			return jsonPathf(err, "tup")
		}
	}
	return nil
}
