// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	"math"
)

type Readings []float64

func (r *Readings) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	if _err := checkJSONNullsAt(data, &jsonNullRule{Elem: &jsonNullRule{Reject: true}}); _err != nil {
		return _err
	}
	type Alias Readings
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(r)))
}

// Validate checks Readings against its JSON Schema constraints.
func (r Readings) Validate() error {
	return nil
}

type Temperature float64

func (t *Temperature) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias Temperature
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(t)))
}

// Validate checks Temperature against its JSON Schema constraints.
func (t Temperature) Validate() error {
	if float64(t) < -273.15 {
		return jsonValueErrorf("%v is less than minimum -273.15", t)
	}
	return nil
}

type NumberPositionsChoices float64

const (
	NumberPositionsChoices1_5 NumberPositionsChoices = 1.5
	NumberPositionsChoices2_5 NumberPositionsChoices = 2.5
)

// Validate checks NumberPositionsChoices against its JSON Schema constraints.
func (n NumberPositionsChoices) Validate() error {
	switch n {
	case NumberPositionsChoices1_5, NumberPositionsChoices2_5:
		return nil
	default:
		return jsonValueErrorf("invalid NumberPositionsChoices value: %v", n)
	}
}

// NumberPositionsConstrained accepts any JSON value. Constraints apply only when the value is number.
type NumberPositionsConstrained struct {
	_value float64
	_raw   json.RawMessage
	_isRaw bool
}

func (n *NumberPositionsConstrained) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		n._raw = append(n._raw[:0], data...)
		n._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &n._value); _err == nil {
		n._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	n._raw = append(n._raw[:0], data...)
	n._isRaw = true
	return nil
}
func (n NumberPositionsConstrained) MarshalJSON() ([]byte, error) {
	if n._isRaw {
		if len(n._raw) == 0 {
			return []byte("null"), nil
		}
		return n._raw, nil
	}
	return json.Marshal(n._value)
}
func (n NumberPositionsConstrained) Float64() float64 { return n._value }
func (n NumberPositionsConstrained) IsNumber() bool   { return !n._isRaw }
func (n NumberPositionsConstrained) Raw() json.RawMessage {
	if n._isRaw {
		return n._raw
	}
	_b, _ := json.Marshal(n._value)
	return _b
}
func (n NumberPositionsConstrained) String() string {
	if n._isRaw {
		return string(n._raw)
	}
	return fmt.Sprintf("%v", n._value)
}

// Validate checks NumberPositionsConstrained against its JSON Schema constraints.
func (n NumberPositionsConstrained) Validate() error {
	if n._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if float64(n._value) < 3.5 {
		return jsonValueErrorf("%v is less than minimum 3.5", n._value)
	}
	return nil
}

// NumberPositions - Every position a "number" can occupy, read twice: once under the default configuration, where it is a float64, and once under --exact-numbers, where it is the literal the document wrote. The two goldens are the same document and differ in that and nothing else, which is what makes the flag's reach readable rather than asserted. Issue #252.
type NumberPositions struct {
	Aliased              *Temperature                `json:"aliased,omitempty"`
	AliasedList          Readings                    `json:"aliasedList,omitzero"`
	Bounded              *float64                    `json:"bounded,omitempty"`
	Choices              *NumberPositionsChoices     `json:"choices,omitempty"`
	Constant             *float64                    `json:"constant,omitempty"`
	Constrained          *NumberPositionsConstrained `json:"constrained,omitempty"`
	Counted              []float64                   `json:"counted,omitzero"`
	Elements             []float64                   `json:"elements,omitzero"`
	IntegerBeside        *int64                      `json:"integerBeside,omitempty"`
	Nullable             *float64                    `json:"nullable,omitempty"`
	Required             float64                     `json:"required"`
	Scalar               *float64                    `json:"scalar,omitempty"`
	Values               map[string]float64          `json:"values,omitzero"`
	WithDefault          *float64                    `json:"withDefault,omitempty"`
	AdditionalProperties map[string]json.RawMessage  `json:"-"`
	_jsonKeys            map[string]bool             // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool             // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (n *NumberPositions) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	n._jsonNulls = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"aliased",
			"aliasedList",
			"bounded",
			"choices",
			"constant",
			"constrained",
			"counted",
			"elements",
			"integerBeside",
			"nullable",
			"required",
			"scalar",
			"values",
			"withDefault",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias NumberPositions
	aux := &struct {
		*Alias
		IntegerBeside **jsonInteger `json:"integerBeside"`
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"aliased", jsonDecodeValue[*Temperature]},
			{"aliasedList", jsonDecodeValue[Readings]},
			{"bounded", jsonDecodeValue[*float64]},
			{"choices", jsonDecodeValue[*NumberPositionsChoices]},
			{"constant", jsonDecodeValue[*float64]},
			{"constrained", jsonDecodeValue[*NumberPositionsConstrained]},
			{"counted", jsonDecodeItems(jsonDecodeValue[float64])},
			{"elements", jsonDecodeItems(jsonDecodeValue[float64])},
			{"integerBeside", jsonDecodeValue[*jsonInteger]},
			{"nullable", jsonDecodeValue[*float64]},
			{"required", jsonDecodeValue[float64]},
			{"scalar", jsonDecodeValue[*float64]},
			{"values", jsonDecodeValues(jsonDecodeValue[float64])},
			{"withDefault", jsonDecodeValue[*float64]},
		})
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.IntegerBeside != nil {
		_iv := *aux.IntegerBeside
		n.IntegerBeside = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"aliased",
			"aliasedList",
			"bounded",
			"choices",
			"constant",
			"integerBeside",
			"required",
			"scalar",
			"withDefault",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		if _v, ok := raw["counted"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "counted")
			}
		}
		if _v, ok := raw["elements"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "elements")
			}
		}
		if _v, ok := raw["values"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, IsMap: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "values")
			}
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"constrained",
			"nullable",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if n._jsonNulls == nil {
					n._jsonNulls = make(map[string]bool, 1)
				}
				n._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"aliased":       true,
			"aliasedList":   true,
			"bounded":       true,
			"choices":       true,
			"constant":      true,
			"constrained":   true,
			"counted":       true,
			"elements":      true,
			"integerBeside": true,
			"nullable":      true,
			"required":      true,
			"scalar":        true,
			"values":        true,
			"withDefault":   true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NumberPositions) MarshalJSON() ([]byte, error) {
	type Alias NumberPositions
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(n._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range n._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// SetDefaults writes each schema default into the properties this value does
// not already carry.
//
// What "does not carry" means depends on what the Go field can hold. A pointer
// is nil exactly when the property was absent, since the decode assigns only for
// a key the document wrote, and is asked directly. A field with no nil state has
// one value standing for both an absent property and an explicit "", 0, false or
// [] -- issue #248 -- so it is asked twice: of _jsonKeys, the key set
// UnmarshalJSON records from the document itself, and of the field, which must
// still be untouched. A value that was never decoded from JSON has no key set,
// and the second test is what then keeps the fields it was built with.
//
// Nothing here touches _jsonKeys. A default is not a property the document sent,
// so a required property still has to have been sent, and SetDefaults cannot
// make one satisfiable that was not.
func (n *NumberPositions) SetDefaults() {
	if n.WithDefault == nil {
		_default := float64(1.2345678901234567)
		n.WithDefault = &_default
	}
}

// Validate checks NumberPositions against its JSON Schema constraints.
func (n NumberPositions) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if n._jsonKeys != nil {
		for _, _req := range []string{"required"} {
			if !n._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	if n._jsonKeys["bounded"] {
		if n.Bounded != nil && float64(*n.Bounded) < 1.5 {
			return fmt.Errorf("bounded: value %v is less than minimum 1.5", *n.Bounded)
		}
	}
	if n._jsonKeys["bounded"] {
		if n.Bounded != nil && float64(*n.Bounded) >= 10 {
			return fmt.Errorf("bounded: value %v must be less than 10", *n.Bounded)
		}
	}
	if n._jsonKeys["bounded"] {
		{
			if n.Bounded != nil {
				q := float64(*n.Bounded) / 0.5
				if math.Abs(q-math.Round(q)) > 1e-9 {
					return fmt.Errorf("bounded: value %v is not a multiple of 0.5", *n.Bounded)
				}
			}
		}
	}
	if n._jsonKeys["constant"] {
		{
			_constGot, _constErr := json.Marshal(n.Constant)
			if _constErr != nil {
				return fmt.Errorf("constant: failed to marshal for const check: %w", _constErr)
			}
			if string(_constGot) != "2.5" {
				return fmt.Errorf("constant: value must be %s, got %s", "2.5", string(_constGot))
			}
		}
	}
	if n._jsonKeys["integerBeside"] {
		if n.IntegerBeside != nil && *n.IntegerBeside < 1 {
			return fmt.Errorf("integerBeside: value %v is less than minimum 1", *n.IntegerBeside)
		}
	}
	if n.Aliased != nil {
		if err := n.Aliased.Validate(); err != nil {
			return jsonPathf(err, "aliased")
		}
	}
	if n.AliasedList != nil {
		if err := n.AliasedList.Validate(); err != nil {
			return jsonPathf(err, "aliasedList")
		}
	}
	if n.Choices != nil {
		if err := n.Choices.Validate(); err != nil {
			return jsonPathf(err, "choices")
		}
	}
	if n.Constrained != nil {
		if err := n.Constrained.Validate(); err != nil {
			return jsonPathf(err, "constrained")
		}
	}
	for _i0, _e0 := range n.Elements {
		if float64(_e0) < 0 {
			return fmt.Errorf("elements[%d]: value %v is less than minimum 0", _i0, _e0)
		}
	}
	for _k0, _e0 := range n.Values {
		if float64(_e0) >= 1 {
			return fmt.Errorf("values[%q]: value %v must be less than 1", _k0, _e0)
		}
	}
	if n._jsonKeys["counted"] {
		// contains validation: count elements matching the contains sub-schema.
		{
			_containsCount := 0
			for _, _cElem := range n.Counted {
				_cMatch := true
				if _cMatch {
					_cBytes, _ := json.Marshal(_cElem)
					var _cf float64
					if json.Unmarshal(_cBytes, &_cf) != nil || _cf < 7.5 {
						_cMatch = false
					}
				}
				if _cMatch {
					_containsCount++
				}
			}
			if _containsCount < 1 {
				return fmt.Errorf("counted: contains: no element matches the contains schema")
			}
		}
	}
	return nil
}
