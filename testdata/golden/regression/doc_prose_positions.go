// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"bytes"
	"encoding/json"
	"fmt"
	"unicode/utf8"
)

// EchoesTheRootTitle - DocProsePositions
type EchoesTheRootTitle string

func (e *EchoesTheRootTitle) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias EchoesTheRootTitle
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(e)))
}

// Validate checks EchoesTheRootTitle against its JSON Schema constraints.
func (e EchoesTheRootTitle) Validate() error {
	return nil
}

// ExamplesAcrossTheReach - Two unconditional schemas each state an example, and both are examples of this one location.
//
// Examples from the schema:
//
//	"from-the-definition"
//	"from-the-branch"
type ExamplesAcrossTheReach string

func (e *ExamplesAcrossTheReach) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ExamplesAcrossTheReach
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(e)))
}

// Validate checks ExamplesAcrossTheReach against its JSON Schema constraints.
func (e ExamplesAcrossTheReach) Validate() error {
	return nil
}

// NearestProseWins - The title the definition states itself
//
// The prose the definition states itself.
type NearestProseWins string

func (n *NearestProseWins) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NearestProseWins
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NearestProseWins against its JSON Schema constraints.
func (n NearestProseWins) Validate() error {
	return nil
}

type PlainDef string

func (p *PlainDef) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias PlainDef
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(p)))
}

// Validate checks PlainDef against its JSON Schema constraints.
func (p PlainDef) Validate() error {
	if utf8.RuneCountInString(string(p)) < 1 {
		return jsonValueErrorf("length %d is less than minimum 1", utf8.RuneCountInString(string(p)))
	}
	return nil
}

// ProseBesideAllOf - Prose written beside the allOf rather than inside it, which the merge copied across by hand.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
//
// Deprecated: the schema marks this deprecated.
type ProseBesideAllOf struct {
	B                    *string                    `json:"b,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (p *ProseBesideAllOf) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"b",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ProseBesideAllOf
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"b", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"b",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"b": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p ProseBesideAllOf) MarshalJSON() ([]byte, error) {
	type Alias ProseBesideAllOf
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ProseBesideAllOf against its JSON Schema constraints.
func (p ProseBesideAllOf) Validate() error {
	return nil
}

// ProseSplitAcrossTheReach - A title the definition states with no prose beside it
//
// Prose one allOf branch further out, with no title beside it.
type ProseSplitAcrossTheReach string

func (p *ProseSplitAcrossTheReach) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ProseSplitAcrossTheReach
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(p)))
}

// Validate checks ProseSplitAcrossTheReach against its JSON Schema constraints.
func (p ProseSplitAcrossTheReach) Validate() error {
	return nil
}

// ProseViaAllOfAlias - A label one allOf branch below the definition
//
// Prose one allOf branch below the definition, which the merge flattens away and no field above carries.
//
// Deprecated: the schema marks this deprecated.
type ProseViaAllOfAlias string

func (p *ProseViaAllOfAlias) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ProseViaAllOfAlias
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(p)))
}

// Validate checks ProseViaAllOfAlias against its JSON Schema constraints.
func (p ProseViaAllOfAlias) Validate() error {
	if utf8.RuneCountInString(string(p)) < 1 {
		return jsonValueErrorf("length %d is less than minimum 1", utf8.RuneCountInString(string(p)))
	}
	return nil
}

// ProseViaAllOfArray - Prose an allOf branch states about an array definition.
type ProseViaAllOfArray []string

func (p *ProseViaAllOfArray) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ProseViaAllOfArray
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(p)))
}

// Validate checks ProseViaAllOfArray against its JSON Schema constraints.
func (p ProseViaAllOfArray) Validate() error {
	return nil
}

// ProseViaAllOfEnum - Prose an allOf branch states about an enum definition.
type ProseViaAllOfEnum string

const (
	ProseViaAllOfEnumRed   ProseViaAllOfEnum = "red"
	ProseViaAllOfEnumGreen ProseViaAllOfEnum = "green"
)

// UnmarshalJSON refuses a JSON null, which the schema's own type excludes.
//
// Without it the named type has no decoder at all, and encoding/json leaves a
// named string, bool or int64 exactly as it found it for a null -- so the
// document was accepted and Validate judged the zero value it never wrote. That
// is invisible whenever the zero is a member of the enum. The two arms above
// carry the same guard inside the decoders they already declare.
func (p *ProseViaAllOfEnum) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ProseViaAllOfEnum
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(p)))
}

// Validate checks ProseViaAllOfEnum against its JSON Schema constraints.
func (p ProseViaAllOfEnum) Validate() error {
	switch p {
	case ProseViaAllOfEnumRed, ProseViaAllOfEnumGreen:
		return nil
	default:
		return jsonValueErrorf("invalid ProseViaAllOfEnum value: %v", p)
	}
}

// ProseViaAllOfStruct - Prose an allOf branch states about an object definition.
//
// Deprecated: the schema marks this deprecated.
type ProseViaAllOfStruct struct {
	S                    *string                    `json:"s,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (p *ProseViaAllOfStruct) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"s",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ProseViaAllOfStruct
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"s", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"s",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"s": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p ProseViaAllOfStruct) MarshalJSON() ([]byte, error) {
	type Alias ProseViaAllOfStruct
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ProseViaAllOfStruct against its JSON Schema constraints.
func (p ProseViaAllOfStruct) Validate() error {
	return nil
}

type ProseViaAnyOf struct {
	X                    *string                    `json:"x,omitempty"`
	Y                    *string                    `json:"y,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (p *ProseViaAnyOf) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	p._jsonKeys = nil
	p._jsonRawProps = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"x",
			"y",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ProseViaAnyOf
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"x", jsonDecodeValue[*string]},
			{"y", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"x",
			"y",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		p._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			p._jsonKeys[_k] = true
		}
		p._jsonRawProps = raw
		knownFields := map[string]bool{
			"x": true,
			"y": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p ProseViaAnyOf) MarshalJSON() ([]byte, error) {
	type Alias ProseViaAnyOf
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ProseViaAnyOf against its JSON Schema constraints.
func (p ProseViaAnyOf) Validate() error {
	// object-level anyOf: at least one flattened variant must match.
	// This check depends on JSON key presence (_jsonKeys), so it is skipped
	// for hand-constructed values (nil _jsonKeys), consistent with how the
	// required-property check above treats untracked presence.
	if p._jsonKeys != nil {
		_anyOfMatches := 0
		{
			_branchMatches := true
			if _branchMatches {
				if _raw, _ok := p._jsonRawProps["x"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "string" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				_anyOfMatches++
			}
		}
		{
			_branchMatches := true
			if _branchMatches {
				if _raw, _ok := p._jsonRawProps["y"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "string" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				_anyOfMatches++
			}
		}
		if _anyOfMatches < 1 {
			return fmt.Errorf("anyOf: no variant matched")
		}
	}
	return nil
}

// ProseViaNestedAllOf - Prose two allOf levels below the definition.
//
// Deprecated: the schema marks this deprecated.
type ProseViaNestedAllOf string

func (p *ProseViaNestedAllOf) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ProseViaNestedAllOf
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(p)))
}

// Validate checks ProseViaNestedAllOf against its JSON Schema constraints.
func (p ProseViaNestedAllOf) Validate() error {
	return nil
}

// TitledTarget - A referenced type's label
//
// Prose on a referenced type, which is where both keywords stay.
type TitledTarget string

func (t *TitledTarget) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias TitledTarget
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(t)))
}

// Validate checks TitledTarget against its JSON Schema constraints.
func (t TitledTarget) Validate() error {
	return nil
}

type ProseViaRef TitledTarget

func (p *ProseViaRef) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	var _target TitledTarget
	if _err := json.Unmarshal(data, &_target); _err != nil {
		return jsonDecodeRefusal(_err)
	}
	*p = ProseViaRef(_target)
	return nil
}

// Validate checks ProseViaRef against its JSON Schema constraints.
func (p ProseViaRef) Validate() error {
	// Returned as it stands: the type this delegates to answers for the very same
	// value, so there is no step of path between the two and nothing to put in
	// front of its message. Wrapping it also lost what the message had recorded
	// about how a container must join it. See issue #279.
	if _err := (TitledTarget(p)).Validate(); _err != nil {
		return _err
	}
	return nil
}

type ProseViaThen struct {
	K                    *string                    `json:"k,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (p *ProseViaThen) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ProseViaThen
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"k", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p ProseViaThen) MarshalJSON() ([]byte, error) {
	type Alias ProseViaThen
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ProseViaThen against its JSON Schema constraints.
func (p ProseViaThen) Validate() error {
	return nil
}

// SelfTitled - A definition whose title, under Go naming rules, is the name it was already given, so repeating it above the declaration would say the identifier twice.
type SelfTitled string

func (s *SelfTitled) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias SelfTitled
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(s)))
}

// Validate checks SelfTitled against its JSON Schema constraints.
func (s SelfTitled) Validate() error {
	return nil
}

// TitledAndDescribedDef - A definition's label above its prose
//
// The paragraph under the label, which Go documentation wants below a summary line rather than run onto the end of one.
type TitledAndDescribedDef string

func (t *TitledAndDescribedDef) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias TitledAndDescribedDef
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(t)))
}

// Validate checks TitledAndDescribedDef against its JSON Schema constraints.
func (t TitledAndDescribedDef) Validate() error {
	return nil
}

// TitledDef - A definition's label
type TitledDef string

func (t *TitledDef) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias TitledDef
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(t)))
}

// Validate checks TitledDef against its JSON Schema constraints.
func (t TitledDef) Validate() error {
	if utf8.RuneCountInString(string(t)) < 1 {
		return jsonValueErrorf("length %d is less than minimum 1", utf8.RuneCountInString(string(t)))
	}
	return nil
}

// TitledDeprecatedAlias - A label with nothing but a deprecation under it
//
// Deprecated: the schema marks this deprecated.
type TitledDeprecatedAlias string

func (t *TitledDeprecatedAlias) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias TitledDeprecatedAlias
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(t)))
}

// Validate checks TitledDeprecatedAlias against its JSON Schema constraints.
func (t TitledDeprecatedAlias) Validate() error {
	return nil
}

// TitledDeprecatedStruct - An object labelled and deprecated, and described by neither
//
// Deprecated: the schema marks this deprecated.
type TitledDeprecatedStruct struct {
	D                    *string                    `json:"d,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (t *TitledDeprecatedStruct) UnmarshalJSON(data []byte) error {
	t.AdditionalProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"d",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias TitledDeprecatedStruct
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(t),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"d", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"d",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"d": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if t.AdditionalProperties == nil {
				t.AdditionalProperties = make(map[string]json.RawMessage)
			}
			t.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (t TitledDeprecatedStruct) MarshalJSON() ([]byte, error) {
	type Alias TitledDeprecatedStruct
	aux := struct {
		Alias
	}{
		Alias: (Alias)(t),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range t.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks TitledDeprecatedStruct against its JSON Schema constraints.
func (t TitledDeprecatedStruct) Validate() error {
	return nil
}

// DocProsePositionsTitleViaAllOf - A label written on an allOf branch of the property
//
// And the prose beside it, from the same branch.
type DocProsePositionsTitleViaAllOf string

func (d *DocProsePositionsTitleViaAllOf) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias DocProsePositionsTitleViaAllOf
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(d)))
}

// Validate checks DocProsePositionsTitleViaAllOf against its JSON Schema constraints.
func (d DocProsePositionsTitleViaAllOf) Validate() error {
	return nil
}

// DocProsePositionsTitledElementItem - An array element's label
type DocProsePositionsTitledElementItem struct {
	E                    *string                    `json:"e,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (d *DocProsePositionsTitledElementItem) UnmarshalJSON(data []byte) error {
	d.AdditionalProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"e",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias DocProsePositionsTitledElementItem
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(d),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"e", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"e",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"e": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if d.AdditionalProperties == nil {
				d.AdditionalProperties = make(map[string]json.RawMessage)
			}
			d.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (d DocProsePositionsTitledElementItem) MarshalJSON() ([]byte, error) {
	type Alias DocProsePositionsTitledElementItem
	aux := struct {
		Alias
	}{
		Alias: (Alias)(d),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range d.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks DocProsePositionsTitledElementItem against its JSON Schema constraints.
func (d DocProsePositionsTitledElementItem) Validate() error {
	return nil
}

// ByLabel - The variant its own title names, so the label is in the identifier and only the prose is written above it.
type ByLabel struct {
	Label                string                     `json:"label"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (b *ByLabel) UnmarshalJSON(data []byte) error {
	b.AdditionalProperties = nil
	b._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"label",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ByLabel
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(b),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"label", jsonDecodeValue[string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"label",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		b._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			b._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"label": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if b.AdditionalProperties == nil {
				b.AdditionalProperties = make(map[string]json.RawMessage)
			}
			b.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (b ByLabel) MarshalJSON() ([]byte, error) {
	type Alias ByLabel
	aux := struct {
		Alias
	}{
		Alias: (Alias)(b),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range b.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ByLabel against its JSON Schema constraints.
func (b ByLabel) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if b._jsonKeys != nil {
		for _, _req := range []string{"label"} {
			if !b._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type ByOrdinal struct {
	Ordinal              int64                      `json:"ordinal"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (b *ByOrdinal) UnmarshalJSON(data []byte) error {
	b.AdditionalProperties = nil
	b._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"ordinal",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ByOrdinal
	aux := &struct {
		*Alias
		Ordinal *jsonInteger `json:"ordinal"`
	}{
		Alias: (*Alias)(b),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"ordinal", jsonDecodeValue[jsonInteger]},
		})
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.Ordinal != nil {
		_iv := *aux.Ordinal
		b.Ordinal = int64(_iv)
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"ordinal",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		b._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			b._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"ordinal": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if b.AdditionalProperties == nil {
				b.AdditionalProperties = make(map[string]json.RawMessage)
			}
			b.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (b ByOrdinal) MarshalJSON() ([]byte, error) {
	type Alias ByOrdinal
	aux := struct {
		Alias
	}{
		Alias: (Alias)(b),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range b.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ByOrdinal against its JSON Schema constraints.
func (b ByOrdinal) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if b._jsonKeys != nil {
		for _, _req := range []string{"ordinal"} {
			if !b._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// DocProsePositionsTitledValueValue - A map value's label
type DocProsePositionsTitledValueValue struct {
	V                    *string                    `json:"v,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (d *DocProsePositionsTitledValueValue) UnmarshalJSON(data []byte) error {
	d.AdditionalProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"v",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias DocProsePositionsTitledValueValue
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(d),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"v", jsonDecodeValue[*string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"v",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"v": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if d.AdditionalProperties == nil {
				d.AdditionalProperties = make(map[string]json.RawMessage)
			}
			d.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (d DocProsePositionsTitledValueValue) MarshalJSON() ([]byte, error) {
	type Alias DocProsePositionsTitledValueValue
	aux := struct {
		Alias
	}{
		Alias: (Alias)(d),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range d.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks DocProsePositionsTitledValueValue against its JSON Schema constraints.
func (d DocProsePositionsTitledValueValue) Validate() error {
	return nil
}

// DocProsePositions - The two prose keywords, at every position each of them can be written and through every applicator that decides whether it is said about the declaration below it. Issue #188 is the `title` half: the keyword named the root type and a oneOf variant and reached the generated source nowhere else, so a schema whose only prose was a title documented nothing at all -- not a $defs entry, not a property, not an array element, not a map value. Issue #200 is the applicator half: a named type's comment was read off the node in hand and nothing else, so a $defs entry written {"allOf":[{"description":"..."}]} declared a bare Go type and lost its prose entirely, there being no field above it to carry it. The two are one matrix because they are one reading: unconditionalReachAt, folded once in Generator.docFor, for the title and the description and the four annotation keywords together. The *Cond and *Anyof cells are the control that says the reach stops where 2020-12 section 7.7.1 stops it, and the *Ref cells the control that says it stops at a reference, whose target carries its own comment.
type DocProsePositions struct {
	// Prose with no label above it, which is the comment every schema in this repository's corpus used to produce.
	DescriptionOnly          *string                   `json:"descriptionOnly,omitempty"`
	EchoesTheRootTitle       *EchoesTheRootTitle       `json:"echoesTheRootTitle,omitempty"`
	ExamplesAcrossTheReach   *ExamplesAcrossTheReach   `json:"examplesAcrossTheReach,omitempty"`
	Mode                     *string                   `json:"mode,omitempty"`
	NearestProseWins         *NearestProseWins         `json:"nearestProseWins,omitempty"`
	PlainDef                 *PlainDef                 `json:"plainDef,omitempty"`
	PlainProperty            *string                   `json:"plainProperty,omitempty"`
	ProseBesideAllOf         *ProseBesideAllOf         `json:"proseBesideAllOf,omitempty"`
	ProseSplitAcrossTheReach *ProseSplitAcrossTheReach `json:"proseSplitAcrossTheReach,omitempty"`
	ProseViaAllOfAlias       *ProseViaAllOfAlias       `json:"proseViaAllOfAlias,omitempty"`
	ProseViaAllOfArray       ProseViaAllOfArray        `json:"proseViaAllOfArray,omitzero"`
	ProseViaAllOfEnum        *ProseViaAllOfEnum        `json:"proseViaAllOfEnum,omitempty"`
	ProseViaAllOfStruct      *ProseViaAllOfStruct      `json:"proseViaAllOfStruct,omitempty"`
	ProseViaAnyOf            *ProseViaAnyOf            `json:"proseViaAnyOf,omitempty"`
	ProseViaNestedAllOf      *ProseViaNestedAllOf      `json:"proseViaNestedAllOf,omitempty"`
	ProseViaRef              *ProseViaRef              `json:"proseViaRef,omitempty"`
	ProseViaThen             *ProseViaThen             `json:"proseViaThen,omitempty"`
	SelfTitledDef            *SelfTitled               `json:"selfTitledDef,omitempty"`
	// A label above the prose
	//
	// The paragraph the label summarises.
	TitleAndDescription *string `json:"titleAndDescription,omitempty"`
	TitleCondThen       *string `json:"titleCondThen,omitempty"`
	// A label and nothing else
	TitleOnly *string `json:"titleOnly,omitempty"`
	// A label with a deprecation under it and no prose between
	//
	// Deprecated: the schema marks this deprecated.
	TitleThenDeprecated *string `json:"titleThenDeprecated,omitempty"`
	// A label written on an allOf branch of the property
	//
	// And the prose beside it, from the same branch.
	TitleViaAllOf *DocProsePositionsTitleViaAllOf `json:"titleViaAllOf,omitempty"`
	// A label an allOf branch states about a merged property
	TitleViaMergedAllOf    *string                                      `json:"titleViaMergedAllOf,omitempty"`
	TitleViaRef            *TitledTarget                                `json:"titleViaRef,omitempty"`
	TitledAndDescribedDef  *TitledAndDescribedDef                       `json:"titledAndDescribedDef,omitempty"`
	TitledDef              *TitledDef                                   `json:"titledDef,omitempty"`
	TitledDeprecatedAlias  *TitledDeprecatedAlias                       `json:"titledDeprecatedAlias,omitempty"`
	TitledDeprecatedStruct *TitledDeprecatedStruct                      `json:"titledDeprecatedStruct,omitempty"`
	TitledElement          []DocProsePositionsTitledElementItem         `json:"titledElement,omitzero"`
	TitledValue            map[string]DocProsePositionsTitledValueValue `json:"titledValue,omitzero"`
	TitledUnion            isDocProsePositions_TitledUnion              `json:"-"`
	AdditionalProperties   map[string]json.RawMessage                   `json:"-"`
	_jsonKeys              map[string]bool                              // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps          map[string]json.RawMessage                   // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
	_jsonNulls             map[string]bool                              // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

// isDocProsePositions_TitledUnion is a sealed interface for the TitledUnion field of DocProsePositions.
// Exactly one variant must be set.
type isDocProsePositions_TitledUnion interface {
	isDocProsePositions_TitledUnion()
}

// DocProsePositions_ByLabel wraps *ByLabel as a TitledUnion variant.
type DocProsePositions_ByLabel struct {
	ByLabel *ByLabel
}

func (*DocProsePositions_ByLabel) isDocProsePositions_TitledUnion() {}

// DocProsePositions_ByOrdinal wraps *ByOrdinal as a TitledUnion variant.
type DocProsePositions_ByOrdinal struct {
	ByOrdinal *ByOrdinal
}

func (*DocProsePositions_ByOrdinal) isDocProsePositions_TitledUnion() {}

// GetByLabel returns the ByLabel variant value, or the zero value if not set.
func (d *DocProsePositions) GetByLabel() *ByLabel {
	if d != nil {
		if v, ok := d.TitledUnion.(*DocProsePositions_ByLabel); ok {
			return v.ByLabel
		}
	}
	var zero *ByLabel
	return zero
}

// GetByOrdinal returns the ByOrdinal variant value, or the zero value if not set.
func (d *DocProsePositions) GetByOrdinal() *ByOrdinal {
	if d != nil {
		if v, ok := d.TitledUnion.(*DocProsePositions_ByOrdinal); ok {
			return v.ByOrdinal
		}
	}
	var zero *ByOrdinal
	return zero
}

func (d *DocProsePositions) UnmarshalJSON(data []byte) error {
	d.AdditionalProperties = nil
	d._jsonKeys = nil
	d._jsonRawProps = nil
	d._jsonNulls = nil
	d.TitledUnion = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"descriptionOnly",
			"echoesTheRootTitle",
			"examplesAcrossTheReach",
			"mode",
			"nearestProseWins",
			"plainDef",
			"plainProperty",
			"proseBesideAllOf",
			"proseSplitAcrossTheReach",
			"proseViaAllOfAlias",
			"proseViaAllOfArray",
			"proseViaAllOfEnum",
			"proseViaAllOfStruct",
			"proseViaAnyOf",
			"proseViaNestedAllOf",
			"proseViaRef",
			"proseViaThen",
			"selfTitledDef",
			"titleAndDescription",
			"titleCondThen",
			"titleOnly",
			"titleThenDeprecated",
			"titleViaAllOf",
			"titleViaMergedAllOf",
			"titleViaRef",
			"titledAndDescribedDef",
			"titledDef",
			"titledDeprecatedAlias",
			"titledDeprecatedStruct",
			"titledElement",
			"titledValue",
			"titledUnion",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias DocProsePositions
	aux := &struct {
		*Alias
		TitledUnion json.RawMessage `json:"titledUnion"`
	}{
		Alias: (*Alias)(d),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"descriptionOnly", jsonDecodeValue[*string]},
			{"echoesTheRootTitle", jsonDecodeValue[*EchoesTheRootTitle]},
			{"examplesAcrossTheReach", jsonDecodeValue[*ExamplesAcrossTheReach]},
			{"mode", jsonDecodeValue[*string]},
			{"nearestProseWins", jsonDecodeValue[*NearestProseWins]},
			{"plainDef", jsonDecodeValue[*PlainDef]},
			{"plainProperty", jsonDecodeValue[*string]},
			{"proseBesideAllOf", jsonDecodeValue[*ProseBesideAllOf]},
			{"proseSplitAcrossTheReach", jsonDecodeValue[*ProseSplitAcrossTheReach]},
			{"proseViaAllOfAlias", jsonDecodeValue[*ProseViaAllOfAlias]},
			{"proseViaAllOfArray", jsonDecodeValue[ProseViaAllOfArray]},
			{"proseViaAllOfEnum", jsonDecodeValue[*ProseViaAllOfEnum]},
			{"proseViaAllOfStruct", jsonDecodeValue[*ProseViaAllOfStruct]},
			{"proseViaAnyOf", jsonDecodeValue[*ProseViaAnyOf]},
			{"proseViaNestedAllOf", jsonDecodeValue[*ProseViaNestedAllOf]},
			{"proseViaRef", jsonDecodeValue[*ProseViaRef]},
			{"proseViaThen", jsonDecodeValue[*ProseViaThen]},
			{"selfTitledDef", jsonDecodeValue[*SelfTitled]},
			{"titleAndDescription", jsonDecodeValue[*string]},
			{"titleCondThen", jsonDecodeValue[*string]},
			{"titleOnly", jsonDecodeValue[*string]},
			{"titleThenDeprecated", jsonDecodeValue[*string]},
			{"titleViaAllOf", jsonDecodeValue[*DocProsePositionsTitleViaAllOf]},
			{"titleViaMergedAllOf", jsonDecodeValue[*string]},
			{"titleViaRef", jsonDecodeValue[*TitledTarget]},
			{"titledAndDescribedDef", jsonDecodeValue[*TitledAndDescribedDef]},
			{"titledDef", jsonDecodeValue[*TitledDef]},
			{"titledDeprecatedAlias", jsonDecodeValue[*TitledDeprecatedAlias]},
			{"titledDeprecatedStruct", jsonDecodeValue[*TitledDeprecatedStruct]},
			{"titledElement", jsonDecodeItems(jsonDecodeValue[DocProsePositionsTitledElementItem])},
			{"titledValue", jsonDecodeValues(jsonDecodeValue[DocProsePositionsTitledValueValue])},
		})
	}

	{
		oneofData := aux.TitledUnion
		// Every refusal this union raises is a sentence about the value the union
		// holds, and the property that reaches it goes in front of that sentence
		// by the rule every other message is joined by (see jsonPathError).
		//
		// What used to go in front was a Go type and a Go field pasted in by hand:
		// DocProsePositions.TitledUnion
		// A caller cannot find either in the document they sent, and at any depth
		// but the first the pair does not even resemble the path. Issue #289.
		oneofErrf := func(format string, args ...any) error {
			return jsonPathf(jsonValueErrorf(format, args...), "%s", "titledUnion")
		}
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isDocProsePositions_TitledUnion
			var oneofStrictErr error

			// Try variant: ByLabel
			{
				if oneofHasRequiredFields(oneofData, "label") {
					var candidate *ByLabel
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						d.TitledUnion = &DocProsePositions_ByLabel{ByLabel: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &DocProsePositions_ByLabel{ByLabel: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant ByLabel: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			// Try variant: ByOrdinal
			{
				if oneofHasRequiredFields(oneofData, "ordinal") {
					var candidate *ByOrdinal
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						d.TitledUnion = &DocProsePositions_ByOrdinal{ByOrdinal: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &DocProsePositions_ByOrdinal{ByOrdinal: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant ByOrdinal: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			if oneofMatched == 0 {
				// A branch is only put to a decode once the keys it selects on are
				// present, so a value that answers none of them leaves no branch
				// reason behind at all -- and the %w below then had nothing to
				// wrap and printed "%!w(<nil>)", which says nothing true about
				// anything. The refusal stands on its own in that case. Issue #289.
				if oneofLastErr == nil {
					return oneofErrf("no matching oneOf variant")
				}
				return oneofErrf("no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					d.TitledUnion = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return oneofErrf("no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return oneofErrf("multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"descriptionOnly",
			"echoesTheRootTitle",
			"examplesAcrossTheReach",
			"mode",
			"nearestProseWins",
			"plainDef",
			"plainProperty",
			"proseBesideAllOf",
			"proseSplitAcrossTheReach",
			"proseViaAllOfAlias",
			"proseViaAllOfArray",
			"proseViaAllOfEnum",
			"proseViaAllOfStruct",
			"proseViaAnyOf",
			"proseViaNestedAllOf",
			"proseViaRef",
			"proseViaThen",
			"selfTitledDef",
			"titleAndDescription",
			"titleOnly",
			"titleThenDeprecated",
			"titleViaAllOf",
			"titleViaMergedAllOf",
			"titleViaRef",
			"titledAndDescribedDef",
			"titledDef",
			"titledDeprecatedAlias",
			"titledDeprecatedStruct",
			"titledUnion",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		if _v, ok := raw["titledElement"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "titledElement")
			}
		}
		if _v, ok := raw["titledValue"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, IsMap: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "titledValue")
			}
		}
		d._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			d._jsonKeys[_k] = true
		}
		d._jsonRawProps = raw
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"titleCondThen",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if d._jsonNulls == nil {
					d._jsonNulls = make(map[string]bool, 1)
				}
				d._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"descriptionOnly":          true,
			"echoesTheRootTitle":       true,
			"examplesAcrossTheReach":   true,
			"mode":                     true,
			"nearestProseWins":         true,
			"plainDef":                 true,
			"plainProperty":            true,
			"proseBesideAllOf":         true,
			"proseSplitAcrossTheReach": true,
			"proseViaAllOfAlias":       true,
			"proseViaAllOfArray":       true,
			"proseViaAllOfEnum":        true,
			"proseViaAllOfStruct":      true,
			"proseViaAnyOf":            true,
			"proseViaNestedAllOf":      true,
			"proseViaRef":              true,
			"proseViaThen":             true,
			"selfTitledDef":            true,
			"titleAndDescription":      true,
			"titleCondThen":            true,
			"titleOnly":                true,
			"titleThenDeprecated":      true,
			"titleViaAllOf":            true,
			"titleViaMergedAllOf":      true,
			"titleViaRef":              true,
			"titledAndDescribedDef":    true,
			"titledDef":                true,
			"titledDeprecatedAlias":    true,
			"titledDeprecatedStruct":   true,
			"titledElement":            true,
			"titledValue":              true,
			"titledUnion":              true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if d.AdditionalProperties == nil {
				d.AdditionalProperties = make(map[string]json.RawMessage)
			}
			d.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (d DocProsePositions) MarshalJSON() ([]byte, error) {
	type Alias DocProsePositions
	aux := struct {
		Alias
		TitledUnion json.RawMessage `json:"titledUnion,omitempty"`
	}{
		Alias: (Alias)(d),
	}

	if d.TitledUnion != nil {
		switch v := d.TitledUnion.(type) {
		case *DocProsePositions_ByLabel:
			raw, err := json.Marshal(v.ByLabel)
			if err != nil {
				return nil, fmt.Errorf("marshaling DocProsePositions.TitledUnion: %w", err)
			}
			aux.TitledUnion = raw
		case *DocProsePositions_ByOrdinal:
			raw, err := json.Marshal(v.ByOrdinal)
			if err != nil {
				return nil, fmt.Errorf("marshaling DocProsePositions.TitledUnion: %w", err)
			}
			aux.TitledUnion = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(d._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range d._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range d.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks DocProsePositions against its JSON Schema constraints.
func (d DocProsePositions) Validate() error {
	// object-level if/then/else: whichever branch the condition selects must
	// hold. Both sides read the raw JSON the unmarshaler kept (_jsonRawProps),
	// so the check is skipped for hand-constructed values, consistent with how
	// the required-property and object-level oneOf/anyOf checks above treat
	// untracked presence. A non-object document never reaches here either, and
	// need not: `properties` and `required` are both vacuously satisfied by one.
	if d._jsonRawProps != nil {
		_condHolds := true
		if _, _ok := d._jsonRawProps["mode"]; !_ok {
			_condHolds = false
		}
		if _condHolds {
			if _raw, _ok := d._jsonRawProps["titleCondThen"]; _ok {
				var _v any
				if _err := json.Unmarshal(_raw, &_v); _err != nil {
					return fmt.Errorf("then: property %q: cannot decode value: %w", "titleCondThen", _err)
				}
				if !(_dynIsString(_v)) {
					return fmt.Errorf("then: property %q does not satisfy the then schema", "titleCondThen")
				}
			}
		}
	}
	if d.EchoesTheRootTitle != nil {
		if err := d.EchoesTheRootTitle.Validate(); err != nil {
			return jsonPathf(err, "echoesTheRootTitle")
		}
	}
	if d.ExamplesAcrossTheReach != nil {
		if err := d.ExamplesAcrossTheReach.Validate(); err != nil {
			return jsonPathf(err, "examplesAcrossTheReach")
		}
	}
	if d.NearestProseWins != nil {
		if err := d.NearestProseWins.Validate(); err != nil {
			return jsonPathf(err, "nearestProseWins")
		}
	}
	if d.PlainDef != nil {
		if err := d.PlainDef.Validate(); err != nil {
			return jsonPathf(err, "plainDef")
		}
	}
	if d.ProseBesideAllOf != nil {
		if err := d.ProseBesideAllOf.Validate(); err != nil {
			return jsonPathf(err, "proseBesideAllOf")
		}
	}
	if d.ProseSplitAcrossTheReach != nil {
		if err := d.ProseSplitAcrossTheReach.Validate(); err != nil {
			return jsonPathf(err, "proseSplitAcrossTheReach")
		}
	}
	if d.ProseViaAllOfAlias != nil {
		if err := d.ProseViaAllOfAlias.Validate(); err != nil {
			return jsonPathf(err, "proseViaAllOfAlias")
		}
	}
	if d.ProseViaAllOfArray != nil {
		if err := d.ProseViaAllOfArray.Validate(); err != nil {
			return jsonPathf(err, "proseViaAllOfArray")
		}
	}
	if d.ProseViaAllOfEnum != nil {
		if err := d.ProseViaAllOfEnum.Validate(); err != nil {
			return jsonPathf(err, "proseViaAllOfEnum")
		}
	}
	if d.ProseViaAllOfStruct != nil {
		if err := d.ProseViaAllOfStruct.Validate(); err != nil {
			return jsonPathf(err, "proseViaAllOfStruct")
		}
	}
	if d.ProseViaAnyOf != nil {
		if err := d.ProseViaAnyOf.Validate(); err != nil {
			return jsonPathf(err, "proseViaAnyOf")
		}
	}
	if d.ProseViaNestedAllOf != nil {
		if err := d.ProseViaNestedAllOf.Validate(); err != nil {
			return jsonPathf(err, "proseViaNestedAllOf")
		}
	}
	if d.ProseViaRef != nil {
		if err := d.ProseViaRef.Validate(); err != nil {
			return jsonPathf(err, "proseViaRef")
		}
	}
	if d.ProseViaThen != nil {
		if err := d.ProseViaThen.Validate(); err != nil {
			return jsonPathf(err, "proseViaThen")
		}
	}
	if d.SelfTitledDef != nil {
		if err := d.SelfTitledDef.Validate(); err != nil {
			return jsonPathf(err, "selfTitledDef")
		}
	}
	if d.TitleViaAllOf != nil {
		if err := d.TitleViaAllOf.Validate(); err != nil {
			return jsonPathf(err, "titleViaAllOf")
		}
	}
	if d.TitleViaRef != nil {
		if err := d.TitleViaRef.Validate(); err != nil {
			return jsonPathf(err, "titleViaRef")
		}
	}
	if d.TitledAndDescribedDef != nil {
		if err := d.TitledAndDescribedDef.Validate(); err != nil {
			return jsonPathf(err, "titledAndDescribedDef")
		}
	}
	if d.TitledDef != nil {
		if err := d.TitledDef.Validate(); err != nil {
			return jsonPathf(err, "titledDef")
		}
	}
	if d.TitledDeprecatedAlias != nil {
		if err := d.TitledDeprecatedAlias.Validate(); err != nil {
			return jsonPathf(err, "titledDeprecatedAlias")
		}
	}
	if d.TitledDeprecatedStruct != nil {
		if err := d.TitledDeprecatedStruct.Validate(); err != nil {
			return jsonPathf(err, "titledDeprecatedStruct")
		}
	}
	for _i, _item := range d.TitledElement {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "titledElement[%d]", _i)
		}
	}
	for _k, _val := range d.TitledValue {
		if err := _val.Validate(); err != nil {
			return jsonPathf(err, "titledValue[%q]", _k)
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := d.TitledUnion.(type) {
	case *DocProsePositions_ByLabel:
		if _oneOfSel.ByLabel != nil {
			if err := _oneOfSel.ByLabel.Validate(); err != nil {
				return jsonPathf(err, "titledUnion")
			}
		}
	case *DocProsePositions_ByOrdinal:
		if _oneOfSel.ByOrdinal != nil {
			if err := _oneOfSel.ByOrdinal.Validate(); err != nil {
				return jsonPathf(err, "titledUnion")
			}
		}
	}
	return nil
}
