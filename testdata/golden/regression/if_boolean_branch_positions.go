// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

// IfFalse accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type IfFalse struct {
	_raw json.RawMessage
}

func (i *IfFalse) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i IfFalse) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i IfFalse) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i IfFalse) IsZero() bool { return len(i._raw) == 0 }

func (i IfFalse) String() string { return string(i._raw) }

// IfFalseSchema is the schema IfFalse validates against.
var IfFalseSchema = _schemaNode{
	Else: _node(_schemaNode{
		Type: []string{"integer"},
	}),
	If: _node(_schemaNode{Boolean: _boolPtr(false)}),
	Then: _node(_schemaNode{
		Type: []string{"string"},
	}),
}

// Validate checks IfFalse against its JSON Schema constraints.
func (i IfFalse) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&IfFalseSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// IfBooleanBranchPositionsElseFalse accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type IfBooleanBranchPositionsElseFalse struct {
	_raw json.RawMessage
}

func (i *IfBooleanBranchPositionsElseFalse) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i IfBooleanBranchPositionsElseFalse) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i IfBooleanBranchPositionsElseFalse) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i IfBooleanBranchPositionsElseFalse) IsZero() bool { return len(i._raw) == 0 }

func (i IfBooleanBranchPositionsElseFalse) String() string { return string(i._raw) }

// IfBooleanBranchPositionsElseFalseSchema is the schema IfBooleanBranchPositionsElseFalse validates against.
var IfBooleanBranchPositionsElseFalseSchema = _schemaNode{
	Else: _node(_schemaNode{Boolean: _boolPtr(false)}),
	If: _node(_schemaNode{
		Type: []string{"integer"},
	}),
	Then: _node(_schemaNode{Boolean: _boolPtr(true)}),
}

// Validate checks IfBooleanBranchPositionsElseFalse against its JSON Schema constraints.
func (i IfBooleanBranchPositionsElseFalse) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&IfBooleanBranchPositionsElseFalseSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// IfBooleanBranchPositionsIfFalse accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type IfBooleanBranchPositionsIfFalse struct {
	_raw json.RawMessage
}

func (i *IfBooleanBranchPositionsIfFalse) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i IfBooleanBranchPositionsIfFalse) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i IfBooleanBranchPositionsIfFalse) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i IfBooleanBranchPositionsIfFalse) IsZero() bool { return len(i._raw) == 0 }

func (i IfBooleanBranchPositionsIfFalse) String() string { return string(i._raw) }

// IfBooleanBranchPositionsIfFalseSchema is the schema IfBooleanBranchPositionsIfFalse validates against.
var IfBooleanBranchPositionsIfFalseSchema = _schemaNode{
	Else: _node(_schemaNode{
		Type: []string{"integer"},
	}),
	If: _node(_schemaNode{Boolean: _boolPtr(false)}),
	Then: _node(_schemaNode{
		Type: []string{"string"},
	}),
}

// Validate checks IfBooleanBranchPositionsIfFalse against its JSON Schema constraints.
func (i IfBooleanBranchPositionsIfFalse) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&IfBooleanBranchPositionsIfFalseSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// IfBooleanBranchPositionsIfTrue accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type IfBooleanBranchPositionsIfTrue struct {
	_raw json.RawMessage
}

func (i *IfBooleanBranchPositionsIfTrue) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i IfBooleanBranchPositionsIfTrue) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i IfBooleanBranchPositionsIfTrue) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i IfBooleanBranchPositionsIfTrue) IsZero() bool { return len(i._raw) == 0 }

func (i IfBooleanBranchPositionsIfTrue) String() string { return string(i._raw) }

// IfBooleanBranchPositionsIfTrueSchema is the schema IfBooleanBranchPositionsIfTrue validates against.
var IfBooleanBranchPositionsIfTrueSchema = _schemaNode{
	If: _node(_schemaNode{Boolean: _boolPtr(true)}),
	Then: _node(_schemaNode{
		Type: []string{"integer"},
	}),
}

// Validate checks IfBooleanBranchPositionsIfTrue against its JSON Schema constraints.
func (i IfBooleanBranchPositionsIfTrue) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&IfBooleanBranchPositionsIfTrueSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// IfBooleanBranchPositionsListItem accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type IfBooleanBranchPositionsListItem struct {
	_raw json.RawMessage
}

func (i *IfBooleanBranchPositionsListItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i IfBooleanBranchPositionsListItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i IfBooleanBranchPositionsListItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i IfBooleanBranchPositionsListItem) IsZero() bool { return len(i._raw) == 0 }

func (i IfBooleanBranchPositionsListItem) String() string { return string(i._raw) }

// IfBooleanBranchPositionsListItemSchema is the schema IfBooleanBranchPositionsListItem validates against.
var IfBooleanBranchPositionsListItemSchema = _schemaNode{
	Else: _node(_schemaNode{
		Type: []string{"integer"},
	}),
	If: _node(_schemaNode{Boolean: _boolPtr(false)}),
	Then: _node(_schemaNode{
		Type: []string{"string"},
	}),
}

// Validate checks IfBooleanBranchPositionsListItem against its JSON Schema constraints.
func (i IfBooleanBranchPositionsListItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&IfBooleanBranchPositionsListItemSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// IfBooleanBranchPositionsThenFalse accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type IfBooleanBranchPositionsThenFalse struct {
	_raw json.RawMessage
}

func (i *IfBooleanBranchPositionsThenFalse) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i IfBooleanBranchPositionsThenFalse) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i IfBooleanBranchPositionsThenFalse) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i IfBooleanBranchPositionsThenFalse) IsZero() bool { return len(i._raw) == 0 }

func (i IfBooleanBranchPositionsThenFalse) String() string { return string(i._raw) }

// IfBooleanBranchPositionsThenFalseSchema is the schema IfBooleanBranchPositionsThenFalse validates against.
var IfBooleanBranchPositionsThenFalseSchema = _schemaNode{
	If: _node(_schemaNode{
		Type: []string{"integer"},
	}),
	Then: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks IfBooleanBranchPositionsThenFalse against its JSON Schema constraints.
func (i IfBooleanBranchPositionsThenFalse) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&IfBooleanBranchPositionsThenFalseSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// IfBooleanBranchPositions - An if/then/else whose `if` or whose branch is a boolean schema, written in a property, in an array element and in a $defs entry a property refers to. The property position resolved these to `any`, which Go forbids methods on, so the keyword was enforced nowhere while the same schema at a document root was judged correctly (issue #134). The $ref column is the control that the two spellings agree.
type IfBooleanBranchPositions struct {
	ElseFalse            IfBooleanBranchPositionsElseFalse  `json:"elseFalse,omitzero"`
	IfFalse              IfBooleanBranchPositionsIfFalse    `json:"ifFalse,omitzero"`
	IfTrue               IfBooleanBranchPositionsIfTrue     `json:"ifTrue,omitzero"`
	List                 []IfBooleanBranchPositionsListItem `json:"list,omitzero"`
	ThenFalse            IfBooleanBranchPositionsThenFalse  `json:"thenFalse,omitzero"`
	ViaRef               IfFalse                            `json:"viaRef,omitzero"`
	AdditionalProperties map[string]json.RawMessage         `json:"-"`
	_jsonKeys            map[string]bool                    // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (i *IfBooleanBranchPositions) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type IfBooleanBranchPositions")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"elseFalse",
			"ifFalse",
			"ifTrue",
			"list",
			"thenFalse",
			"viaRef",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias IfBooleanBranchPositions
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"list",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"elseFalse": true,
			"ifFalse":   true,
			"ifTrue":    true,
			"list":      true,
			"thenFalse": true,
			"viaRef":    true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i IfBooleanBranchPositions) MarshalJSON() ([]byte, error) {
	type Alias IfBooleanBranchPositions
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks IfBooleanBranchPositions against its JSON Schema constraints.
func (i IfBooleanBranchPositions) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["elseFalse"] {
		if err := i.ElseFalse.Validate(); err != nil {
			return jsonPathf(err, "elseFalse")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["ifFalse"] {
		if err := i.IfFalse.Validate(); err != nil {
			return jsonPathf(err, "ifFalse")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["ifTrue"] {
		if err := i.IfTrue.Validate(); err != nil {
			return jsonPathf(err, "ifTrue")
		}
	}
	for _i, _item := range i.List {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "list[%d]", _i)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["thenFalse"] {
		if err := i.ThenFalse.Validate(); err != nil {
			return jsonPathf(err, "thenFalse")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["viaRef"] {
		if err := i.ViaRef.Validate(); err != nil {
			return jsonPathf(err, "viaRef")
		}
	}
	return nil
}
