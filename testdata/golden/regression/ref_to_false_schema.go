// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

type Always any

// Never accepts any JSON value and validates a root-level "not" constraint.
type Never struct {
	_raw json.RawMessage
}

func (n *Never) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n Never) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n Never) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n Never) IsZero() bool { return len(n._raw) == 0 }

func (n Never) String() string { return string(n._raw) }

// Validate checks Never against its JSON Schema "not" constraint.
func (n Never) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// RefToFalseSchemaBeside accepts any JSON value and validates a root-level "not" constraint.
type RefToFalseSchemaBeside struct {
	_raw json.RawMessage
}

func (r *RefToFalseSchemaBeside) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefToFalseSchemaBeside) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefToFalseSchemaBeside) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefToFalseSchemaBeside) IsZero() bool { return len(r._raw) == 0 }

func (r RefToFalseSchemaBeside) String() string { return string(r._raw) }

// Validate checks RefToFalseSchemaBeside against its JSON Schema "not" constraint.
func (r RefToFalseSchemaBeside) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// RefToFalseSchemaViaAllOf accepts any JSON value and validates a root-level "not" constraint.
type RefToFalseSchemaViaAllOf struct {
	_raw json.RawMessage
}

func (r *RefToFalseSchemaViaAllOf) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefToFalseSchemaViaAllOf) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefToFalseSchemaViaAllOf) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefToFalseSchemaViaAllOf) IsZero() bool { return len(r._raw) == 0 }

func (r RefToFalseSchemaViaAllOf) String() string { return string(r._raw) }

// Validate checks RefToFalseSchemaViaAllOf against its JSON Schema "not" constraint.
func (r RefToFalseSchemaViaAllOf) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// RefToFalseSchemaViaAnyOf accepts any JSON value and validates a root-level "not" constraint.
type RefToFalseSchemaViaAnyOf struct {
	_raw json.RawMessage
}

func (r *RefToFalseSchemaViaAnyOf) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefToFalseSchemaViaAnyOf) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefToFalseSchemaViaAnyOf) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefToFalseSchemaViaAnyOf) IsZero() bool { return len(r._raw) == 0 }

func (r RefToFalseSchemaViaAnyOf) String() string { return string(r._raw) }

// Validate checks RefToFalseSchemaViaAnyOf against its JSON Schema "not" constraint.
func (r RefToFalseSchemaViaAnyOf) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// RefToFalseSchemaViaNested accepts any JSON value and validates a root-level "not" constraint.
type RefToFalseSchemaViaNested struct {
	_raw json.RawMessage
}

func (r *RefToFalseSchemaViaNested) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r RefToFalseSchemaViaNested) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r RefToFalseSchemaViaNested) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r RefToFalseSchemaViaNested) IsZero() bool { return len(r._raw) == 0 }

func (r RefToFalseSchemaViaNested) String() string { return string(r._raw) }

// Validate checks RefToFalseSchemaViaNested against its JSON Schema "not" constraint.
func (r RefToFalseSchemaViaNested) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

type RefToFalseSchema struct {
	Always               Always                      `json:"always,omitempty"`
	AlwaysAllOf          any                         `json:"alwaysAllOf,omitempty"`
	AlwaysList           []Always                    `json:"alwaysList,omitzero"`
	Beside               RefToFalseSchemaBeside      `json:"beside,omitzero"`
	List                 []Never                     `json:"list,omitzero"`
	Map                  map[string]Never            `json:"map,omitzero"`
	Prop                 Never                       `json:"prop,omitzero"`
	Tuple                []any                       `json:"tuple,omitzero"`
	ViaAllOf             RefToFalseSchemaViaAllOf    `json:"viaAllOf,omitzero"`
	ViaAnyOf             RefToFalseSchemaViaAnyOf    `json:"viaAnyOf,omitzero"`
	ViaNested            RefToFalseSchemaViaNested   `json:"viaNested,omitzero"`
	ViaOneOf             isRefToFalseSchema_ViaOneOf `json:"-"`
	AdditionalProperties map[string]json.RawMessage  `json:"-"`
	_jsonKeys            map[string]bool             // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool             // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

// isRefToFalseSchema_ViaOneOf is a sealed interface for the ViaOneOf field of RefToFalseSchema.
// Exactly one variant must be set.
type isRefToFalseSchema_ViaOneOf interface {
	isRefToFalseSchema_ViaOneOf()
}

// RefToFalseSchema_Never wraps *Never as a ViaOneOf variant.
type RefToFalseSchema_Never struct {
	Never *Never
}

func (*RefToFalseSchema_Never) isRefToFalseSchema_ViaOneOf() {}

// GetNever returns the Never variant value, or the zero value if not set.
func (r *RefToFalseSchema) GetNever() *Never {
	if r != nil {
		if v, ok := r.ViaOneOf.(*RefToFalseSchema_Never); ok {
			return v.Never
		}
	}
	var zero *Never
	return zero
}

func (r *RefToFalseSchema) UnmarshalJSON(data []byte) error {
	r.AdditionalProperties = nil
	r._jsonKeys = nil
	r._jsonNulls = nil
	r.ViaOneOf = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type RefToFalseSchema")
	}
	type Alias RefToFalseSchema
	aux := &struct {
		*Alias
		ViaOneOf json.RawMessage `json:"viaOneOf"`
	}{
		Alias: (*Alias)(r),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}

	{
		oneofData := aux.ViaOneOf
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isRefToFalseSchema_ViaOneOf
			var oneofStrictErr error

			// Try variant: Never
			{
				var candidate *Never
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					r.ViaOneOf = &RefToFalseSchema_Never{Never: candidate}
					oneofMatched++
					if candidate != nil {
						if _vErr := candidate.Validate(); _vErr == nil {
							oneofStrict++
							oneofStrictSel = &RefToFalseSchema_Never{Never: candidate}
						} else {
							oneofStrictErr = fmt.Errorf("variant Never: %w", _vErr)
						}
					} else {
						oneofOpaque++
					}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("RefToFalseSchema.ViaOneOf: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					r.ViaOneOf = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return fmt.Errorf("RefToFalseSchema.ViaOneOf: no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return fmt.Errorf("RefToFalseSchema.ViaOneOf: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"alwaysList",
			"beside",
			"list",
			"map",
			"tuple",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		r._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			r._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"always",
			"alwaysAllOf",
			"viaOneOf",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if r._jsonNulls == nil {
					r._jsonNulls = make(map[string]bool, 1)
				}
				r._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"always":      true,
			"alwaysAllOf": true,
			"alwaysList":  true,
			"beside":      true,
			"list":        true,
			"map":         true,
			"prop":        true,
			"tuple":       true,
			"viaAllOf":    true,
			"viaAnyOf":    true,
			"viaNested":   true,
			"viaOneOf":    true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if r.AdditionalProperties == nil {
				r.AdditionalProperties = make(map[string]json.RawMessage)
			}
			r.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (r RefToFalseSchema) MarshalJSON() ([]byte, error) {
	type Alias RefToFalseSchema
	aux := struct {
		Alias
		ViaOneOf json.RawMessage `json:"viaOneOf,omitempty"`
	}{
		Alias: (Alias)(r),
	}

	if r.ViaOneOf != nil {
		switch v := r.ViaOneOf.(type) {
		case *RefToFalseSchema_Never:
			raw, err := json.Marshal(v.Never)
			if err != nil {
				return nil, fmt.Errorf("marshaling RefToFalseSchema.ViaOneOf: %w", err)
			}
			aux.ViaOneOf = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(r._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range r._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range r.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks RefToFalseSchema against its JSON Schema constraints.
func (r RefToFalseSchema) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["beside"] {
		if err := r.Beside.Validate(); err != nil {
			return fmt.Errorf("beside.%w", err)
		}
	}
	for _i, _item := range r.List {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("list[%d].%w", _i, err)
		}
	}
	for _k, _val := range r.Map {
		if err := _val.Validate(); err != nil {
			return fmt.Errorf("map[%q].%w", _k, err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["prop"] {
		if err := r.Prop.Validate(); err != nil {
			return fmt.Errorf("prop.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["viaAllOf"] {
		if err := r.ViaAllOf.Validate(); err != nil {
			return fmt.Errorf("viaAllOf.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["viaAnyOf"] {
		if err := r.ViaAnyOf.Validate(); err != nil {
			return fmt.Errorf("viaAnyOf.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if r._jsonKeys == nil || r._jsonKeys["viaNested"] {
		if err := r.ViaNested.Validate(); err != nil {
			return fmt.Errorf("viaNested.%w", err)
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := r.ViaOneOf.(type) {
	case *RefToFalseSchema_Never:
		if _oneOfSel.Never != nil {
			if err := _oneOfSel.Never.Validate(); err != nil {
				return fmt.Errorf("viaOneOf.%w", err)
			}
		}
	}
	// Tuple items: validate each position against its schema type.
	for _idx, _elem := range r.Tuple {
		_ = _elem
		if _idx == 0 {
			_raw, _mErr := json.Marshal(_elem)
			if _mErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _mErr)
			}
			var _typed Never
			if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _uErr)
			}
			if _vErr := _typed.Validate(); _vErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _vErr)
			}
		}
	}
	return nil
}
