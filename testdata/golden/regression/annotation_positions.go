// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	"math"
	"unicode/utf8"
)

// AnnAlias - A declared string, which becomes a plain named type.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	"alias-1"
//	"alias-2"
//
// Deprecated: the schema marks this deprecated.
type AnnAlias string

func (a *AnnAlias) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnnAlias")
	}
	type Alias AnnAlias
	return json.Unmarshal(data, (*Alias)(a))
}

// Validate checks AnnAlias against its JSON Schema constraints.
func (a AnnAlias) Validate() error {
	if utf8.RuneCountInString(string(a)) < 1 {
		return fmt.Errorf("value: length %d is less than minimum 1", utf8.RuneCountInString(string(a)))
	}
	return nil
}

// AnnBigInt - An integer, which becomes the arbitrary-precision wrapper under BigIntSupport.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	7
//
// Deprecated: the schema marks this deprecated.
type AnnBigInt int64

func (a *AnnBigInt) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnnBigInt")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type AnnBigInt")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*a = AnnBigInt(_i)
		return nil
	}
	// Try float with zero fractional part (e.g., 1.0).
	if _f, _fErr := _n.Float64(); _fErr == nil {
		if _f == math.Trunc(_f) && !math.IsInf(_f, 0) && _f >= -9.223372036854776e+18 && _f <= 9.223372036854776e+18 {
			*a = AnnBigInt(int64(_f))
			return nil
		}
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks AnnBigInt against its JSON Schema constraints.
func (a AnnBigInt) Validate() error {
	if float64(a) < 0 {
		return fmt.Errorf("value: %v is less than minimum 0", a)
	}
	return nil
}

// AnnDynamic - Applicators and no type of its own, checked against the decoded value.
// AnnDynamic accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	"ab"
//
// Deprecated: the schema marks this deprecated.
type AnnDynamic struct {
	_raw json.RawMessage
}

func (a *AnnDynamic) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnnDynamic) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnnDynamic) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnnDynamic) IsZero() bool { return len(a._raw) == 0 }

func (a AnnDynamic) String() string { return string(a._raw) }

// AnnDynamicSchema is the schema AnnDynamic validates against.
var AnnDynamicSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			MinLength: _intPtr(2),
			Type:      []string{"string"},
		},
		_schemaNode{
			Minimum: _floatPtr(5.0),
			Type:    []string{"integer"},
		},
	},
}

// Validate checks AnnDynamic against its JSON Schema constraints.
func (a AnnDynamic) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnnDynamicSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// AnnEnum - A closed set of strings, which becomes a const enum.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	"red"
//
// Deprecated: the schema marks this deprecated.
type AnnEnum string

const (
	AnnEnumRed   AnnEnum = "red"
	AnnEnumGreen AnnEnum = "green"
)

// Validate checks AnnEnum against its JSON Schema constraints.
func (a AnnEnum) Validate() error {
	switch a {
	case AnnEnumRed, AnnEnumGreen:
		return nil
	default:
		return fmt.Errorf("invalid AnnEnum value: %v", a)
	}
}

// AnnInferred - Constraints with no declared type, so the Go type is inferred.
// AnnInferred accepts any JSON value. Constraints apply only when the value is number.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	4
//
// Deprecated: the schema marks this deprecated.
type AnnInferred struct {
	_value float64
	_raw   json.RawMessage
	_isRaw bool
}

func (a *AnnInferred) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		a._raw = append(a._raw[:0], data...)
		a._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &a._value); _err == nil {
		a._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	a._raw = append(a._raw[:0], data...)
	a._isRaw = true
	return nil
}
func (a AnnInferred) MarshalJSON() ([]byte, error) {
	if a._isRaw {
		if len(a._raw) == 0 {
			return []byte("null"), nil
		}
		return a._raw, nil
	}
	return json.Marshal(a._value)
}
func (a AnnInferred) Float64() float64 { return a._value }
func (a AnnInferred) IsNumber() bool   { return !a._isRaw }
func (a AnnInferred) Raw() json.RawMessage {
	if a._isRaw {
		return a._raw
	}
	_b, _ := json.Marshal(a._value)
	return _b
}
func (a AnnInferred) String() string {
	if a._isRaw {
		return string(a._raw)
	}
	return fmt.Sprintf("%v", a._value)
}

// Validate checks AnnInferred against its JSON Schema constraints.
func (a AnnInferred) Validate() error {
	if a._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if float64(a._value) < 3 {
		return fmt.Errorf("value: %v is less than minimum 3", a._value)
	}
	return nil
}

// AnnNot - A root-level negation.
// AnnNot accepts any JSON value and validates a root-level "not" constraint.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	1
//
// Deprecated: the schema marks this deprecated.
type AnnNot struct {
	_raw json.RawMessage
}

func (a *AnnNot) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnnNot) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnnNot) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnnNot) IsZero() bool { return len(a._raw) == 0 }

func (a AnnNot) String() string { return string(a._raw) }

// Validate checks AnnNot against its JSON Schema "not" constraint.
func (a AnnNot) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	// Decode raw JSON to determine the value's type.
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("not: cannot decode value: %w", _err)
	}
	if _, _sOk := _v.(string); _sOk {
		return fmt.Errorf("not: value must not be string")
	}
	return nil
}

// AnnRawEnum - A heterogeneous enum, which becomes a json.RawMessage enum rather than a const one.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	7
//
// Deprecated: the schema marks this deprecated.
type AnnRawEnum json.RawMessage

var annRawEnumAllowedJSON = []string{
	"\"mixed\"",
	"7",
}

func (a *AnnRawEnum) UnmarshalJSON(data []byte) error {
	*a = AnnRawEnum(data)
	return nil
}

func (a AnnRawEnum) MarshalJSON() ([]byte, error) {
	if len(a) == 0 {
		return []byte("null"), nil
	}
	return json.RawMessage(a).MarshalJSON()
}

// Validate checks AnnRawEnum against its JSON Schema constraints.
func (a AnnRawEnum) Validate() error {
	// Normalize to compact JSON for comparison (handles whitespace, key order, number format).
	var tmp any
	if err := json.Unmarshal([]byte(a), &tmp); err != nil {
		return fmt.Errorf("invalid AnnRawEnum value: %s", string(a))
	}
	canonical, err := json.Marshal(tmp)
	if err != nil {
		return fmt.Errorf("invalid AnnRawEnum value: %s", string(a))
	}
	_canon := string(canonical)
	for _, allowed := range annRawEnumAllowedJSON {
		if _canon == allowed {
			return nil
		}
	}
	return fmt.Errorf("invalid AnnRawEnum value: %s", _canon)
}

// AnnRuntime - unevaluatedItems beside an in-place applicator, so the schema is held as data.
// AnnRuntime accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	["a"]
//
// Deprecated: the schema marks this deprecated.
type AnnRuntime struct {
	_raw json.RawMessage
}

func (a *AnnRuntime) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnnRuntime) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnnRuntime) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnnRuntime) IsZero() bool { return len(a._raw) == 0 }

func (a AnnRuntime) String() string { return string(a._raw) }

// AnnRuntimeSchema is the schema AnnRuntime validates against.
var AnnRuntimeSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			PrefixItems: []_schemaNode{
				_schemaNode{
					Type: []string{"string"},
				},
			},
		},
	},
	UnevaluatedItems: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks AnnRuntime against its JSON Schema constraints.
func (a AnnRuntime) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnnRuntimeSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// AnnStruct - An object, the kind that carried these keywords before #171.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	{"a":"x"}
//
// Deprecated: the schema marks this deprecated.
type AnnStruct struct {
	A                    *string                    `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (a *AnnStruct) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnnStruct")
	}
	type Alias AnnStruct
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnnStruct) MarshalJSON() ([]byte, error) {
	type Alias AnnStruct
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnnStruct against its JSON Schema constraints.
func (a AnnStruct) Validate() error {
	return nil
}

// AnnTypeOnly - A type union no single Go type covers.
// AnnTypeOnly accepts any JSON value and validates that it is one of the allowed types.
//
// Read-only: the schema says "readOnly", so the owning authority manages
// this value and an application is not expected to send it.
// Write-only: the schema says "writeOnly", so the value is not expected
// to be present when the instance is retrieved.
// Examples from the schema:
//
//	1
//	"s"
//
// Deprecated: the schema marks this deprecated.
type AnnTypeOnly struct {
	_raw json.RawMessage
}

func (a *AnnTypeOnly) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnnTypeOnly) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnnTypeOnly) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnnTypeOnly) IsZero() bool { return len(a._raw) == 0 }

func (a AnnTypeOnly) String() string { return string(a._raw) }

// Validate checks AnnTypeOnly against its JSON Schema type constraint.
func (a AnnTypeOnly) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return fmt.Errorf("type: null is not allowed")
	}
	switch _tv := _v.(type) {
	case float64:
		if _tv != math.Trunc(_tv) || math.IsInf(_tv, 0) {
			return fmt.Errorf("type: expected integer, got number")
		}
		return nil
	case string:
		_ = _tv
		return nil
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

// DepAlias - Deprecated, and nothing else.
//
// Deprecated: the schema marks this deprecated.
type DepAlias string

func (d *DepAlias) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type DepAlias")
	}
	type Alias DepAlias
	return json.Unmarshal(data, (*Alias)(d))
}

// Validate checks DepAlias against its JSON Schema constraints.
func (d DepAlias) Validate() error {
	if utf8.RuneCountInString(string(d)) < 1 {
		return fmt.Errorf("value: length %d is less than minimum 1", utf8.RuneCountInString(string(d)))
	}
	return nil
}

// DepBigInt - Deprecated, and nothing else.
//
// Deprecated: the schema marks this deprecated.
type DepBigInt int64

func (d *DepBigInt) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type DepBigInt")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type DepBigInt")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*d = DepBigInt(_i)
		return nil
	}
	// Try float with zero fractional part (e.g., 1.0).
	if _f, _fErr := _n.Float64(); _fErr == nil {
		if _f == math.Trunc(_f) && !math.IsInf(_f, 0) && _f >= -9.223372036854776e+18 && _f <= 9.223372036854776e+18 {
			*d = DepBigInt(int64(_f))
			return nil
		}
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks DepBigInt against its JSON Schema constraints.
func (d DepBigInt) Validate() error {
	if float64(d) < 0 {
		return fmt.Errorf("value: %v is less than minimum 0", d)
	}
	return nil
}

// DepDynamic - Deprecated, and nothing else.
// DepDynamic accepts any JSON value. The schema declares no type of its own, so
// the constraints come entirely from its applicators and are checked against
// the decoded value by Validate.
//
// Deprecated: the schema marks this deprecated.
type DepDynamic struct {
	_raw json.RawMessage
}

func (d *DepDynamic) UnmarshalJSON(data []byte) error {
	d._raw = append(d._raw[:0], data...)
	return nil
}

func (d DepDynamic) MarshalJSON() ([]byte, error) {
	if len(d._raw) == 0 {
		return []byte("null"), nil
	}
	return d._raw, nil
}

func (d DepDynamic) Raw() json.RawMessage { return d._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (d DepDynamic) IsZero() bool { return len(d._raw) == 0 }

func (d DepDynamic) String() string { return string(d._raw) }

// Validate checks DepDynamic against its JSON Schema constraints.
func (d DepDynamic) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(d._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(d._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	{
		_matches := 0
		if _dynIsString(_v) && _dynStrOK(_v, func(s string) bool { return utf8.RuneCountInString(s) >= 2 }) {
			_matches++
		}
		if _dynIsInteger(_v) && _dynNumOK(_v, func(f float64) bool { return f >= 5.0 }) {
			_matches++
		}
		if _matches != 1 {
			return fmt.Errorf("oneOf: value matches %d variants, expected exactly 1", _matches)
		}
	}
	return nil
}

// DepEnum - Deprecated, and nothing else.
//
// Deprecated: the schema marks this deprecated.
type DepEnum string

const (
	DepEnumOn  DepEnum = "on"
	DepEnumOff DepEnum = "off"
)

// Validate checks DepEnum against its JSON Schema constraints.
func (d DepEnum) Validate() error {
	switch d {
	case DepEnumOn, DepEnumOff:
		return nil
	default:
		return fmt.Errorf("invalid DepEnum value: %v", d)
	}
}

// DepInferred - Deprecated, and nothing else.
// DepInferred accepts any JSON value. Constraints apply only when the value is number.
//
// Deprecated: the schema marks this deprecated.
type DepInferred struct {
	_value float64
	_raw   json.RawMessage
	_isRaw bool
}

func (d *DepInferred) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		d._raw = append(d._raw[:0], data...)
		d._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &d._value); _err == nil {
		d._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	d._raw = append(d._raw[:0], data...)
	d._isRaw = true
	return nil
}
func (d DepInferred) MarshalJSON() ([]byte, error) {
	if d._isRaw {
		if len(d._raw) == 0 {
			return []byte("null"), nil
		}
		return d._raw, nil
	}
	return json.Marshal(d._value)
}
func (d DepInferred) Float64() float64 { return d._value }
func (d DepInferred) IsNumber() bool   { return !d._isRaw }
func (d DepInferred) Raw() json.RawMessage {
	if d._isRaw {
		return d._raw
	}
	_b, _ := json.Marshal(d._value)
	return _b
}
func (d DepInferred) String() string {
	if d._isRaw {
		return string(d._raw)
	}
	return fmt.Sprintf("%v", d._value)
}

// Validate checks DepInferred against its JSON Schema constraints.
func (d DepInferred) Validate() error {
	if d._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if float64(d._value) < 3 {
		return fmt.Errorf("value: %v is less than minimum 3", d._value)
	}
	return nil
}

// DepNot - Deprecated, and nothing else.
// DepNot accepts any JSON value and validates a root-level "not" constraint.
//
// Deprecated: the schema marks this deprecated.
type DepNot struct {
	_raw json.RawMessage
}

func (d *DepNot) UnmarshalJSON(data []byte) error {
	d._raw = append(d._raw[:0], data...)
	return nil
}

func (d DepNot) MarshalJSON() ([]byte, error) {
	if len(d._raw) == 0 {
		return []byte("null"), nil
	}
	return d._raw, nil
}

func (d DepNot) Raw() json.RawMessage { return d._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (d DepNot) IsZero() bool { return len(d._raw) == 0 }

func (d DepNot) String() string { return string(d._raw) }

// Validate checks DepNot against its JSON Schema "not" constraint.
func (d DepNot) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(d._raw) == 0 {
		return nil
	}
	// Decode raw JSON to determine the value's type.
	var _v any
	if _err := json.Unmarshal(d._raw, &_v); _err != nil {
		return fmt.Errorf("not: cannot decode value: %w", _err)
	}
	if _, _sOk := _v.(string); _sOk {
		return fmt.Errorf("not: value must not be string")
	}
	return nil
}

// DepRawEnum - Deprecated, and nothing else.
//
// Deprecated: the schema marks this deprecated.
type DepRawEnum json.RawMessage

var depRawEnumAllowedJSON = []string{
	"\"either\"",
	"9",
}

func (d *DepRawEnum) UnmarshalJSON(data []byte) error {
	*d = DepRawEnum(data)
	return nil
}

func (d DepRawEnum) MarshalJSON() ([]byte, error) {
	if len(d) == 0 {
		return []byte("null"), nil
	}
	return json.RawMessage(d).MarshalJSON()
}

// Validate checks DepRawEnum against its JSON Schema constraints.
func (d DepRawEnum) Validate() error {
	// Normalize to compact JSON for comparison (handles whitespace, key order, number format).
	var tmp any
	if err := json.Unmarshal([]byte(d), &tmp); err != nil {
		return fmt.Errorf("invalid DepRawEnum value: %s", string(d))
	}
	canonical, err := json.Marshal(tmp)
	if err != nil {
		return fmt.Errorf("invalid DepRawEnum value: %s", string(d))
	}
	_canon := string(canonical)
	for _, allowed := range depRawEnumAllowedJSON {
		if _canon == allowed {
			return nil
		}
	}
	return fmt.Errorf("invalid DepRawEnum value: %s", _canon)
}

// DepRuntime - Deprecated, and nothing else.
// DepRuntime accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
//
// Deprecated: the schema marks this deprecated.
type DepRuntime struct {
	_raw json.RawMessage
}

func (d *DepRuntime) UnmarshalJSON(data []byte) error {
	d._raw = append(d._raw[:0], data...)
	return nil
}

func (d DepRuntime) MarshalJSON() ([]byte, error) {
	if len(d._raw) == 0 {
		return []byte("null"), nil
	}
	return d._raw, nil
}

func (d DepRuntime) Raw() json.RawMessage { return d._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (d DepRuntime) IsZero() bool { return len(d._raw) == 0 }

func (d DepRuntime) String() string { return string(d._raw) }

// DepRuntimeSchema is the schema DepRuntime validates against.
var DepRuntimeSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			PrefixItems: []_schemaNode{
				_schemaNode{
					Type: []string{"string"},
				},
			},
		},
	},
	UnevaluatedItems: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks DepRuntime against its JSON Schema constraints.
func (d DepRuntime) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(d._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(d._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&DepRuntimeSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// DepStruct - Deprecated, and nothing else.
//
// Deprecated: the schema marks this deprecated.
type DepStruct struct {
	C                    *string                    `json:"c,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (d *DepStruct) UnmarshalJSON(data []byte) error {
	d.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type DepStruct")
	}
	type Alias DepStruct
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(d),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"c",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"c": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if d.AdditionalProperties == nil {
				d.AdditionalProperties = make(map[string]json.RawMessage)
			}
			d.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (d DepStruct) MarshalJSON() ([]byte, error) {
	type Alias DepStruct
	aux := struct {
		Alias
	}{
		Alias: (Alias)(d),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range d.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks DepStruct against its JSON Schema constraints.
func (d DepStruct) Validate() error {
	return nil
}

// DepTypeOnly - Deprecated, and nothing else.
// DepTypeOnly accepts any JSON value and validates that it is one of the allowed types.
//
// Deprecated: the schema marks this deprecated.
type DepTypeOnly struct {
	_raw json.RawMessage
}

func (d *DepTypeOnly) UnmarshalJSON(data []byte) error {
	d._raw = append(d._raw[:0], data...)
	return nil
}

func (d DepTypeOnly) MarshalJSON() ([]byte, error) {
	if len(d._raw) == 0 {
		return []byte("null"), nil
	}
	return d._raw, nil
}

func (d DepTypeOnly) Raw() json.RawMessage { return d._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (d DepTypeOnly) IsZero() bool { return len(d._raw) == 0 }

func (d DepTypeOnly) String() string { return string(d._raw) }

// Validate checks DepTypeOnly against its JSON Schema type constraint.
func (d DepTypeOnly) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(d._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(d._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return fmt.Errorf("type: null is not allowed")
	}
	switch _tv := _v.(type) {
	case float64:
		if _tv != math.Trunc(_tv) || math.IsInf(_tv, 0) {
			return fmt.Errorf("type: expected integer, got number")
		}
		return nil
	case string:
		_ = _tv
		return nil
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type PlainAlias string

func (p *PlainAlias) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PlainAlias")
	}
	type Alias PlainAlias
	return json.Unmarshal(data, (*Alias)(p))
}

// Validate checks PlainAlias against its JSON Schema constraints.
func (p PlainAlias) Validate() error {
	if utf8.RuneCountInString(string(p)) < 1 {
		return fmt.Errorf("value: length %d is less than minimum 1", utf8.RuneCountInString(string(p)))
	}
	return nil
}

type PlainBigInt int64

func (p *PlainBigInt) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PlainBigInt")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type PlainBigInt")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*p = PlainBigInt(_i)
		return nil
	}
	// Try float with zero fractional part (e.g., 1.0).
	if _f, _fErr := _n.Float64(); _fErr == nil {
		if _f == math.Trunc(_f) && !math.IsInf(_f, 0) && _f >= -9.223372036854776e+18 && _f <= 9.223372036854776e+18 {
			*p = PlainBigInt(int64(_f))
			return nil
		}
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks PlainBigInt against its JSON Schema constraints.
func (p PlainBigInt) Validate() error {
	if float64(p) < 0 {
		return fmt.Errorf("value: %v is less than minimum 0", p)
	}
	return nil
}

// PlainDynamic accepts any JSON value. The schema declares no type of its own, so
// the constraints come entirely from its applicators and are checked against
// the decoded value by Validate.
type PlainDynamic struct {
	_raw json.RawMessage
}

func (p *PlainDynamic) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PlainDynamic) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PlainDynamic) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PlainDynamic) IsZero() bool { return len(p._raw) == 0 }

func (p PlainDynamic) String() string { return string(p._raw) }

// Validate checks PlainDynamic against its JSON Schema constraints.
func (p PlainDynamic) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	{
		_matches := 0
		if _dynIsString(_v) && _dynStrOK(_v, func(s string) bool { return utf8.RuneCountInString(s) >= 2 }) {
			_matches++
		}
		if _dynIsInteger(_v) && _dynNumOK(_v, func(f float64) bool { return f >= 5.0 }) {
			_matches++
		}
		if _matches != 1 {
			return fmt.Errorf("oneOf: value matches %d variants, expected exactly 1", _matches)
		}
	}
	return nil
}

type PlainEnum string

const (
	PlainEnumUp   PlainEnum = "up"
	PlainEnumDown PlainEnum = "down"
)

// Validate checks PlainEnum against its JSON Schema constraints.
func (p PlainEnum) Validate() error {
	switch p {
	case PlainEnumUp, PlainEnumDown:
		return nil
	default:
		return fmt.Errorf("invalid PlainEnum value: %v", p)
	}
}

// PlainInferred accepts any JSON value. Constraints apply only when the value is number.
type PlainInferred struct {
	_value float64
	_raw   json.RawMessage
	_isRaw bool
}

func (p *PlainInferred) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		p._raw = append(p._raw[:0], data...)
		p._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &p._value); _err == nil {
		p._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	p._raw = append(p._raw[:0], data...)
	p._isRaw = true
	return nil
}
func (p PlainInferred) MarshalJSON() ([]byte, error) {
	if p._isRaw {
		if len(p._raw) == 0 {
			return []byte("null"), nil
		}
		return p._raw, nil
	}
	return json.Marshal(p._value)
}
func (p PlainInferred) Float64() float64 { return p._value }
func (p PlainInferred) IsNumber() bool   { return !p._isRaw }
func (p PlainInferred) Raw() json.RawMessage {
	if p._isRaw {
		return p._raw
	}
	_b, _ := json.Marshal(p._value)
	return _b
}
func (p PlainInferred) String() string {
	if p._isRaw {
		return string(p._raw)
	}
	return fmt.Sprintf("%v", p._value)
}

// Validate checks PlainInferred against its JSON Schema constraints.
func (p PlainInferred) Validate() error {
	if p._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	if float64(p._value) < 3 {
		return fmt.Errorf("value: %v is less than minimum 3", p._value)
	}
	return nil
}

// PlainNot accepts any JSON value and validates a root-level "not" constraint.
type PlainNot struct {
	_raw json.RawMessage
}

func (p *PlainNot) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PlainNot) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PlainNot) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PlainNot) IsZero() bool { return len(p._raw) == 0 }

func (p PlainNot) String() string { return string(p._raw) }

// Validate checks PlainNot against its JSON Schema "not" constraint.
func (p PlainNot) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	// Decode raw JSON to determine the value's type.
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("not: cannot decode value: %w", _err)
	}
	if _, _sOk := _v.(string); _sOk {
		return fmt.Errorf("not: value must not be string")
	}
	return nil
}

// PlainRuntime accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type PlainRuntime struct {
	_raw json.RawMessage
}

func (p *PlainRuntime) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PlainRuntime) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PlainRuntime) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PlainRuntime) IsZero() bool { return len(p._raw) == 0 }

func (p PlainRuntime) String() string { return string(p._raw) }

// PlainRuntimeSchema is the schema PlainRuntime validates against.
var PlainRuntimeSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			PrefixItems: []_schemaNode{
				_schemaNode{
					Type: []string{"string"},
				},
			},
		},
	},
	UnevaluatedItems: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks PlainRuntime against its JSON Schema constraints.
func (p PlainRuntime) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&PlainRuntimeSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type PlainStruct struct {
	B                    *string                    `json:"b,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (p *PlainStruct) UnmarshalJSON(data []byte) error {
	p.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type PlainStruct")
	}
	type Alias PlainStruct
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(p),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"b",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"b": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if p.AdditionalProperties == nil {
				p.AdditionalProperties = make(map[string]json.RawMessage)
			}
			p.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (p PlainStruct) MarshalJSON() ([]byte, error) {
	type Alias PlainStruct
	aux := struct {
		Alias
	}{
		Alias: (Alias)(p),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range p.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks PlainStruct against its JSON Schema constraints.
func (p PlainStruct) Validate() error {
	return nil
}

// PlainTypeOnly accepts any JSON value and validates that it is one of the allowed types.
type PlainTypeOnly struct {
	_raw json.RawMessage
}

func (p *PlainTypeOnly) UnmarshalJSON(data []byte) error {
	p._raw = append(p._raw[:0], data...)
	return nil
}

func (p PlainTypeOnly) MarshalJSON() ([]byte, error) {
	if len(p._raw) == 0 {
		return []byte("null"), nil
	}
	return p._raw, nil
}

func (p PlainTypeOnly) Raw() json.RawMessage { return p._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (p PlainTypeOnly) IsZero() bool { return len(p._raw) == 0 }

func (p PlainTypeOnly) String() string { return string(p._raw) }

// Validate checks PlainTypeOnly against its JSON Schema type constraint.
func (p PlainTypeOnly) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(p._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(p._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return fmt.Errorf("type: null is not allowed")
	}
	switch _tv := _v.(type) {
	case float64:
		if _tv != math.Trunc(_tv) || math.IsInf(_tv, 0) {
			return fmt.Errorf("type: expected integer, got number")
		}
		return nil
	case string:
		_ = _tv
		return nil
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

// AnnotationPositions - Every annotation-vocabulary keyword written on every kind of named type schemagen can generate. Before issue #171 only the two struct kinds carried them: an alias, an enum, an inferred alias, a type-only wrapper, a dynamic wrapper, a `not` wrapper and a runtime-annotation wrapper all took the description above their declaration and dropped `deprecated`, `readOnly`, `writeOnly` and `examples`, so a $defs entry the schema marked deprecated produced a Go type nothing warns about. The big-int alias is the one kind that needs a configuration to exist, and is generated from this same document under BigIntSupport. Each kind is written three times. AnnX carries all four keywords. PlainX is the control, writing the three booleans as false, and must read as a type with nothing said about it. DepX carries a description and `deprecated` alone, and it is the one that can see whether the notice is its own paragraph: beside `examples` gofmt inserts the blank comment line itself, around the indented block the example lines become, so a template that forgot the paragraph break would still look right there and be wrong everywhere else.
type AnnotationPositions struct {
	Alias                *AnnAlias                  `json:"alias,omitempty"`
	AliasPlain           *PlainAlias                `json:"aliasPlain,omitempty"`
	BigInt               *AnnBigInt                 `json:"bigInt,omitempty"`
	BigIntPlain          *PlainBigInt               `json:"bigIntPlain,omitempty"`
	Dynamic              AnnDynamic                 `json:"dynamic,omitzero"`
	DynamicPlain         PlainDynamic               `json:"dynamicPlain,omitzero"`
	Enum                 *AnnEnum                   `json:"enum,omitempty"`
	EnumPlain            *PlainEnum                 `json:"enumPlain,omitempty"`
	Inferred             *AnnInferred               `json:"inferred,omitempty"`
	InferredPlain        *PlainInferred             `json:"inferredPlain,omitempty"`
	Not                  AnnNot                     `json:"not,omitzero"`
	NotPlain             PlainNot                   `json:"notPlain,omitzero"`
	RawEnum              AnnRawEnum                 `json:"rawEnum,omitempty"`
	Runtime              AnnRuntime                 `json:"runtime,omitzero"`
	RuntimePlain         PlainRuntime               `json:"runtimePlain,omitzero"`
	Struct               *AnnStruct                 `json:"struct,omitempty"`
	StructPlain          *PlainStruct               `json:"structPlain,omitempty"`
	TypeOnly             AnnTypeOnly                `json:"typeOnly,omitzero"`
	TypeOnlyPlain        PlainTypeOnly              `json:"typeOnlyPlain,omitzero"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool            // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (a *AnnotationPositions) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonNulls = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnnotationPositions")
	}
	type Alias AnnotationPositions
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"alias",
			"aliasPlain",
			"bigInt",
			"bigIntPlain",
			"dynamic",
			"dynamicPlain",
			"enum",
			"enumPlain",
			"rawEnum",
			"struct",
			"structPlain",
			"typeOnly",
			"typeOnlyPlain",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"inferred",
			"inferredPlain",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if a._jsonNulls == nil {
					a._jsonNulls = make(map[string]bool, 1)
				}
				a._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"alias":         true,
			"aliasPlain":    true,
			"bigInt":        true,
			"bigIntPlain":   true,
			"dynamic":       true,
			"dynamicPlain":  true,
			"enum":          true,
			"enumPlain":     true,
			"inferred":      true,
			"inferredPlain": true,
			"not":           true,
			"notPlain":      true,
			"rawEnum":       true,
			"runtime":       true,
			"runtimePlain":  true,
			"struct":        true,
			"structPlain":   true,
			"typeOnly":      true,
			"typeOnlyPlain": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnnotationPositions) MarshalJSON() ([]byte, error) {
	type Alias AnnotationPositions
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(a._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range a._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnnotationPositions against its JSON Schema constraints.
func (a AnnotationPositions) Validate() error {
	if a.Alias != nil {
		if err := a.Alias.Validate(); err != nil {
			return fmt.Errorf("alias.%w", err)
		}
	}
	if a.AliasPlain != nil {
		if err := a.AliasPlain.Validate(); err != nil {
			return fmt.Errorf("aliasPlain.%w", err)
		}
	}
	if a.BigInt != nil {
		if err := a.BigInt.Validate(); err != nil {
			return fmt.Errorf("bigInt.%w", err)
		}
	}
	if a.BigIntPlain != nil {
		if err := a.BigIntPlain.Validate(); err != nil {
			return fmt.Errorf("bigIntPlain.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["dynamic"] {
		if err := a.Dynamic.Validate(); err != nil {
			return fmt.Errorf("dynamic.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["dynamicPlain"] {
		if err := a.DynamicPlain.Validate(); err != nil {
			return fmt.Errorf("dynamicPlain.%w", err)
		}
	}
	if a.Enum != nil {
		if err := a.Enum.Validate(); err != nil {
			return fmt.Errorf("enum.%w", err)
		}
	}
	if a.EnumPlain != nil {
		if err := a.EnumPlain.Validate(); err != nil {
			return fmt.Errorf("enumPlain.%w", err)
		}
	}
	if a.Inferred != nil {
		if err := a.Inferred.Validate(); err != nil {
			return fmt.Errorf("inferred.%w", err)
		}
	}
	if a.InferredPlain != nil {
		if err := a.InferredPlain.Validate(); err != nil {
			return fmt.Errorf("inferredPlain.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["not"] {
		if err := a.Not.Validate(); err != nil {
			return fmt.Errorf("not.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["notPlain"] {
		if err := a.NotPlain.Validate(); err != nil {
			return fmt.Errorf("notPlain.%w", err)
		}
	}
	if a.RawEnum != nil {
		if err := a.RawEnum.Validate(); err != nil {
			return fmt.Errorf("rawEnum.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["runtime"] {
		if err := a.Runtime.Validate(); err != nil {
			return fmt.Errorf("runtime.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["runtimePlain"] {
		if err := a.RuntimePlain.Validate(); err != nil {
			return fmt.Errorf("runtimePlain.%w", err)
		}
	}
	if a.Struct != nil {
		if err := a.Struct.Validate(); err != nil {
			return fmt.Errorf("struct.%w", err)
		}
	}
	if a.StructPlain != nil {
		if err := a.StructPlain.Validate(); err != nil {
			return fmt.Errorf("structPlain.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["typeOnly"] {
		if err := a.TypeOnly.Validate(); err != nil {
			return fmt.Errorf("typeOnly.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["typeOnlyPlain"] {
		if err := a.TypeOnlyPlain.Validate(); err != nil {
			return fmt.Errorf("typeOnlyPlain.%w", err)
		}
	}
	return nil
}
