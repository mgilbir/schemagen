// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
	"math"
	"unicode/utf8"
)

type RowsItem struct {
	Label                string                     `json:"label"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (r *RowsItem) UnmarshalJSON(data []byte) error {
	r.AdditionalProperties = nil
	r._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"label",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias RowsItem
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(r),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"label", jsonDecodeValue[string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"label",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		r._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			r._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"label": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if r.AdditionalProperties == nil {
				r.AdditionalProperties = make(map[string]json.RawMessage)
			}
			r.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (r RowsItem) MarshalJSON() ([]byte, error) {
	type Alias RowsItem
	aux := struct {
		Alias
	}{
		Alias: (Alias)(r),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range r.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks RowsItem against its JSON Schema constraints.
func (r RowsItem) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if r._jsonKeys != nil {
		for _, _req := range []string{"label"} {
			if !r._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	if utf8.RuneCountInString(r.Label) < 1 {
		return fmt.Errorf("label: length %d is less than minimum 1", utf8.RuneCountInString(r.Label))
	}
	return nil
}

type Rows []RowsItem

func (r *Rows) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	if _err := checkJSONNullsAt(data, &jsonNullRule{Elem: &jsonNullRule{Reject: true}}); _err != nil {
		return _err
	}
	type Alias Rows
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(r)))
}

// Validate checks Rows against its JSON Schema constraints.
func (r Rows) Validate() error {
	for _i0, _e0 := range r {
		if _err := _e0.Validate(); _err != nil {
			return jsonElemPathf(_err, "[%d]", _i0)
		}
	}
	return nil
}

type Tags []string

func (t *Tags) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	if _err := checkJSONNullsAt(data, &jsonNullRule{Elem: &jsonNullRule{Reject: true}}); _err != nil {
		return _err
	}
	type Alias Tags
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(t)))
}

// Validate checks Tags against its JSON Schema constraints.
func (t Tags) Validate() error {
	for _i0, _e0 := range t {
		if utf8.RuneCountInString(string(_e0)) < 2 {
			return jsonElemErrorf("[%d]: length %d is less than minimum 2", _i0, utf8.RuneCountInString(string(_e0)))
		}
	}
	return nil
}

type ItemConstraintsMarksItem float64

const (
	ItemConstraintsMarksItem5 ItemConstraintsMarksItem = 5
)

// Validate checks ItemConstraintsMarksItem against its JSON Schema constraints.
func (i ItemConstraintsMarksItem) Validate() error {
	switch i {
	case ItemConstraintsMarksItem5:
		return nil
	default:
		return jsonValueErrorf("invalid ItemConstraintsMarksItem value: %v", i)
	}
}

type ItemConstraints struct {
	Codes                []string                   `json:"codes,omitzero"`
	Counts               []int64                    `json:"counts,omitzero"`
	Grid                 [][]int64                  `json:"grid,omitzero"`
	Marks                []ItemConstraintsMarksItem `json:"marks,omitzero"`
	Names                []string                   `json:"names"`
	Nicknames            []*string                  `json:"nicknames,omitzero"`
	Ratios               []float64                  `json:"ratios,omitzero"`
	Rows                 Rows                       `json:"rows,omitzero"`
	Tags                 Tags                       `json:"tags,omitzero"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (i *ItemConstraints) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"codes",
			"counts",
			"grid",
			"marks",
			"names",
			"nicknames",
			"ratios",
			"rows",
			"tags",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias ItemConstraints
	aux := &struct {
		*Alias
		Counts *[]jsonInteger   `json:"counts"`
		Grid   *[][]jsonInteger `json:"grid"`
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"codes", jsonDecodeItems(jsonDecodeValue[string])},
			{"counts", jsonDecodeItems(jsonDecodeValue[jsonInteger])},
			{"grid", jsonDecodeItems(jsonDecodeItems(jsonDecodeValue[jsonInteger]))},
			{"marks", jsonDecodeItems(jsonDecodeValue[ItemConstraintsMarksItem])},
			{"names", jsonDecodeItems(jsonDecodeValue[string])},
			{"nicknames", jsonDecodeItems(jsonDecodeValue[*string])},
			{"ratios", jsonDecodeItems(jsonDecodeValue[float64])},
			{"rows", jsonDecodeValue[Rows]},
			{"tags", jsonDecodeValue[Tags]},
		})
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.Counts != nil {
		_iv := *aux.Counts
		i.Counts = jsonIntegerSlice(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	if aux.Grid != nil {
		_iv := *aux.Grid
		i.Grid = jsonIntegerSlice(_iv, func(_ix0 []jsonInteger) []int64 {
			return jsonIntegerSlice(_ix0, func(_ix1 jsonInteger) int64 { return int64(_ix1) })
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"nicknames",
			"rows",
			"tags",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		if _v, ok := raw["codes"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "codes")
			}
		}
		if _v, ok := raw["counts"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "counts")
			}
		}
		if _v, ok := raw["grid"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}}); err != nil {
				return jsonPathf(err, "%s", "grid")
			}
		}
		if _v, ok := raw["marks"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "marks")
			}
		}
		if _v, ok := raw["names"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "names")
			}
		}
		if _v, ok := raw["ratios"]; ok {
			if err := checkJSONNullsAt(_v, &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return jsonPathf(err, "%s", "ratios")
			}
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"codes":     true,
			"counts":    true,
			"grid":      true,
			"marks":     true,
			"names":     true,
			"nicknames": true,
			"ratios":    true,
			"rows":      true,
			"tags":      true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i ItemConstraints) MarshalJSON() ([]byte, error) {
	type Alias ItemConstraints
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks ItemConstraints against its JSON Schema constraints.
func (i ItemConstraints) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if i._jsonKeys != nil {
		for _, _req := range []string{"names"} {
			if !i._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	for _i, _item := range i.Marks {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "marks[%d]", _i)
		}
	}
	if i.Rows != nil {
		if err := i.Rows.Validate(); err != nil {
			return jsonPathf(err, "rows")
		}
	}
	if i.Tags != nil {
		if err := i.Tags.Validate(); err != nil {
			return jsonPathf(err, "tags")
		}
	}
	for _i0, _e0 := range i.Codes {
		if _matched, _ := ecma262.MatchString("^x[0-9][0-9]$", ecmaflags.Unicode, string(_e0)); !_matched {
			return fmt.Errorf("codes[%d]: value %q does not match pattern %s", _i0, _e0, "^x[0-9][0-9]$")
		}
	}
	for _i0, _e0 := range i.Counts {
		if float64(_e0) < 3 {
			return fmt.Errorf("counts[%d]: value %v is less than minimum 3", _i0, _e0)
		}
		if float64(_e0) > 99 {
			return fmt.Errorf("counts[%d]: value %v exceeds maximum 99", _i0, _e0)
		}
		{
			_q := float64(_e0) / 3
			if math.Abs(_q-math.Round(_q)) > 1e-9 {
				return fmt.Errorf("counts[%d]: value %v is not a multiple of 3", _i0, _e0)
			}
		}
	}
	for _i0, _e0 := range i.Grid {
		if len(_e0) > 3 {
			return fmt.Errorf("grid[%d]: has %d items, maximum is 3", _i0, len(_e0))
		}
		for _i1, _e1 := range _e0 {
			if float64(_e1) < 1 {
				return fmt.Errorf("grid[%d][%d]: value %v is less than minimum 1", _i0, _i1, _e1)
			}
		}
	}
	for _i0, _e0 := range i.Names {
		if utf8.RuneCountInString(string(_e0)) < 2 {
			return fmt.Errorf("names[%d]: length %d is less than minimum 2", _i0, utf8.RuneCountInString(string(_e0)))
		}
		if utf8.RuneCountInString(string(_e0)) > 8 {
			return fmt.Errorf("names[%d]: length %d exceeds maximum 8", _i0, utf8.RuneCountInString(string(_e0)))
		}
	}
	for _i0, _e0 := range i.Nicknames {
		if _e0 == nil {
			continue // a JSON null left no value behind to check
		}
		if utf8.RuneCountInString(string(*_e0)) < 2 {
			return fmt.Errorf("nicknames[%d]: length %d is less than minimum 2", _i0, utf8.RuneCountInString(string(*_e0)))
		}
	}
	for _i0, _e0 := range i.Ratios {
		if float64(_e0) <= 0 {
			return fmt.Errorf("ratios[%d]: value %v must be greater than 0", _i0, _e0)
		}
		if float64(_e0) >= 1 {
			return fmt.Errorf("ratios[%d]: value %v must be less than 1", _i0, _e0)
		}
	}
	return nil
}
