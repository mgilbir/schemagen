// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

// NullableObj accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableObj struct {
	_raw json.RawMessage
}

func (n *NullableObj) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableObj) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableObj) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableObj) IsZero() bool { return len(n._raw) == 0 }

func (n NullableObj) String() string { return string(n._raw) }

// NullableObjSchema is the schema NullableObj validates against.
var NullableObjSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableObj against its JSON Schema constraints.
func (n NullableObj) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableObjSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type NullableWordValue string

const (
	NullableWordValueA NullableWordValue = "a"
	NullableWordValueC NullableWordValue = "c"
)

// Validate checks NullableWordValue against its JSON Schema constraints.
func (n NullableWordValue) Validate() error {
	switch n {
	case NullableWordValueA, NullableWordValueC:
		return nil
	default:
		return fmt.Errorf("invalid NullableWordValue value: %v", n)
	}
}

// NullableWord accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type NullableWord struct {
	_raw json.RawMessage
}

func (n *NullableWord) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableWord) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableWord) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableWord) IsZero() bool { return len(n._raw) == 0 }

func (n NullableWord) String() string { return string(n._raw) }

// NullableWordSchema is the schema NullableWord validates against.
var NullableWordSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Enum: []string{"\"a\"", "\"c\""},
			Type: []string{"string"},
		},
		_schemaNode{
			Type: []string{"null"},
		},
	},
}

// Validate checks NullableWord against its JSON Schema constraints.
func (n NullableWord) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&NullableWordSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// NullableAnyOfNamedBranch - The same collapse at the one site that answers with an alias rather than with a field. `type X *T` is a pointer underlying type, which Go forbids methods on, so X carries no Validate and nothing ever calls T's -- neither a caller holding an X nor the property here, which reaches it through a $ref. Both nullable definitions accepted every object. Worse, the branch was generated under the alias's own name, so the enum spelling emitted the same identifier twice and the output did not compile at all.
type NullableAnyOfNamedBranch struct {
	Obj                  NullableObj                `json:"obj,omitzero"`
	Word                 NullableWord               `json:"word,omitzero"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (n *NullableAnyOfNamedBranch) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type NullableAnyOfNamedBranch")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"obj",
			"word",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias NullableAnyOfNamedBranch
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"obj":  true,
			"word": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NullableAnyOfNamedBranch) MarshalJSON() ([]byte, error) {
	type Alias NullableAnyOfNamedBranch
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NullableAnyOfNamedBranch against its JSON Schema constraints.
func (n NullableAnyOfNamedBranch) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["obj"] {
		if err := n.Obj.Validate(); err != nil {
			return fmt.Errorf("obj.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["word"] {
		if err := n.Word.Validate(); err != nil {
			return fmt.Errorf("word.%w", err)
		}
	}
	return nil
}
