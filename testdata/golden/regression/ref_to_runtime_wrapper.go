// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

// Wrapped accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type Wrapped struct {
	_raw json.RawMessage
}

func (w *Wrapped) UnmarshalJSON(data []byte) error {
	w._raw = append(w._raw[:0], data...)
	return nil
}

func (w Wrapped) MarshalJSON() ([]byte, error) {
	if len(w._raw) == 0 {
		return []byte("null"), nil
	}
	return w._raw, nil
}

func (w Wrapped) Raw() json.RawMessage { return w._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (w Wrapped) IsZero() bool { return len(w._raw) == 0 }

func (w Wrapped) String() string { return string(w._raw) }

// WrappedSchema is the schema Wrapped validates against.
var WrappedSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			AnyOf: []_schemaNode{
				_schemaNode{
					MinLength: _intPtr(2),
					Type:      []string{"string"},
				},
			},
		},
	},
}

// Validate checks Wrapped against its JSON Schema constraints.
func (w Wrapped) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(w._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(w._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&WrappedSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type RefToRuntimeWrapper Wrapped

func (r *RefToRuntimeWrapper) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type RefToRuntimeWrapper")
	}
	var _target Wrapped
	if _err := json.Unmarshal(data, &_target); _err != nil {
		return _err
	}
	*r = RefToRuntimeWrapper(_target)
	return nil
}
func (r RefToRuntimeWrapper) MarshalJSON() ([]byte, error) {
	return json.Marshal(Wrapped(r))
}

// Validate checks RefToRuntimeWrapper against its JSON Schema constraints.
func (r RefToRuntimeWrapper) Validate() error {
	// Returned as it stands: the type this delegates to answers for the very same
	// value, so there is no step of path between the two and nothing to put in
	// front of its message. Wrapping it also lost what the message had recorded
	// about how a container must join it. See issue #279.
	if _err := (Wrapped(r)).Validate(); _err != nil {
		return _err
	}
	return nil
}
