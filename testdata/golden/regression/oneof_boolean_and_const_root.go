// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

// OneOfBooleanAndConstRoot - Issue #125's own reproducer, at the document root. The root union lives in a Go struct, so besides miscounting the branches it had nowhere for the scalar the `const` branch admits to decode into at all -- "x" was refused by encoding/json before a branch was tried.
// OneOfBooleanAndConstRoot accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfBooleanAndConstRoot struct {
	_raw json.RawMessage
}

func (o *OneOfBooleanAndConstRoot) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfBooleanAndConstRoot) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfBooleanAndConstRoot) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfBooleanAndConstRoot) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfBooleanAndConstRoot) String() string { return string(o._raw) }

// OneOfBooleanAndConstRootSchema is the schema OneOfBooleanAndConstRoot validates against.
var OneOfBooleanAndConstRootSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Const: _strPtr("\"x\""),
		},
		_schemaNode{Boolean: _boolPtr(false)},
	},
}

// Validate checks OneOfBooleanAndConstRoot against its JSON Schema constraints.
func (o OneOfBooleanAndConstRoot) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfBooleanAndConstRootSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}
