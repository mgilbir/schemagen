// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

type OneOfRootScalarBranchValueOption0 struct {
	K                    string                     `json:"k"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfRootScalarBranchValueOption0) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfRootScalarBranchValueOption0
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"k", jsonDecodeValue[string]},
		})
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfRootScalarBranchValueOption0) MarshalJSON() ([]byte, error) {
	type Alias OneOfRootScalarBranchValueOption0
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfRootScalarBranchValueOption0 against its JSON Schema constraints.
func (o OneOfRootScalarBranchValueOption0) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"k"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// OneOfRootScalarBranch - A root oneOf whose branches the union selects on correctly, but one of which admits a value no Go struct can hold. The union keeps its sealed interface; what changes is that the document is no longer decoded into the struct first, since there is no field there to fill and the attempt refused every scalar the schema allows.
type OneOfRootScalarBranch struct {
	Value isOneOfRootScalarBranch_Value `json:"-"`
}

// isOneOfRootScalarBranch_Value is a sealed interface for the Value field of OneOfRootScalarBranch.
// Exactly one variant must be set.
type isOneOfRootScalarBranch_Value interface {
	isOneOfRootScalarBranch_Value()
}

// OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0 wraps *OneOfRootScalarBranchValueOption0 as a Value variant.
type OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0 struct {
	OneOfRootScalarBranchValueOption0 *OneOfRootScalarBranchValueOption0
}

func (*OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0) isOneOfRootScalarBranch_Value() {}

// OneOfRootScalarBranch_String wraps string as a Value variant.
type OneOfRootScalarBranch_String struct {
	String string
}

func (*OneOfRootScalarBranch_String) isOneOfRootScalarBranch_Value() {}

// GetOneOfRootScalarBranchValueOption0 returns the OneOfRootScalarBranchValueOption0 variant value, or the zero value if not set.
func (o *OneOfRootScalarBranch) GetOneOfRootScalarBranchValueOption0() *OneOfRootScalarBranchValueOption0 {
	if o != nil {
		if v, ok := o.Value.(*OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0); ok {
			return v.OneOfRootScalarBranchValueOption0
		}
	}
	var zero *OneOfRootScalarBranchValueOption0
	return zero
}

// GetString returns the String variant value, or the zero value if not set.
func (o *OneOfRootScalarBranch) GetString() string {
	if o != nil {
		if v, ok := o.Value.(*OneOfRootScalarBranch_String); ok {
			return v.String
		}
	}
	var zero string
	return zero
}

func (o *OneOfRootScalarBranch) UnmarshalJSON(data []byte) error {
	o.Value = nil

	{
		// Top-level oneOf: the entire JSON object is the variant data.
		oneofData := json.RawMessage(data)
		// The union is the whole of this value, so it has no name of its own to
		// report under and its refusals are sentences about the value; whatever
		// contains it puts the path in front. Issue #289.
		oneofErrf := func(format string, args ...any) error {
			return jsonValueErrorf(format, args...)
		}
		// The schema behind this union excludes a JSON null, and the loop below
		// steps over one rather than judging it -- which at the top level, where
		// the union is the whole value and no parent check runs first, is an
		// acceptance. See OneOfDef.RejectNull.
		if string(oneofData) == "null" {
			return oneofErrf("null is not allowed")
		}
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isOneOfRootScalarBranch_Value
			var oneofStrictErr error

			// Try variant: OneOfRootScalarBranchValueOption0
			{
				if oneofHasRequiredFields(oneofData, "k") {
					var candidate *OneOfRootScalarBranchValueOption0
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.Value = &OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0{OneOfRootScalarBranchValueOption0: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0{OneOfRootScalarBranchValueOption0: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfRootScalarBranchValueOption0: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			// Try variant: String
			{
				var candidate string
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					o.Value = &OneOfRootScalarBranch_String{String: candidate}
					oneofMatched++
					// Nothing this branch says is left for a Validate to
					// carry, so matching it is satisfying it.
					oneofStrict++
					oneofStrictSel = &OneOfRootScalarBranch_String{String: candidate}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				// A branch is only put to a decode once the keys it selects on are
				// present, so a value that answers none of them leaves no branch
				// reason behind at all -- and the %w below then had nothing to
				// wrap and printed "%!w(<nil>)", which says nothing true about
				// anything. The refusal stands on its own in that case. Issue #289.
				if oneofLastErr == nil {
					return oneofErrf("no matching oneOf variant")
				}
				return oneofErrf("no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					o.Value = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return oneofErrf("no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return oneofErrf("multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}

	return nil
}
func (o OneOfRootScalarBranch) MarshalJSON() ([]byte, error) {
	// Top-level oneOf: marshal the selected variant directly as the root object.
	if o.Value != nil {
		switch v := o.Value.(type) {
		case *OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0:
			return json.Marshal(v.OneOfRootScalarBranchValueOption0)
		case *OneOfRootScalarBranch_String:
			return json.Marshal(v.String)
		}
	}
	return []byte("null"), nil
}

// Validate checks OneOfRootScalarBranch against its JSON Schema constraints.
func (o OneOfRootScalarBranch) Validate() error {
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := o.Value.(type) {
	case *OneOfRootScalarBranch_OneOfRootScalarBranchValueOption0:
		if _oneOfSel.OneOfRootScalarBranchValueOption0 != nil {
			if err := _oneOfSel.OneOfRootScalarBranchValueOption0.Validate(); err != nil {
				return err
			}
		}
	}
	return nil
}
