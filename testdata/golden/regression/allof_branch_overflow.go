// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"bytes"
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
	"math"
	"unicode/utf8"
)

type StrictBase struct {
	Base                 *int64                     `json:"base,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (s *StrictBase) UnmarshalJSON(data []byte) error {
	s.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type StrictBase")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"base",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias StrictBase
	aux := &struct {
		*Alias
		Base **jsonInteger `json:"base"`
	}{
		Alias: (*Alias)(s),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.Base != nil {
		_iv := *aux.Base
		s.Base = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"base",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"base": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if s.AdditionalProperties == nil {
				s.AdditionalProperties = make(map[string]json.RawMessage)
			}
			s.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (s StrictBase) MarshalJSON() ([]byte, error) {
	type Alias StrictBase
	aux := struct {
		Alias
	}{
		Alias: (Alias)(s),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range s.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks StrictBase against its JSON Schema constraints.
func (s StrictBase) Validate() error {
	if len(s.AdditionalProperties) > 0 {
		for k := range s.AdditionalProperties {
			return fmt.Errorf("additional property %q is not allowed", k)
		}
	}
	return nil
}

type AllOfBranchOverflowAdjacent struct {
	A                    *int64                     `json:"a,omitempty"`
	B                    *int64                     `json:"b,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowAdjacent) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a.PatternProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowAdjacent")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
			"b",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowAdjacent
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
		B **jsonInteger `json:"b"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	if aux.B != nil {
		_iv := *aux.B
		a.B = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
			"b",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
			"b": true,
		}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^x", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if a.PatternProperties == nil {
					a.PatternProperties = make(map[string]json.RawMessage)
				}
				a.PatternProperties[rawKey] = rawVal
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowAdjacent) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowAdjacent
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.PatternProperties {
		obj[k] = v
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowAdjacent against its JSON Schema constraints.
func (a AllOfBranchOverflowAdjacent) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(a.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^x", ecmaflags.Unicode),
		}
		for k, v := range a.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					b := bytes.TrimSpace(v)
					var jt string
					if len(b) == 0 {
						jt = "unknown"
					} else {
						switch b[0] {
						case '"':
							jt = "string"
						case '{':
							jt = "object"
						case '[':
							jt = "array"
						case 't', 'f':
							jt = "boolean"
						case 'n':
							jt = "null"
						default:
							jt = "number"
							// Draft 6 onward reads the value: 1.0 is an integer, so
							// the number is parsed rather than scanned. Every arm
							// below that accepts "number" also accepts "integer", so
							// widening this classification narrows nothing.
							var _ppNum float64
							if json.Unmarshal(b, &_ppNum) == nil &&
								!math.IsInf(_ppNum, 0) && _ppNum == math.Trunc(_ppNum) {
								jt = "integer"
							}
						}
					}
					if jt != "integer" {
						return fmt.Errorf("patternProperties %s: key %q value must be integer", "^x", k)
					}
				}
			}
		}
	}
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		_branchAcct0 := map[string]bool{
			"b": true,
		}
		_branchPat0 := []*ecma262.Regexp{
			ecma262.MustCompile("^x", ecmaflags.Unicode),
		}
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if _branchAcct0[k] {
				_bAcct = true
			}
			if !_bAcct {
				for _, _re := range _branchPat0 {
					if _re.MatchString(k) {
						_bAcct = true
						break
					}
				}
			}
			if !_bAcct {
				return fmt.Errorf("additionalProperties: property %q is not allowed", k)
			}
		}
	}
	return nil
}

type AllOfBranchOverflowBareBranch0Value int64

func (a *AllOfBranchOverflowBareBranch0Value) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowBareBranch0Value")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type AllOfBranchOverflowBareBranch0Value")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*a = AllOfBranchOverflowBareBranch0Value(_i)
		return nil
	}
	// Float notation with nothing after the point (1.0, 1e2), read exactly.
	// See jsonIntegerFromLiteral: float64 holds 2^63 and 2^63-1 as one number,
	// so it cannot be asked whether a literal is an int64.
	if _i, _iOK := jsonIntegerFromLiteral(_n.String()); _iOK {
		*a = AllOfBranchOverflowBareBranch0Value(_i)
		return nil
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks AllOfBranchOverflowBareBranch0Value against its JSON Schema constraints.
func (a AllOfBranchOverflowBareBranch0Value) Validate() error {
	if a < 5 {
		return fmt.Errorf("value: %v is less than minimum 5", a)
	}
	return nil
}

type AllOfBranchOverflowBare struct {
	A                    *int64                     `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowBare) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowBare")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowBare
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowBare) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowBare
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowBare against its JSON Schema constraints.
func (a AllOfBranchOverflowBare) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if !_bAcct {
				{
					// The sub-schema's own type answers for the value: the decode
					// enforces its shape and the Validate enforces the rest.
					var _bVal AllOfBranchOverflowBareBranch0Value
					if _bErr := json.Unmarshal(_bv, &_bVal); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
					if _bErr := _bVal.Validate(); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
				}
			}
		}
	}
	return nil
}

type AllOfBranchOverflowBranchUnevaluatedBranch0Value string

func (a *AllOfBranchOverflowBranchUnevaluatedBranch0Value) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowBranchUnevaluatedBranch0Value")
	}
	type Alias AllOfBranchOverflowBranchUnevaluatedBranch0Value
	return json.Unmarshal(data, (*Alias)(a))
}

// Validate checks AllOfBranchOverflowBranchUnevaluatedBranch0Value against its JSON Schema constraints.
func (a AllOfBranchOverflowBranchUnevaluatedBranch0Value) Validate() error {
	if utf8.RuneCountInString(string(a)) < 3 {
		return fmt.Errorf("value: length %d is less than minimum 3", utf8.RuneCountInString(string(a)))
	}
	return nil
}

type AllOfBranchOverflowBranchUnevaluated struct {
	A                    *int64                     `json:"a,omitempty"`
	B                    *int64                     `json:"b,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowBranchUnevaluated) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowBranchUnevaluated")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
			"b",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowBranchUnevaluated
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
		B **jsonInteger `json:"b"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	if aux.B != nil {
		_iv := *aux.B
		a.B = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
			"b",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
			"b": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowBranchUnevaluated) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowBranchUnevaluated
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowBranchUnevaluated against its JSON Schema constraints.
func (a AllOfBranchOverflowBranchUnevaluated) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		_branchAcct0 := map[string]bool{
			"b": true,
		}
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if _branchAcct0[k] {
				_bAcct = true
			}
			if !_bAcct {
				{
					// The sub-schema's own type answers for the value: the decode
					// enforces its shape and the Validate enforces the rest.
					var _bVal AllOfBranchOverflowBranchUnevaluatedBranch0Value
					if _bErr := json.Unmarshal(_bv, &_bVal); _bErr != nil {
						return fmt.Errorf("unevaluatedProperties: property %q: %w", k, _bErr)
					}
					if _bErr := _bVal.Validate(); _bErr != nil {
						return fmt.Errorf("unevaluatedProperties: property %q: %w", k, _bErr)
					}
				}
			}
		}
	}
	return nil
}

type AllOfBranchOverflowBranchUnevaluatedFalse struct {
	A                    *int64                     `json:"a,omitempty"`
	B                    *int64                     `json:"b,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowBranchUnevaluatedFalse) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowBranchUnevaluatedFalse")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
			"b",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowBranchUnevaluatedFalse
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
		B **jsonInteger `json:"b"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	if aux.B != nil {
		_iv := *aux.B
		a.B = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
			"b",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
			"b": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowBranchUnevaluatedFalse) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowBranchUnevaluatedFalse
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowBranchUnevaluatedFalse against its JSON Schema constraints.
func (a AllOfBranchOverflowBranchUnevaluatedFalse) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		_branchAcct0 := map[string]bool{
			"b": true,
		}
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if _branchAcct0[k] {
				_bAcct = true
			}
			if !_bAcct {
				return fmt.Errorf("unevaluatedProperties: property %q is not allowed", k)
			}
		}
	}
	return nil
}

type AllOfBranchOverflowForbid struct {
	A                    *int64                     `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowForbid) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowForbid")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowForbid
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowForbid) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowForbid
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowForbid against its JSON Schema constraints.
func (a AllOfBranchOverflowForbid) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if !_bAcct {
				return fmt.Errorf("additionalProperties: property %q is not allowed", k)
			}
		}
	}
	return nil
}

type AllOfBranchOverflowNestedAllOfBranch0Value int64

func (a *AllOfBranchOverflowNestedAllOfBranch0Value) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowNestedAllOfBranch0Value")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type AllOfBranchOverflowNestedAllOfBranch0Value")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*a = AllOfBranchOverflowNestedAllOfBranch0Value(_i)
		return nil
	}
	// Float notation with nothing after the point (1.0, 1e2), read exactly.
	// See jsonIntegerFromLiteral: float64 holds 2^63 and 2^63-1 as one number,
	// so it cannot be asked whether a literal is an int64.
	if _i, _iOK := jsonIntegerFromLiteral(_n.String()); _iOK {
		*a = AllOfBranchOverflowNestedAllOfBranch0Value(_i)
		return nil
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks AllOfBranchOverflowNestedAllOfBranch0Value against its JSON Schema constraints.
func (a AllOfBranchOverflowNestedAllOfBranch0Value) Validate() error {
	if a < 5 {
		return fmt.Errorf("value: %v is less than minimum 5", a)
	}
	return nil
}

type AllOfBranchOverflowNestedAllOf struct {
	A                    *int64                     `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowNestedAllOf) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowNestedAllOf")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowNestedAllOf
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowNestedAllOf) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowNestedAllOf
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowNestedAllOf against its JSON Schema constraints.
func (a AllOfBranchOverflowNestedAllOf) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if !_bAcct {
				{
					// The sub-schema's own type answers for the value: the decode
					// enforces its shape and the Validate enforces the rest.
					var _bVal AllOfBranchOverflowNestedAllOfBranch0Value
					if _bErr := json.Unmarshal(_bv, &_bVal); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
					if _bErr := _bVal.Validate(); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
				}
			}
		}
	}
	return nil
}

type AllOfBranchOverflowObjectValueA struct {
	N                    *int64                     `json:"n,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
}

func (a *AllOfBranchOverflowObjectValueA) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowObjectValueA")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"n",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowObjectValueA
	aux := &struct {
		*Alias
		N **jsonInteger `json:"n"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.N != nil {
		_iv := *aux.N
		a.N = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"n",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"n": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowObjectValueA) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowObjectValueA
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowObjectValueA against its JSON Schema constraints.
func (a AllOfBranchOverflowObjectValueA) Validate() error {
	return nil
}

type AllOfBranchOverflowObjectValueBranch0Value struct {
	N                    int64                      `json:"n"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (a *AllOfBranchOverflowObjectValueBranch0Value) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowObjectValueBranch0Value")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"n",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowObjectValueBranch0Value
	aux := &struct {
		*Alias
		N *jsonInteger `json:"n"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.N != nil {
		_iv := *aux.N
		a.N = int64(_iv)
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"n",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"n": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowObjectValueBranch0Value) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowObjectValueBranch0Value
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowObjectValueBranch0Value against its JSON Schema constraints.
func (a AllOfBranchOverflowObjectValueBranch0Value) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if a._jsonKeys != nil {
		for _, _req := range []string{"n"} {
			if !a._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	if a.N < 5 {
		return fmt.Errorf("n: value %v is less than minimum 5", a.N)
	}
	return nil
}

type AllOfBranchOverflowObjectValue struct {
	A                    *AllOfBranchOverflowObjectValueA `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage       `json:"-"`
	_jsonKeys            map[string]bool                  // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage       // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowObjectValue) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowObjectValue")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowObjectValue
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowObjectValue) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowObjectValue
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowObjectValue against its JSON Schema constraints.
func (a AllOfBranchOverflowObjectValue) Validate() error {
	if a.A != nil {
		if err := a.A.Validate(); err != nil {
			return fmt.Errorf("a.%w", err)
		}
	}
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if !_bAcct {
				{
					// The sub-schema's own type answers for the value: the decode
					// enforces its shape and the Validate enforces the rest.
					var _bVal AllOfBranchOverflowObjectValueBranch0Value
					if _bErr := json.Unmarshal(_bv, &_bVal); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
					if _bErr := _bVal.Validate(); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
				}
			}
		}
	}
	return nil
}

type AllOfBranchOverflowOwnAdditional struct {
	A                    int64            `json:"a"`
	AdditionalProperties map[string]int64 `json:"-"`
	_jsonKeys            map[string]bool  // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (a *AllOfBranchOverflowOwnAdditional) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowOwnAdditional")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowOwnAdditional
	aux := &struct {
		*Alias
		A *jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = int64(_iv)
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if string(rawVal) == "null" {
				return fmt.Errorf("additionalProperties[%q]: null is not allowed", rawKey)
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]int64)
			}
			var _iv jsonInteger
			if err := json.Unmarshal(rawVal, &_iv); err != nil {
				return fmt.Errorf("additionalProperties[%s]: %w", rawKey, err)
			}
			a.AdditionalProperties[rawKey] = int64(_iv)
		}
	}

	return nil
}
func (a AllOfBranchOverflowOwnAdditional) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowOwnAdditional
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		raw, err := json.Marshal(v)
		if err != nil {
			return nil, fmt.Errorf("marshaling additional property %q: %w", k, err)
		}
		obj[k] = raw
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowOwnAdditional against its JSON Schema constraints.
func (a AllOfBranchOverflowOwnAdditional) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if a._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !a._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	for _k0, _e0 := range a.AdditionalProperties {
		if float64(_e0) < 5 {
			return fmt.Errorf("additionalProperties[%q]: value %v is less than minimum 5", _k0, _e0)
		}
	}
	return nil
}

type AllOfBranchOverflowPlain struct {
	A                    int64                      `json:"a"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (a *AllOfBranchOverflowPlain) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowPlain")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowPlain
	aux := &struct {
		*Alias
		A *jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = int64(_iv)
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowPlain) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowPlain
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowPlain against its JSON Schema constraints.
func (a AllOfBranchOverflowPlain) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if a._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !a._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type AllOfBranchOverflowSoleBranch struct {
	AdditionalProperties map[string]string `json:"-"`
}

func (a *AllOfBranchOverflowSoleBranch) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowSoleBranch")
	}
	type Alias AllOfBranchOverflowSoleBranch
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if string(rawVal) == "null" {
				return fmt.Errorf("additionalProperties[%q]: null is not allowed", rawKey)
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]string)
			}
			var val string
			if err := json.Unmarshal(rawVal, &val); err != nil {
				return fmt.Errorf("additionalProperties[%s]: %w", rawKey, err)
			}
			a.AdditionalProperties[rawKey] = val
		}
	}

	return nil
}
func (a AllOfBranchOverflowSoleBranch) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowSoleBranch
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		raw, err := json.Marshal(v)
		if err != nil {
			return nil, fmt.Errorf("marshaling additional property %q: %w", k, err)
		}
		obj[k] = raw
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowSoleBranch against its JSON Schema constraints.
func (a AllOfBranchOverflowSoleBranch) Validate() error {
	for _k0, _e0 := range a.AdditionalProperties {
		if utf8.RuneCountInString(string(_e0)) < 2 {
			return fmt.Errorf("additionalProperties[%q]: length %d is less than minimum 2", _k0, utf8.RuneCountInString(string(_e0)))
		}
	}
	return nil
}

type AllOfBranchOverflowTwoBranchesBranch0Value int64

func (a *AllOfBranchOverflowTwoBranchesBranch0Value) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowTwoBranchesBranch0Value")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type AllOfBranchOverflowTwoBranchesBranch0Value")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*a = AllOfBranchOverflowTwoBranchesBranch0Value(_i)
		return nil
	}
	// Float notation with nothing after the point (1.0, 1e2), read exactly.
	// See jsonIntegerFromLiteral: float64 holds 2^63 and 2^63-1 as one number,
	// so it cannot be asked whether a literal is an int64.
	if _i, _iOK := jsonIntegerFromLiteral(_n.String()); _iOK {
		*a = AllOfBranchOverflowTwoBranchesBranch0Value(_i)
		return nil
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks AllOfBranchOverflowTwoBranchesBranch0Value against its JSON Schema constraints.
func (a AllOfBranchOverflowTwoBranchesBranch0Value) Validate() error {
	if a < 5 {
		return fmt.Errorf("value: %v is less than minimum 5", a)
	}
	return nil
}

type AllOfBranchOverflowTwoBranchesBranch1Value int64

func (a *AllOfBranchOverflowTwoBranchesBranch1Value) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowTwoBranchesBranch1Value")
	}
	// Use json.Number to accept 1.0 as a valid integer (zero fractional part).
	// Reject JSON strings (e.g., "1") — json.Number would accept them since it is a string type.
	if len(data) > 0 && data[0] == '"' {
		return fmt.Errorf("cannot unmarshal string into Go value of type AllOfBranchOverflowTwoBranchesBranch1Value")
	}
	var _n json.Number
	if _err := json.Unmarshal(data, &_n); _err != nil {
		return _err
	}
	if _i, _iErr := _n.Int64(); _iErr == nil {
		*a = AllOfBranchOverflowTwoBranchesBranch1Value(_i)
		return nil
	}
	// Float notation with nothing after the point (1.0, 1e2), read exactly.
	// See jsonIntegerFromLiteral: float64 holds 2^63 and 2^63-1 as one number,
	// so it cannot be asked whether a literal is an int64.
	if _i, _iOK := jsonIntegerFromLiteral(_n.String()); _iOK {
		*a = AllOfBranchOverflowTwoBranchesBranch1Value(_i)
		return nil
	}
	return fmt.Errorf("value %s cannot be represented as int64", _n.String())
}

// Validate checks AllOfBranchOverflowTwoBranchesBranch1Value against its JSON Schema constraints.
func (a AllOfBranchOverflowTwoBranchesBranch1Value) Validate() error {
	if a > 9 {
		return fmt.Errorf("value: %v exceeds maximum 9", a)
	}
	return nil
}

type AllOfBranchOverflowTwoBranches struct {
	A                    *int64                     `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowTwoBranches) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowTwoBranches")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowTwoBranches
	aux := &struct {
		*Alias
		A **jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		a.A = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowTwoBranches) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowTwoBranches
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowTwoBranches against its JSON Schema constraints.
func (a AllOfBranchOverflowTwoBranches) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if !_bAcct {
				{
					// The sub-schema's own type answers for the value: the decode
					// enforces its shape and the Validate enforces the rest.
					var _bVal AllOfBranchOverflowTwoBranchesBranch0Value
					if _bErr := json.Unmarshal(_bv, &_bVal); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
					if _bErr := _bVal.Validate(); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
				}
			}
		}
	}
	{
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if !_bAcct {
				{
					// The sub-schema's own type answers for the value: the decode
					// enforces its shape and the Validate enforces the rest.
					var _bVal AllOfBranchOverflowTwoBranchesBranch1Value
					if _bErr := json.Unmarshal(_bv, &_bVal); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
					if _bErr := _bVal.Validate(); _bErr != nil {
						return fmt.Errorf("additionalProperties: property %q: %w", k, _bErr)
					}
				}
			}
		}
	}
	return nil
}

type AllOfBranchOverflowViaRef struct {
	Base                 *int64                     `json:"base,omitempty"`
	Other                *int64                     `json:"other,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AllOfBranchOverflowViaRef) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflowViaRef")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"base",
			"other",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflowViaRef
	aux := &struct {
		*Alias
		Base  **jsonInteger `json:"base"`
		Other **jsonInteger `json:"other"`
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.Base != nil {
		_iv := *aux.Base
		a.Base = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	if aux.Other != nil {
		_iv := *aux.Other
		a.Other = jsonIntegerPtr(_iv, func(_ix0 jsonInteger) int64 { return int64(_ix0) })
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"base",
			"other",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"base":  true,
			"other": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflowViaRef) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflowViaRef
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflowViaRef against its JSON Schema constraints.
func (a AllOfBranchOverflowViaRef) Validate() error {
	// Per-branch overflow: an allOf/anyOf branch's own additionalProperties or
	// unevaluatedProperties sees every key of the instance except the ones that
	// branch itself accounts for.
	{
		_branchAcct0 := map[string]bool{
			"base": true,
		}
		for k, _bv := range a._jsonRawProps {
			_ = _bv
			_bAcct := false
			if _branchAcct0[k] {
				_bAcct = true
			}
			if !_bAcct {
				return fmt.Errorf("additionalProperties: property %q is not allowed", k)
			}
		}
	}
	return nil
}

type AllOfBranchOverflow struct {
	Adjacent               *AllOfBranchOverflowAdjacent               `json:"adjacent,omitempty"`
	Bare                   *AllOfBranchOverflowBare                   `json:"bare,omitempty"`
	BranchUnevaluated      *AllOfBranchOverflowBranchUnevaluated      `json:"branchUnevaluated,omitempty"`
	BranchUnevaluatedFalse *AllOfBranchOverflowBranchUnevaluatedFalse `json:"branchUnevaluatedFalse,omitempty"`
	Forbid                 *AllOfBranchOverflowForbid                 `json:"forbid,omitempty"`
	NestedAllOf            *AllOfBranchOverflowNestedAllOf            `json:"nestedAllOf,omitempty"`
	ObjectValue            *AllOfBranchOverflowObjectValue            `json:"objectValue,omitempty"`
	OwnAdditional          *AllOfBranchOverflowOwnAdditional          `json:"ownAdditional,omitempty"`
	Plain                  *AllOfBranchOverflowPlain                  `json:"plain,omitempty"`
	SoleBranch             *AllOfBranchOverflowSoleBranch             `json:"soleBranch,omitempty"`
	TwoBranches            *AllOfBranchOverflowTwoBranches            `json:"twoBranches,omitempty"`
	ViaRef                 *AllOfBranchOverflowViaRef                 `json:"viaRef,omitempty"`
	AdditionalProperties   map[string]json.RawMessage                 `json:"-"`
}

func (a *AllOfBranchOverflow) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AllOfBranchOverflow")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"adjacent",
			"bare",
			"branchUnevaluated",
			"branchUnevaluatedFalse",
			"forbid",
			"nestedAllOf",
			"objectValue",
			"ownAdditional",
			"plain",
			"soleBranch",
			"twoBranches",
			"viaRef",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias AllOfBranchOverflow
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"adjacent",
			"bare",
			"branchUnevaluated",
			"branchUnevaluatedFalse",
			"forbid",
			"nestedAllOf",
			"objectValue",
			"ownAdditional",
			"plain",
			"soleBranch",
			"twoBranches",
			"viaRef",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		knownFields := map[string]bool{
			"adjacent":               true,
			"bare":                   true,
			"branchUnevaluated":      true,
			"branchUnevaluatedFalse": true,
			"forbid":                 true,
			"nestedAllOf":            true,
			"objectValue":            true,
			"ownAdditional":          true,
			"plain":                  true,
			"soleBranch":             true,
			"twoBranches":            true,
			"viaRef":                 true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AllOfBranchOverflow) MarshalJSON() ([]byte, error) {
	type Alias AllOfBranchOverflow
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AllOfBranchOverflow against its JSON Schema constraints.
func (a AllOfBranchOverflow) Validate() error {
	if a.Adjacent != nil {
		if err := a.Adjacent.Validate(); err != nil {
			return fmt.Errorf("adjacent.%w", err)
		}
	}
	if a.Bare != nil {
		if err := a.Bare.Validate(); err != nil {
			return fmt.Errorf("bare.%w", err)
		}
	}
	if a.BranchUnevaluated != nil {
		if err := a.BranchUnevaluated.Validate(); err != nil {
			return fmt.Errorf("branchUnevaluated.%w", err)
		}
	}
	if a.BranchUnevaluatedFalse != nil {
		if err := a.BranchUnevaluatedFalse.Validate(); err != nil {
			return fmt.Errorf("branchUnevaluatedFalse.%w", err)
		}
	}
	if a.Forbid != nil {
		if err := a.Forbid.Validate(); err != nil {
			return fmt.Errorf("forbid.%w", err)
		}
	}
	if a.NestedAllOf != nil {
		if err := a.NestedAllOf.Validate(); err != nil {
			return fmt.Errorf("nestedAllOf.%w", err)
		}
	}
	if a.ObjectValue != nil {
		if err := a.ObjectValue.Validate(); err != nil {
			return fmt.Errorf("objectValue.%w", err)
		}
	}
	if a.OwnAdditional != nil {
		if err := a.OwnAdditional.Validate(); err != nil {
			return fmt.Errorf("ownAdditional.%w", err)
		}
	}
	if a.Plain != nil {
		if err := a.Plain.Validate(); err != nil {
			return fmt.Errorf("plain.%w", err)
		}
	}
	if a.SoleBranch != nil {
		if err := a.SoleBranch.Validate(); err != nil {
			return fmt.Errorf("soleBranch.%w", err)
		}
	}
	if a.TwoBranches != nil {
		if err := a.TwoBranches.Validate(); err != nil {
			return fmt.Errorf("twoBranches.%w", err)
		}
	}
	if a.ViaRef != nil {
		if err := a.ViaRef.Validate(); err != nil {
			return fmt.Errorf("viaRef.%w", err)
		}
	}
	return nil
}
