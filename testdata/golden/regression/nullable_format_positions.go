// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
	"math"
)

type NullableV4String string

func (n *NullableV4String) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableV4String
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableV4String against its JSON Schema constraints.
func (n NullableV4String) Validate() error {
	return nil
}

// NullableV4 accepts any JSON value and validates that it is one of the allowed types.
type NullableV4 struct {
	_raw json.RawMessage
}

func (n *NullableV4) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableV4) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableV4) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableV4) IsZero() bool { return len(n._raw) == 0 }

func (n NullableV4) String() string { return string(n._raw) }

// Validate checks NullableV4 against its JSON Schema type constraint.
func (n NullableV4) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableV4String
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type ChainInnerString string

func (c *ChainInnerString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ChainInnerString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(c)))
}

// Validate checks ChainInnerString against its JSON Schema constraints.
func (c ChainInnerString) Validate() error {
	return nil
}

// ChainInner accepts any JSON value and validates that it is one of the allowed types.
type ChainInner struct {
	_raw json.RawMessage
}

func (c *ChainInner) UnmarshalJSON(data []byte) error {
	c._raw = append(c._raw[:0], data...)
	return nil
}

func (c ChainInner) MarshalJSON() ([]byte, error) {
	if len(c._raw) == 0 {
		return []byte("null"), nil
	}
	return c._raw, nil
}

func (c ChainInner) Raw() json.RawMessage { return c._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (c ChainInner) IsZero() bool { return len(c._raw) == 0 }

func (c ChainInner) String() string { return string(c._raw) }

// Validate checks ChainInner against its JSON Schema type constraint.
func (c ChainInner) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(c._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(c._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv ChainInnerString
			if _err := json.Unmarshal(c._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type ChainOuterString string

func (c *ChainOuterString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias ChainOuterString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(c)))
}

// Validate checks ChainOuterString against its JSON Schema constraints.
func (c ChainOuterString) Validate() error {
	return nil
}

// ChainOuter accepts any JSON value and validates that it is one of the allowed types.
type ChainOuter struct {
	_raw json.RawMessage
}

func (c *ChainOuter) UnmarshalJSON(data []byte) error {
	c._raw = append(c._raw[:0], data...)
	return nil
}

func (c ChainOuter) MarshalJSON() ([]byte, error) {
	if len(c._raw) == 0 {
		return []byte("null"), nil
	}
	return c._raw, nil
}

func (c ChainOuter) Raw() json.RawMessage { return c._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (c ChainOuter) IsZero() bool { return len(c._raw) == 0 }

func (c ChainOuter) String() string { return string(c._raw) }

// Validate checks ChainOuter against its JSON Schema type constraint.
func (c ChainOuter) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(c._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(c._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv ChainOuterString
			if _err := json.Unmarshal(c._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsBranchOption0String string

func (n *NullableFormatPositionsBranchOption0String) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsBranchOption0String
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsBranchOption0String against its JSON Schema constraints.
func (n NullableFormatPositionsBranchOption0String) Validate() error {
	return nil
}

// NullableFormatPositionsBranchOption0 accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsBranchOption0 struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsBranchOption0) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsBranchOption0) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsBranchOption0) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsBranchOption0) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsBranchOption0) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsBranchOption0 against its JSON Schema type constraint.
func (n NullableFormatPositionsBranchOption0) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsBranchOption0String
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsBucketsPattern0String string

func (n *NullableFormatPositionsBucketsPattern0String) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsBucketsPattern0String
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsBucketsPattern0String against its JSON Schema constraints.
func (n NullableFormatPositionsBucketsPattern0String) Validate() error {
	return nil
}

// NullableFormatPositionsBucketsPattern0 accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsBucketsPattern0 struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsBucketsPattern0) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsBucketsPattern0) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsBucketsPattern0) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsBucketsPattern0) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsBucketsPattern0) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsBucketsPattern0 against its JSON Schema type constraint.
func (n NullableFormatPositionsBucketsPattern0) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsBucketsPattern0String
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsBuckets struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (n *NullableFormatPositionsBuckets) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n.PatternProperties = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsBuckets
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return jsonDecodeRefusal(err)
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^p", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if n.PatternProperties == nil {
					n.PatternProperties = make(map[string]json.RawMessage)
				}
				n.PatternProperties[rawKey] = rawVal
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NullableFormatPositionsBuckets) MarshalJSON() ([]byte, error) {
	type Alias NullableFormatPositionsBuckets
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range n.PatternProperties {
		obj[k] = v
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NullableFormatPositionsBuckets against its JSON Schema constraints.
func (n NullableFormatPositionsBuckets) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(n.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^p", ecmaflags.Unicode),
		}
		for k, v := range n.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv NullableFormatPositionsBucketsPattern0
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^p", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^p", k, _vErr)
					}
				}
			}
		}
	}
	return nil
}

type NullableFormatPositionsInlineString string

func (n *NullableFormatPositionsInlineString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsInlineString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsInlineString against its JSON Schema constraints.
func (n NullableFormatPositionsInlineString) Validate() error {
	return nil
}

// NullableFormatPositionsInline accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsInline struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsInline) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsInline) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsInline) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsInline) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsInline) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsInline against its JSON Schema type constraint.
func (n NullableFormatPositionsInline) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsInlineString
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsListItemString string

func (n *NullableFormatPositionsListItemString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsListItemString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsListItemString against its JSON Schema constraints.
func (n NullableFormatPositionsListItemString) Validate() error {
	return nil
}

// NullableFormatPositionsListItem accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsListItem struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsListItem) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsListItem) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsListItem) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsListItem) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsListItem) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsListItem against its JSON Schema type constraint.
func (n NullableFormatPositionsListItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsListItemString
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsMailString string

func (n *NullableFormatPositionsMailString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsMailString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsMailString against its JSON Schema constraints.
func (n NullableFormatPositionsMailString) Validate() error {
	return nil
}

// NullableFormatPositionsMail accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsMail struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsMail) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsMail) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsMail) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsMail) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsMail) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsMail against its JSON Schema type constraint.
func (n NullableFormatPositionsMail) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsMailString
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsMapValueString string

func (n *NullableFormatPositionsMapValueString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsMapValueString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsMapValueString against its JSON Schema constraints.
func (n NullableFormatPositionsMapValueString) Validate() error {
	return nil
}

// NullableFormatPositionsMapValue accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsMapValue struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsMapValue) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsMapValue) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsMapValue) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsMapValue) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsMapValue) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsMapValue against its JSON Schema type constraint.
func (n NullableFormatPositionsMapValue) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsMapValueString
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsStampString string

func (n *NullableFormatPositionsStampString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsStampString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsStampString against its JSON Schema constraints.
func (n NullableFormatPositionsStampString) Validate() error {
	return nil
}

// NullableFormatPositionsStamp accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsStamp struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsStamp) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsStamp) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsStamp) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsStamp) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsStamp) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsStamp against its JSON Schema type constraint.
func (n NullableFormatPositionsStamp) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsStampString
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsWrappedString string

func (n *NullableFormatPositionsWrappedString) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsWrappedString
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsWrappedString against its JSON Schema constraints.
func (n NullableFormatPositionsWrappedString) Validate() error {
	return nil
}

// NullableFormatPositionsWrapped accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsWrapped struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsWrapped) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsWrapped) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsWrapped) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsWrapped) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsWrapped) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsWrapped against its JSON Schema type constraint.
func (n NullableFormatPositionsWrapped) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsWrappedString
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

type NullableFormatPositionsTupleItem0String string

func (n *NullableFormatPositionsTupleItem0String) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias NullableFormatPositionsTupleItem0String
	return jsonDecodeRefusal(json.Unmarshal(data, (*Alias)(n)))
}

// Validate checks NullableFormatPositionsTupleItem0String against its JSON Schema constraints.
func (n NullableFormatPositionsTupleItem0String) Validate() error {
	return nil
}

// NullableFormatPositionsTupleItem0 accepts any JSON value and validates that it is one of the allowed types.
type NullableFormatPositionsTupleItem0 struct {
	_raw json.RawMessage
}

func (n *NullableFormatPositionsTupleItem0) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NullableFormatPositionsTupleItem0) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NullableFormatPositionsTupleItem0) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NullableFormatPositionsTupleItem0) IsZero() bool { return len(n._raw) == 0 }

func (n NullableFormatPositionsTupleItem0) String() string { return string(n._raw) }

// Validate checks NullableFormatPositionsTupleItem0 against its JSON Schema type constraint.
func (n NullableFormatPositionsTupleItem0) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(n._raw, &_v); _err != nil {
		return fmt.Errorf("type: cannot decode value: %w", _err)
	}
	_typeBranchValid := false
	{
		_branchMatches := true
		{
			// The alternative is a reference; the value matches it when it
			// decodes into the referenced type and that type accepts it.
			var _bv NullableFormatPositionsTupleItem0String
			if _err := json.Unmarshal(n._raw, &_bv); _err != nil {
				_branchMatches = false
			} else if _bvv, _ok := any(&_bv).(interface{ Validate() error }); _ok {
				if _err := _bvv.Validate(); _err != nil {
					_branchMatches = false
				}
			}
		}
		if _branchMatches {
			_typeBranchValid = true
		}
	}
	if _typeBranchValid {
		return nil
	}
	if _v == nil {
		return nil
	}
	switch _tv := _v.(type) {
	case float64:
		_ = _tv
		return fmt.Errorf("type: number is not allowed")
	case string:
		_ = _tv
		return fmt.Errorf("type: string is not allowed")
	case bool:
		_ = _tv
		return fmt.Errorf("type: boolean is not allowed")
	case []any:
		_ = _tv
		return fmt.Errorf("type: array is not allowed")
	case map[string]any:
		_ = _tv
		return fmt.Errorf("type: object is not allowed")
	default:
		return fmt.Errorf("type: unexpected value type %T", _v)
	}
}

// NullableFormatPositions - The nullable spelling of a formatted string, in every position it can be written. Each resolved to *string, losing the Go type the non-nullable spelling gets; the named form became `type X *string`, a pointer underlying type Go forbids methods on, so it carried no Validate and the format was enforced nowhere.
type NullableFormatPositions struct {
	Branch               isNullableFormatPositions_Branch           `json:"-"`
	Buckets              *NullableFormatPositionsBuckets            `json:"buckets,omitempty"`
	Map                  map[string]NullableFormatPositionsMapValue `json:"map,omitzero"`
	AdditionalProperties map[string]json.RawMessage                 `json:"-"`
	_jsonKeys            map[string]bool                            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool                            // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
	Chain                ChainOuter                                 `json:"chain,omitzero"`
	Inline               NullableFormatPositionsInline              `json:"inline,omitzero"`
	List                 []NullableFormatPositionsListItem          `json:"list,omitzero"`
	Mail                 NullableFormatPositionsMail                `json:"mail,omitzero"`
	Ref                  NullableV4                                 `json:"ref,omitzero"`
	Stamp                NullableFormatPositionsStamp               `json:"stamp,omitzero"`
	Tuple                []any                                      `json:"tuple,omitzero"`
	Wrapped              NullableFormatPositionsWrapped             `json:"wrapped,omitzero"`
}

// isNullableFormatPositions_Branch is a sealed interface for the Branch field of NullableFormatPositions.
// Exactly one variant must be set.
type isNullableFormatPositions_Branch interface {
	isNullableFormatPositions_Branch()
}

// NullableFormatPositions_NullableFormatPositionsBranchOption0 wraps NullableFormatPositionsBranchOption0 as a Branch variant.
type NullableFormatPositions_NullableFormatPositionsBranchOption0 struct {
	NullableFormatPositionsBranchOption0 NullableFormatPositionsBranchOption0
}

func (*NullableFormatPositions_NullableFormatPositionsBranchOption0) isNullableFormatPositions_Branch() {
}

// NullableFormatPositions_Integer wraps int64 as a Branch variant.
type NullableFormatPositions_Integer struct {
	Integer int64
}

func (*NullableFormatPositions_Integer) isNullableFormatPositions_Branch() {}

// GetNullableFormatPositionsBranchOption0 returns the NullableFormatPositionsBranchOption0 variant value, or the zero value if not set.
func (n *NullableFormatPositions) GetNullableFormatPositionsBranchOption0() NullableFormatPositionsBranchOption0 {
	if n != nil {
		if v, ok := n.Branch.(*NullableFormatPositions_NullableFormatPositionsBranchOption0); ok {
			return v.NullableFormatPositionsBranchOption0
		}
	}
	var zero NullableFormatPositionsBranchOption0
	return zero
}

// GetInteger returns the Integer variant value, or the zero value if not set.
func (n *NullableFormatPositions) GetInteger() int64 {
	if n != nil {
		if v, ok := n.Branch.(*NullableFormatPositions_Integer); ok {
			return v.Integer
		}
	}
	var zero int64
	return zero
}

func (n *NullableFormatPositions) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	n._jsonNulls = nil
	n.Branch = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"buckets",
			"chain",
			"inline",
			"list",
			"mail",
			"map",
			"ref",
			"stamp",
			"tuple",
			"wrapped",
			"branch",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias NullableFormatPositions
	aux := &struct {
		*Alias
		Branch json.RawMessage `json:"branch"`
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{name: "buckets", decode: jsonDecodeValue[*NullableFormatPositionsBuckets]},
			{name: "chain", decode: jsonDecodeValue[ChainOuter]},
			{name: "inline", decode: jsonDecodeValue[NullableFormatPositionsInline]},
			{name: "list", decode: jsonDecodeItems(jsonDecodeValue[NullableFormatPositionsListItem])},
			{name: "mail", decode: jsonDecodeValue[NullableFormatPositionsMail]},
			{name: "map", decode: jsonDecodeValues(jsonDecodeValue[NullableFormatPositionsMapValue])},
			{name: "ref", decode: jsonDecodeValue[NullableV4]},
			{name: "stamp", decode: jsonDecodeValue[NullableFormatPositionsStamp]},
			{name: "tuple", decode: jsonDecodeItems(jsonDecodeValue[any])},
			{name: "wrapped", decode: jsonDecodeValue[NullableFormatPositionsWrapped]},
		})
	}

	{
		oneofData := aux.Branch
		// Every refusal this union raises is a sentence about the value the union
		// holds, and the property that reaches it goes in front of that sentence
		// by the rule every other message is joined by (see jsonPathError).
		//
		// What used to go in front was a Go type and a Go field pasted in by hand:
		// NullableFormatPositions.Branch
		// A caller cannot find either in the document they sent, and at any depth
		// but the first the pair does not even resemble the path. Issue #289.
		oneofErrf := func(format string, args ...any) error {
			return jsonPathf(jsonValueErrorf(format, args...), "%s", "branch")
		}
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isNullableFormatPositions_Branch
			var oneofStrictErr error

			// Try variant: NullableFormatPositionsBranchOption0
			{
				var candidate NullableFormatPositionsBranchOption0
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					n.Branch = &NullableFormatPositions_NullableFormatPositionsBranchOption0{NullableFormatPositionsBranchOption0: candidate}
					oneofMatched++
					if _vErr := candidate.Validate(); _vErr == nil {
						oneofStrict++
						oneofStrictSel = &NullableFormatPositions_NullableFormatPositionsBranchOption0{NullableFormatPositionsBranchOption0: candidate}
					} else {
						oneofStrictErr = fmt.Errorf("variant NullableFormatPositionsBranchOption0: %w", _vErr)
					}
				} else {
					oneofLastErr = err
				}
			}

			// Try variant: Integer
			{
				var candidate int64
				// Selection gates on whether the branch decodes, so an
				// integer branch has to read a number the same way the rest
				// of the file does -- otherwise a document this branch
				// accepts is reported as matching no branch at all.
				var _iv jsonInteger
				_ivErr := json.Unmarshal(oneofData, &_iv)
				if _ivErr == nil {
					candidate = int64(_iv)
				}
				if err := _ivErr; err == nil {
					n.Branch = &NullableFormatPositions_Integer{Integer: candidate}
					oneofMatched++
					// Nothing this branch says is left for a Validate to
					// carry, so matching it is satisfying it.
					oneofStrict++
					oneofStrictSel = &NullableFormatPositions_Integer{Integer: candidate}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				// A branch is only put to a decode once the keys it selects on are
				// present, so a value that answers none of them leaves no branch
				// reason behind at all -- and the %w below then had nothing to
				// wrap and printed "%!w(<nil>)", which says nothing true about
				// anything. The refusal stands on its own in that case. Issue #289.
				if oneofLastErr == nil {
					return oneofErrf("no matching oneOf variant")
				}
				return oneofErrf("no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					n.Branch = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return oneofErrf("no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return oneofErrf("multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"buckets",
			"list",
			"map",
			"tuple",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"branch",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if n._jsonNulls == nil {
					n._jsonNulls = make(map[string]bool, 1)
				}
				n._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"buckets": true,
			"chain":   true,
			"inline":  true,
			"list":    true,
			"mail":    true,
			"map":     true,
			"ref":     true,
			"stamp":   true,
			"tuple":   true,
			"wrapped": true,
			"branch":  true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NullableFormatPositions) MarshalJSON() ([]byte, error) {
	type Alias NullableFormatPositions
	aux := struct {
		Alias
		Branch json.RawMessage `json:"branch,omitempty"`
	}{
		Alias: (Alias)(n),
	}

	if n.Branch != nil {
		switch v := n.Branch.(type) {
		case *NullableFormatPositions_NullableFormatPositionsBranchOption0:
			raw, err := json.Marshal(v.NullableFormatPositionsBranchOption0)
			if err != nil {
				return nil, fmt.Errorf("marshaling NullableFormatPositions.Branch: %w", err)
			}
			aux.Branch = raw
		case *NullableFormatPositions_Integer:
			raw, err := json.Marshal(v.Integer)
			if err != nil {
				return nil, fmt.Errorf("marshaling NullableFormatPositions.Branch: %w", err)
			}
			aux.Branch = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(n._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range n._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NullableFormatPositions against its JSON Schema constraints.
func (n NullableFormatPositions) Validate() error {
	if n.Buckets != nil {
		if err := n.Buckets.Validate(); err != nil {
			return jsonPathf(err, "buckets")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["chain"] {
		if err := n.Chain.Validate(); err != nil {
			return jsonPathf(err, "chain")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["inline"] {
		if err := n.Inline.Validate(); err != nil {
			return jsonPathf(err, "inline")
		}
	}
	for _i, _item := range n.List {
		if err := _item.Validate(); err != nil {
			return jsonPathf(err, "list[%d]", _i)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["mail"] {
		if err := n.Mail.Validate(); err != nil {
			return jsonPathf(err, "mail")
		}
	}
	for _k, _val := range n.Map {
		if err := _val.Validate(); err != nil {
			return jsonPathf(err, "map[%q]", _k)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["ref"] {
		if err := n.Ref.Validate(); err != nil {
			return jsonPathf(err, "ref")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["stamp"] {
		if err := n.Stamp.Validate(); err != nil {
			return jsonPathf(err, "stamp")
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if n._jsonKeys == nil || n._jsonKeys["wrapped"] {
		if err := n.Wrapped.Validate(); err != nil {
			return jsonPathf(err, "wrapped")
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := n.Branch.(type) {
	case *NullableFormatPositions_NullableFormatPositionsBranchOption0:
		if err := _oneOfSel.NullableFormatPositionsBranchOption0.Validate(); err != nil {
			return jsonPathf(err, "branch")
		}
	}
	// Tuple items: validate each position against its schema type.
	for _idx, _elem := range n.Tuple {
		_ = _elem
		if _idx == 0 {
			_raw, _mErr := json.Marshal(_elem)
			if _mErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _mErr)
			}
			var _typed NullableFormatPositionsTupleItem0
			if _uErr := json.Unmarshal(_raw, &_typed); _uErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _uErr)
			}
			if _vErr := _typed.Validate(); _vErr != nil {
				return fmt.Errorf("tuple: items[%d]: %w", _idx, _vErr)
			}
		}
		if _idx == 1 {
			_cv, _cvOk := _elem.(float64)
			if !_cvOk || _cv != math.Trunc(_cv) {
				return fmt.Errorf("tuple: items[%d]: expected integer, got %T", _idx, _elem)
			}
		}
	}
	return nil
}
