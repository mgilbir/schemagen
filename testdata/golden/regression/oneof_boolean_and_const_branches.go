// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

// OneOfBooleanAndConstBranchesFalseBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfBooleanAndConstBranchesFalseBranch struct {
	_raw json.RawMessage
}

func (o *OneOfBooleanAndConstBranchesFalseBranch) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfBooleanAndConstBranchesFalseBranch) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfBooleanAndConstBranchesFalseBranch) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfBooleanAndConstBranchesFalseBranch) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfBooleanAndConstBranchesFalseBranch) String() string { return string(o._raw) }

// OneOfBooleanAndConstBranchesFalseBranchSchema is the schema OneOfBooleanAndConstBranchesFalseBranch validates against.
var OneOfBooleanAndConstBranchesFalseBranchSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{Boolean: _boolPtr(false)},
	},
}

// Validate checks OneOfBooleanAndConstBranchesFalseBranch against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranchesFalseBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfBooleanAndConstBranchesFalseBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// OneOfBooleanAndConstBranchesMixed accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfBooleanAndConstBranchesMixed struct {
	_raw json.RawMessage
}

func (o *OneOfBooleanAndConstBranchesMixed) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfBooleanAndConstBranchesMixed) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfBooleanAndConstBranchesMixed) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfBooleanAndConstBranchesMixed) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfBooleanAndConstBranchesMixed) String() string { return string(o._raw) }

// OneOfBooleanAndConstBranchesMixedSchema is the schema OneOfBooleanAndConstBranchesMixed validates against.
var OneOfBooleanAndConstBranchesMixedSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Const: _strPtr("\"x\""),
		},
		_schemaNode{Boolean: _boolPtr(false)},
	},
}

// Validate checks OneOfBooleanAndConstBranchesMixed against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranchesMixed) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfBooleanAndConstBranchesMixedSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type OneOfBooleanAndConstBranchesObjectsOnlyOption0 struct {
	K                    string                     `json:"k"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfBooleanAndConstBranchesObjectsOnlyOption0) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type OneOfBooleanAndConstBranchesObjectsOnlyOption0")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfBooleanAndConstBranchesObjectsOnlyOption0
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBooleanAndConstBranchesObjectsOnlyOption0) MarshalJSON() ([]byte, error) {
	type Alias OneOfBooleanAndConstBranchesObjectsOnlyOption0
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfBooleanAndConstBranchesObjectsOnlyOption0 against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranchesObjectsOnlyOption0) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"k"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type OneOfBooleanAndConstBranchesObjectsOnlyOption1 struct {
	J                    string                     `json:"j"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfBooleanAndConstBranchesObjectsOnlyOption1) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type OneOfBooleanAndConstBranchesObjectsOnlyOption1")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"j",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfBooleanAndConstBranchesObjectsOnlyOption1
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"j",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"j": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBooleanAndConstBranchesObjectsOnlyOption1) MarshalJSON() ([]byte, error) {
	type Alias OneOfBooleanAndConstBranchesObjectsOnlyOption1
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfBooleanAndConstBranchesObjectsOnlyOption1 against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranchesObjectsOnlyOption1) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"j"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type OneOfBooleanAndConstBranchesTrueBranchOption0 struct {
	K                    string                     `json:"k"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfBooleanAndConstBranchesTrueBranchOption0) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type OneOfBooleanAndConstBranchesTrueBranchOption0")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfBooleanAndConstBranchesTrueBranchOption0
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBooleanAndConstBranchesTrueBranchOption0) MarshalJSON() ([]byte, error) {
	type Alias OneOfBooleanAndConstBranchesTrueBranchOption0
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfBooleanAndConstBranchesTrueBranchOption0 against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranchesTrueBranchOption0) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"k"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// OneOfBooleanAndConstBranchesTypedEnumBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfBooleanAndConstBranchesTypedEnumBranch struct {
	_raw json.RawMessage
}

func (o *OneOfBooleanAndConstBranchesTypedEnumBranch) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfBooleanAndConstBranchesTypedEnumBranch) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfBooleanAndConstBranchesTypedEnumBranch) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfBooleanAndConstBranchesTypedEnumBranch) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfBooleanAndConstBranchesTypedEnumBranch) String() string { return string(o._raw) }

// OneOfBooleanAndConstBranchesTypedEnumBranchSchema is the schema OneOfBooleanAndConstBranchesTypedEnumBranch validates against.
var OneOfBooleanAndConstBranchesTypedEnumBranchSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Minimum: _floatPtr(10),
					Type:    []string{"integer"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Enum: []string{"\"a\""},
			Type: []string{"string"},
		},
	},
}

// Validate checks OneOfBooleanAndConstBranchesTypedEnumBranch against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranchesTypedEnumBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfBooleanAndConstBranchesTypedEnumBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// OneOfBooleanAndConstBranches - A oneOf mixing an object branch with branches the sealed-interface union cannot select on. Selection decodes a const, an enum and a boolean `false` branch into `any`, which matches every document -- so a document satisfying exactly one branch was counted as matching all of them and refused, and a document satisfying none was left at one match and accepted. The all-object group beside them is the control: it selects correctly and must keep its union.
type OneOfBooleanAndConstBranches struct {
	FalseBranch          OneOfBooleanAndConstBranchesFalseBranch     `json:"falseBranch,omitzero"`
	Mixed                OneOfBooleanAndConstBranchesMixed           `json:"mixed,omitzero"`
	TypedEnumBranch      OneOfBooleanAndConstBranchesTypedEnumBranch `json:"typedEnumBranch,omitzero"`
	ObjectsOnly          isOneOfBooleanAndConstBranches_ObjectsOnly  `json:"-"`
	ScalarsOnly          isOneOfBooleanAndConstBranches_ScalarsOnly  `json:"-"`
	TrueBranch           isOneOfBooleanAndConstBranches_TrueBranch   `json:"-"`
	AdditionalProperties map[string]json.RawMessage                  `json:"-"`
	_jsonKeys            map[string]bool                             // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool                             // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

// isOneOfBooleanAndConstBranches_ObjectsOnly is a sealed interface for the ObjectsOnly field of OneOfBooleanAndConstBranches.
// Exactly one variant must be set.
type isOneOfBooleanAndConstBranches_ObjectsOnly interface {
	isOneOfBooleanAndConstBranches_ObjectsOnly()
}

// OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0 wraps *OneOfBooleanAndConstBranchesObjectsOnlyOption0 as a ObjectsOnly variant.
type OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0 struct {
	OneOfBooleanAndConstBranchesObjectsOnlyOption0 *OneOfBooleanAndConstBranchesObjectsOnlyOption0
}

func (*OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0) isOneOfBooleanAndConstBranches_ObjectsOnly() {
}

// OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1 wraps *OneOfBooleanAndConstBranchesObjectsOnlyOption1 as a ObjectsOnly variant.
type OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1 struct {
	OneOfBooleanAndConstBranchesObjectsOnlyOption1 *OneOfBooleanAndConstBranchesObjectsOnlyOption1
}

func (*OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1) isOneOfBooleanAndConstBranches_ObjectsOnly() {
}

// GetOneOfBooleanAndConstBranchesObjectsOnlyOption0 returns the OneOfBooleanAndConstBranchesObjectsOnlyOption0 variant value, or the zero value if not set.
func (o *OneOfBooleanAndConstBranches) GetOneOfBooleanAndConstBranchesObjectsOnlyOption0() *OneOfBooleanAndConstBranchesObjectsOnlyOption0 {
	if o != nil {
		if v, ok := o.ObjectsOnly.(*OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0); ok {
			return v.OneOfBooleanAndConstBranchesObjectsOnlyOption0
		}
	}
	var zero *OneOfBooleanAndConstBranchesObjectsOnlyOption0
	return zero
}

// GetOneOfBooleanAndConstBranchesObjectsOnlyOption1 returns the OneOfBooleanAndConstBranchesObjectsOnlyOption1 variant value, or the zero value if not set.
func (o *OneOfBooleanAndConstBranches) GetOneOfBooleanAndConstBranchesObjectsOnlyOption1() *OneOfBooleanAndConstBranchesObjectsOnlyOption1 {
	if o != nil {
		if v, ok := o.ObjectsOnly.(*OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1); ok {
			return v.OneOfBooleanAndConstBranchesObjectsOnlyOption1
		}
	}
	var zero *OneOfBooleanAndConstBranchesObjectsOnlyOption1
	return zero
}

// isOneOfBooleanAndConstBranches_ScalarsOnly is a sealed interface for the ScalarsOnly field of OneOfBooleanAndConstBranches.
// Exactly one variant must be set.
type isOneOfBooleanAndConstBranches_ScalarsOnly interface {
	isOneOfBooleanAndConstBranches_ScalarsOnly()
}

// OneOfBooleanAndConstBranches_String wraps string as a ScalarsOnly variant.
type OneOfBooleanAndConstBranches_String struct {
	String string
}

func (*OneOfBooleanAndConstBranches_String) isOneOfBooleanAndConstBranches_ScalarsOnly() {}

// OneOfBooleanAndConstBranches_Integer wraps int64 as a ScalarsOnly variant.
type OneOfBooleanAndConstBranches_Integer struct {
	Integer int64
}

func (*OneOfBooleanAndConstBranches_Integer) isOneOfBooleanAndConstBranches_ScalarsOnly() {}

// GetString returns the String variant value, or the zero value if not set.
func (o *OneOfBooleanAndConstBranches) GetString() string {
	if o != nil {
		if v, ok := o.ScalarsOnly.(*OneOfBooleanAndConstBranches_String); ok {
			return v.String
		}
	}
	var zero string
	return zero
}

// GetInteger returns the Integer variant value, or the zero value if not set.
func (o *OneOfBooleanAndConstBranches) GetInteger() int64 {
	if o != nil {
		if v, ok := o.ScalarsOnly.(*OneOfBooleanAndConstBranches_Integer); ok {
			return v.Integer
		}
	}
	var zero int64
	return zero
}

// isOneOfBooleanAndConstBranches_TrueBranch is a sealed interface for the TrueBranch field of OneOfBooleanAndConstBranches.
// Exactly one variant must be set.
type isOneOfBooleanAndConstBranches_TrueBranch interface {
	isOneOfBooleanAndConstBranches_TrueBranch()
}

// OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0 wraps *OneOfBooleanAndConstBranchesTrueBranchOption0 as a TrueBranch variant.
type OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0 struct {
	OneOfBooleanAndConstBranchesTrueBranchOption0 *OneOfBooleanAndConstBranchesTrueBranchOption0
}

func (*OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0) isOneOfBooleanAndConstBranches_TrueBranch() {
}

// OneOfBooleanAndConstBranches_Any wraps any as a TrueBranch variant.
type OneOfBooleanAndConstBranches_Any struct {
	Any any
}

func (*OneOfBooleanAndConstBranches_Any) isOneOfBooleanAndConstBranches_TrueBranch() {}

// GetOneOfBooleanAndConstBranchesTrueBranchOption0 returns the OneOfBooleanAndConstBranchesTrueBranchOption0 variant value, or the zero value if not set.
func (o *OneOfBooleanAndConstBranches) GetOneOfBooleanAndConstBranchesTrueBranchOption0() *OneOfBooleanAndConstBranchesTrueBranchOption0 {
	if o != nil {
		if v, ok := o.TrueBranch.(*OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0); ok {
			return v.OneOfBooleanAndConstBranchesTrueBranchOption0
		}
	}
	var zero *OneOfBooleanAndConstBranchesTrueBranchOption0
	return zero
}

// GetAny returns the Any variant value, or the zero value if not set.
func (o *OneOfBooleanAndConstBranches) GetAny() any {
	if o != nil {
		if v, ok := o.TrueBranch.(*OneOfBooleanAndConstBranches_Any); ok {
			return v.Any
		}
	}
	var zero any
	return zero
}

func (o *OneOfBooleanAndConstBranches) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	o._jsonNulls = nil
	o.ObjectsOnly = nil
	o.ScalarsOnly = nil
	o.TrueBranch = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type OneOfBooleanAndConstBranches")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"falseBranch",
			"mixed",
			"typedEnumBranch",
			"objectsOnly",
			"scalarsOnly",
			"trueBranch",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfBooleanAndConstBranches
	aux := &struct {
		*Alias
		ObjectsOnly json.RawMessage `json:"objectsOnly"`
		ScalarsOnly json.RawMessage `json:"scalarsOnly"`
		TrueBranch  json.RawMessage `json:"trueBranch"`
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	{
		oneofData := aux.ObjectsOnly
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isOneOfBooleanAndConstBranches_ObjectsOnly
			var oneofStrictErr error

			// Try variant: OneOfBooleanAndConstBranchesObjectsOnlyOption0
			{
				if oneofHasRequiredFields(oneofData, "k") {
					var candidate *OneOfBooleanAndConstBranchesObjectsOnlyOption0
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.ObjectsOnly = &OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0{OneOfBooleanAndConstBranchesObjectsOnlyOption0: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0{OneOfBooleanAndConstBranchesObjectsOnlyOption0: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfBooleanAndConstBranchesObjectsOnlyOption0: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			// Try variant: OneOfBooleanAndConstBranchesObjectsOnlyOption1
			{
				if oneofHasRequiredFields(oneofData, "j") {
					var candidate *OneOfBooleanAndConstBranchesObjectsOnlyOption1
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.ObjectsOnly = &OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1{OneOfBooleanAndConstBranchesObjectsOnlyOption1: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1{OneOfBooleanAndConstBranchesObjectsOnlyOption1: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfBooleanAndConstBranchesObjectsOnlyOption1: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("OneOfBooleanAndConstBranches.ObjectsOnly: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					o.ObjectsOnly = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return fmt.Errorf("OneOfBooleanAndConstBranches.ObjectsOnly: no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfBooleanAndConstBranches.ObjectsOnly: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}

	{
		oneofData := aux.ScalarsOnly
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error

			// Try variant: String
			{
				var candidate string
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					o.ScalarsOnly = &OneOfBooleanAndConstBranches_String{String: candidate}
					oneofMatched++
				} else {
					oneofLastErr = err
				}
			}

			// Try variant: Integer
			{
				var candidate int64
				// Selection gates on whether the branch decodes, so an
				// integer branch has to read a number the same way the rest
				// of the file does -- otherwise a document this branch
				// accepts is reported as matching no branch at all.
				var _iv jsonInteger
				_ivErr := json.Unmarshal(oneofData, &_iv)
				if _ivErr == nil {
					candidate = int64(_iv)
				}
				if err := _ivErr; err == nil {
					o.ScalarsOnly = &OneOfBooleanAndConstBranches_Integer{Integer: candidate}
					oneofMatched++
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("OneOfBooleanAndConstBranches.ScalarsOnly: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfBooleanAndConstBranches.ScalarsOnly: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}

	{
		oneofData := aux.TrueBranch
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isOneOfBooleanAndConstBranches_TrueBranch
			var oneofStrictErr error

			// Try variant: OneOfBooleanAndConstBranchesTrueBranchOption0
			{
				if oneofHasRequiredFields(oneofData, "k") {
					var candidate *OneOfBooleanAndConstBranchesTrueBranchOption0
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.TrueBranch = &OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0{OneOfBooleanAndConstBranchesTrueBranchOption0: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0{OneOfBooleanAndConstBranchesTrueBranchOption0: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfBooleanAndConstBranchesTrueBranchOption0: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			// Try variant: Any
			{
				var candidate any
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					o.TrueBranch = &OneOfBooleanAndConstBranches_Any{Any: candidate}
					oneofMatched++
					// Nothing this branch says is left for a Validate to
					// carry, so matching it is satisfying it.
					oneofStrict++
					oneofStrictSel = &OneOfBooleanAndConstBranches_Any{Any: candidate}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("OneOfBooleanAndConstBranches.TrueBranch: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					o.TrueBranch = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return fmt.Errorf("OneOfBooleanAndConstBranches.TrueBranch: no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfBooleanAndConstBranches.TrueBranch: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"falseBranch",
			"mixed",
			"objectsOnly",
			"scalarsOnly",
			"typedEnumBranch",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"trueBranch",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if o._jsonNulls == nil {
					o._jsonNulls = make(map[string]bool, 1)
				}
				o._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"falseBranch":     true,
			"mixed":           true,
			"typedEnumBranch": true,
			"objectsOnly":     true,
			"scalarsOnly":     true,
			"trueBranch":      true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBooleanAndConstBranches) MarshalJSON() ([]byte, error) {
	type Alias OneOfBooleanAndConstBranches
	aux := struct {
		Alias
		ObjectsOnly json.RawMessage `json:"objectsOnly,omitempty"`
		ScalarsOnly json.RawMessage `json:"scalarsOnly,omitempty"`
		TrueBranch  json.RawMessage `json:"trueBranch,omitempty"`
	}{
		Alias: (Alias)(o),
	}

	if o.ObjectsOnly != nil {
		switch v := o.ObjectsOnly.(type) {
		case *OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0:
			raw, err := json.Marshal(v.OneOfBooleanAndConstBranchesObjectsOnlyOption0)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfBooleanAndConstBranches.ObjectsOnly: %w", err)
			}
			aux.ObjectsOnly = raw
		case *OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1:
			raw, err := json.Marshal(v.OneOfBooleanAndConstBranchesObjectsOnlyOption1)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfBooleanAndConstBranches.ObjectsOnly: %w", err)
			}
			aux.ObjectsOnly = raw
		}
	}

	if o.ScalarsOnly != nil {
		switch v := o.ScalarsOnly.(type) {
		case *OneOfBooleanAndConstBranches_String:
			raw, err := json.Marshal(v.String)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfBooleanAndConstBranches.ScalarsOnly: %w", err)
			}
			aux.ScalarsOnly = raw
		case *OneOfBooleanAndConstBranches_Integer:
			raw, err := json.Marshal(v.Integer)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfBooleanAndConstBranches.ScalarsOnly: %w", err)
			}
			aux.ScalarsOnly = raw
		}
	}

	if o.TrueBranch != nil {
		switch v := o.TrueBranch.(type) {
		case *OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0:
			raw, err := json.Marshal(v.OneOfBooleanAndConstBranchesTrueBranchOption0)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfBooleanAndConstBranches.TrueBranch: %w", err)
			}
			aux.TrueBranch = raw
		case *OneOfBooleanAndConstBranches_Any:
			raw, err := json.Marshal(v.Any)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfBooleanAndConstBranches.TrueBranch: %w", err)
			}
			aux.TrueBranch = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(o._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range o._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfBooleanAndConstBranches against its JSON Schema constraints.
func (o OneOfBooleanAndConstBranches) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["falseBranch"] {
		if err := o.FalseBranch.Validate(); err != nil {
			return fmt.Errorf("falseBranch.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["mixed"] {
		if err := o.Mixed.Validate(); err != nil {
			return fmt.Errorf("mixed.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["typedEnumBranch"] {
		if err := o.TypedEnumBranch.Validate(); err != nil {
			return fmt.Errorf("typedEnumBranch.%w", err)
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := o.ObjectsOnly.(type) {
	case *OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption0:
		if _oneOfSel.OneOfBooleanAndConstBranchesObjectsOnlyOption0 != nil {
			if err := _oneOfSel.OneOfBooleanAndConstBranchesObjectsOnlyOption0.Validate(); err != nil {
				return fmt.Errorf("objectsOnly.%w", err)
			}
		}
	case *OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesObjectsOnlyOption1:
		if _oneOfSel.OneOfBooleanAndConstBranchesObjectsOnlyOption1 != nil {
			if err := _oneOfSel.OneOfBooleanAndConstBranchesObjectsOnlyOption1.Validate(); err != nil {
				return fmt.Errorf("objectsOnly.%w", err)
			}
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := o.TrueBranch.(type) {
	case *OneOfBooleanAndConstBranches_OneOfBooleanAndConstBranchesTrueBranchOption0:
		if _oneOfSel.OneOfBooleanAndConstBranchesTrueBranchOption0 != nil {
			if err := _oneOfSel.OneOfBooleanAndConstBranchesTrueBranchOption0.Validate(); err != nil {
				return fmt.Errorf("trueBranch.%w", err)
			}
		}
	}
	return nil
}
