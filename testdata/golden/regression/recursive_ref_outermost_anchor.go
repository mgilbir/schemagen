// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

type Inner struct {
	AdditionalProperties map[string]*Inner `json:"-"`
	_nonObject           bool              // set by UnmarshalJSON when the JSON data is not an object
	_rawNonObject        json.RawMessage   // raw bytes of non-object data for lossless roundtrip
}

func (i *Inner) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._nonObject = false
	i._rawNonObject = nil
	// The schema admits a document that is not an object, so object constraints
	// are type-conditional. Non-object JSON data is accepted here and judged by
	// Validate; raw bytes are preserved for roundtrip.
	if len(data) == 0 || data[0] != '{' {
		i._nonObject = true
		i._rawNonObject = append(i._rawNonObject[:0], data...)
		return nil
	}
	type Alias Inner
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return jsonDecodeRefusal(err)
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]*Inner)
			}
			var val *Inner
			if err := json.Unmarshal(rawVal, &val); err != nil {
				return jsonElemPathf(jsonDecodeRefusal(err), "[%q]", rawKey)
			}
			i.AdditionalProperties[rawKey] = val
		}
	}

	return nil
}
func (i Inner) MarshalJSON() ([]byte, error) {
	// Non-object data was silently accepted — return the original raw bytes.
	if i._nonObject {
		if len(i._rawNonObject) > 0 {
			return i._rawNonObject, nil
		}
		return []byte("null"), nil
	}
	type Alias Inner
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.AdditionalProperties {
		raw, err := json.Marshal(v)
		if err != nil {
			return nil, fmt.Errorf("marshaling additional property %q: %w", k, err)
		}
		obj[k] = raw
	}
	return json.Marshal(obj)
}

// Validate checks Inner against its JSON Schema constraints.
func (i Inner) Validate() error {
	// Non-object data was silently accepted — validate non-object constraints if any.
	if i._nonObject {
		return nil
	}
	for _k0, _e0 := range i.AdditionalProperties {
		if _e0 == nil {
			continue // a JSON null left no value behind to check
		}
		if _err := _e0.Validate(); _err != nil {
			return jsonElemPathf(_err, "[%q]", _k0)
		}
	}
	return nil
}

// Root accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type Root struct {
	_raw json.RawMessage
}

func (r *Root) UnmarshalJSON(data []byte) error {
	r._raw = append(r._raw[:0], data...)
	return nil
}

func (r Root) MarshalJSON() ([]byte, error) {
	if len(r._raw) == 0 {
		return []byte("null"), nil
	}
	return r._raw, nil
}

func (r Root) Raw() json.RawMessage { return r._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (r Root) IsZero() bool { return len(r._raw) == 0 }

func (r Root) String() string { return string(r._raw) }

// RootSchema is the schema Root validates against, and the nodes below
// it are the parts of that schema something inside it refers back to.
//
// They are assembled in init() rather than declared with their values because
// the schema is recursive, and Go refuses a cycle between package-level variable
// initialisers. Taking a node's address is not part of that cycle, so the
// assignments may run in any order.
var (
	RootSchema   _schemaNode
	_rtRootNode1 _schemaNode
)

func init() {
	_rtRootNode1 = _schemaNode{
		AdditionalProperties: _node(_schemaNode{
			DynamicRef: &_dynamicRef{
				Anchor:   "",
				Fallback: _node(_schemaNode{Ref: &_rtRootNode1}),
			},
		}),
		DynamicAnchors: []_schemaAnchor{
			{Name: ""},
		},
	}
	RootSchema = _schemaNode{
		Else: _node(_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{Ref: &_rtRootNode1},
			},
			DynamicAnchors: []_schemaAnchor{
				{Name: ""},
			},
			Type: []string{"object", "integer"},
		}),
		If: _node(_schemaNode{
			PropertyNames: _node(_schemaNode{
				Pattern: _strPtr("^[a-m]"),
			}),
		}),
		Then: _node(_schemaNode{
			AllOf: []_schemaNode{
				_schemaNode{Ref: &_rtRootNode1},
			},
			DynamicAnchors: []_schemaAnchor{
				{Name: ""},
			},
		}),
	}
}

// Validate checks Root against its JSON Schema constraints.
func (r Root) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(r._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(r._raw, &_v); _err != nil {
		// A sentence about the value, joined by the same rule as the verdict
		// below. Structural: the raw bytes came from a decoder that had already
		// accepted them as JSON, so nothing has been seen to reach this.
		return jsonValueErrorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&RootSchema, _v); !_res.ok {
		return _evalError(_res)
	}
	return nil
}
