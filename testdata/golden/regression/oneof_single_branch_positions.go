// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

// OneOfSingleBranchPositionsConstBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfSingleBranchPositionsConstBranch struct {
	_raw json.RawMessage
}

func (o *OneOfSingleBranchPositionsConstBranch) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfSingleBranchPositionsConstBranch) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfSingleBranchPositionsConstBranch) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfSingleBranchPositionsConstBranch) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfSingleBranchPositionsConstBranch) String() string { return string(o._raw) }

// OneOfSingleBranchPositionsConstBranchSchema is the schema OneOfSingleBranchPositionsConstBranch validates against.
var OneOfSingleBranchPositionsConstBranchSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
			Type:  []string{"string"},
		},
	},
}

// Validate checks OneOfSingleBranchPositionsConstBranch against its JSON Schema constraints.
func (o OneOfSingleBranchPositionsConstBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfSingleBranchPositionsConstBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// OneOfSingleBranchPositionsEmptyEnum accepts any JSON value and validates a root-level "not" constraint.
type OneOfSingleBranchPositionsEmptyEnum struct {
	_raw json.RawMessage
}

func (o *OneOfSingleBranchPositionsEmptyEnum) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfSingleBranchPositionsEmptyEnum) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfSingleBranchPositionsEmptyEnum) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfSingleBranchPositionsEmptyEnum) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfSingleBranchPositionsEmptyEnum) String() string { return string(o._raw) }

// Validate checks OneOfSingleBranchPositionsEmptyEnum against its JSON Schema "not" constraint.
func (o OneOfSingleBranchPositionsEmptyEnum) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// OneOfSingleBranchPositionsFalseBranch accepts any JSON value and validates a root-level "not" constraint.
type OneOfSingleBranchPositionsFalseBranch struct {
	_raw json.RawMessage
}

func (o *OneOfSingleBranchPositionsFalseBranch) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfSingleBranchPositionsFalseBranch) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfSingleBranchPositionsFalseBranch) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfSingleBranchPositionsFalseBranch) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfSingleBranchPositionsFalseBranch) String() string { return string(o._raw) }

// Validate checks OneOfSingleBranchPositionsFalseBranch against its JSON Schema "not" constraint.
func (o OneOfSingleBranchPositionsFalseBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// OneOfSingleBranchPositionsListItem accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfSingleBranchPositionsListItem struct {
	_raw json.RawMessage
}

func (o *OneOfSingleBranchPositionsListItem) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfSingleBranchPositionsListItem) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfSingleBranchPositionsListItem) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfSingleBranchPositionsListItem) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfSingleBranchPositionsListItem) String() string { return string(o._raw) }

// OneOfSingleBranchPositionsListItemSchema is the schema OneOfSingleBranchPositionsListItem validates against.
var OneOfSingleBranchPositionsListItemSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
			Type:  []string{"string"},
		},
	},
}

// Validate checks OneOfSingleBranchPositionsListItem against its JSON Schema constraints.
func (o OneOfSingleBranchPositionsListItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfSingleBranchPositionsListItemSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// OneOfSingleBranchPositionsMapValue accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfSingleBranchPositionsMapValue struct {
	_raw json.RawMessage
}

func (o *OneOfSingleBranchPositionsMapValue) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfSingleBranchPositionsMapValue) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfSingleBranchPositionsMapValue) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfSingleBranchPositionsMapValue) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfSingleBranchPositionsMapValue) String() string { return string(o._raw) }

// OneOfSingleBranchPositionsMapValueSchema is the schema OneOfSingleBranchPositionsMapValue validates against.
var OneOfSingleBranchPositionsMapValueSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
			Type:  []string{"string"},
		},
	},
}

// Validate checks OneOfSingleBranchPositionsMapValue against its JSON Schema constraints.
func (o OneOfSingleBranchPositionsMapValue) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfSingleBranchPositionsMapValueSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type OneOfSingleBranchPositionsObjBranchOption0 struct {
	K                    string                     `json:"k"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfSingleBranchPositionsObjBranchOption0) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type OneOfSingleBranchPositionsObjBranchOption0")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfSingleBranchPositionsObjBranchOption0
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfSingleBranchPositionsObjBranchOption0) MarshalJSON() ([]byte, error) {
	type Alias OneOfSingleBranchPositionsObjBranchOption0
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfSingleBranchPositionsObjBranchOption0 against its JSON Schema constraints.
func (o OneOfSingleBranchPositionsObjBranchOption0) Validate() error {
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"k"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

// OneOfSingleBranchPositionsTwoBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type OneOfSingleBranchPositionsTwoBranch struct {
	_raw json.RawMessage
}

func (o *OneOfSingleBranchPositionsTwoBranch) UnmarshalJSON(data []byte) error {
	o._raw = append(o._raw[:0], data...)
	return nil
}

func (o OneOfSingleBranchPositionsTwoBranch) MarshalJSON() ([]byte, error) {
	if len(o._raw) == 0 {
		return []byte("null"), nil
	}
	return o._raw, nil
}

func (o OneOfSingleBranchPositionsTwoBranch) Raw() json.RawMessage { return o._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (o OneOfSingleBranchPositionsTwoBranch) IsZero() bool { return len(o._raw) == 0 }

func (o OneOfSingleBranchPositionsTwoBranch) String() string { return string(o._raw) }

// OneOfSingleBranchPositionsTwoBranchSchema is the schema OneOfSingleBranchPositionsTwoBranch validates against.
var OneOfSingleBranchPositionsTwoBranchSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
			Type:  []string{"string"},
		},
		_schemaNode{
			Type: []string{"integer"},
		},
	},
}

// Validate checks OneOfSingleBranchPositionsTwoBranch against its JSON Schema constraints.
func (o OneOfSingleBranchPositionsTwoBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(o._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(o._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&OneOfSingleBranchPositionsTwoBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// OneOfSingleBranchPositions - A oneOf of one branch on a declared property built a sealed-interface union of one variant, and a lone variant that outruns selection is matched by every document the field can hold. So the branch was tested nowhere: constBranch accepted "b", falseBranch and emptyEnum accepted values no document satisfies, and the arity guard that let them through read a group of one as a group that never reaches the union. boundBranch, typedBranch, twoBranch, list and map are the controls: the first two are decided by selection and keep the union, and the last three were already right.
type OneOfSingleBranchPositions struct {
	ConstBranch          OneOfSingleBranchPositionsConstBranch         `json:"constBranch,omitzero"`
	EmptyEnum            OneOfSingleBranchPositionsEmptyEnum           `json:"emptyEnum,omitzero"`
	FalseBranch          OneOfSingleBranchPositionsFalseBranch         `json:"falseBranch,omitzero"`
	List                 []OneOfSingleBranchPositionsListItem          `json:"list,omitzero"`
	Map                  map[string]OneOfSingleBranchPositionsMapValue `json:"map,omitzero"`
	TwoBranch            OneOfSingleBranchPositionsTwoBranch           `json:"twoBranch,omitzero"`
	BoundBranch          isOneOfSingleBranchPositions_BoundBranch      `json:"-"`
	ObjBranch            isOneOfSingleBranchPositions_ObjBranch        `json:"-"`
	TypedBranch          isOneOfSingleBranchPositions_TypedBranch      `json:"-"`
	AdditionalProperties map[string]json.RawMessage                    `json:"-"`
	_jsonKeys            map[string]bool                               // set by UnmarshalJSON for optional field / dependentSchemas validation
}

// isOneOfSingleBranchPositions_BoundBranch is a sealed interface for the BoundBranch field of OneOfSingleBranchPositions.
// Exactly one variant must be set.
type isOneOfSingleBranchPositions_BoundBranch interface {
	isOneOfSingleBranchPositions_BoundBranch()
}

// OneOfSingleBranchPositions_Integer wraps int64 as a BoundBranch variant.
type OneOfSingleBranchPositions_Integer struct {
	Integer int64
}

func (*OneOfSingleBranchPositions_Integer) isOneOfSingleBranchPositions_BoundBranch() {}

// GetInteger returns the Integer variant value, or the zero value if not set.
func (o *OneOfSingleBranchPositions) GetInteger() int64 {
	if o != nil {
		if v, ok := o.BoundBranch.(*OneOfSingleBranchPositions_Integer); ok {
			return v.Integer
		}
	}
	var zero int64
	return zero
}

// isOneOfSingleBranchPositions_ObjBranch is a sealed interface for the ObjBranch field of OneOfSingleBranchPositions.
// Exactly one variant must be set.
type isOneOfSingleBranchPositions_ObjBranch interface {
	isOneOfSingleBranchPositions_ObjBranch()
}

// OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0 wraps *OneOfSingleBranchPositionsObjBranchOption0 as a ObjBranch variant.
type OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0 struct {
	OneOfSingleBranchPositionsObjBranchOption0 *OneOfSingleBranchPositionsObjBranchOption0
}

func (*OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0) isOneOfSingleBranchPositions_ObjBranch() {
}

// GetOneOfSingleBranchPositionsObjBranchOption0 returns the OneOfSingleBranchPositionsObjBranchOption0 variant value, or the zero value if not set.
func (o *OneOfSingleBranchPositions) GetOneOfSingleBranchPositionsObjBranchOption0() *OneOfSingleBranchPositionsObjBranchOption0 {
	if o != nil {
		if v, ok := o.ObjBranch.(*OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0); ok {
			return v.OneOfSingleBranchPositionsObjBranchOption0
		}
	}
	var zero *OneOfSingleBranchPositionsObjBranchOption0
	return zero
}

// isOneOfSingleBranchPositions_TypedBranch is a sealed interface for the TypedBranch field of OneOfSingleBranchPositions.
// Exactly one variant must be set.
type isOneOfSingleBranchPositions_TypedBranch interface {
	isOneOfSingleBranchPositions_TypedBranch()
}

// OneOfSingleBranchPositions_String wraps string as a TypedBranch variant.
type OneOfSingleBranchPositions_String struct {
	String string
}

func (*OneOfSingleBranchPositions_String) isOneOfSingleBranchPositions_TypedBranch() {}

// GetString returns the String variant value, or the zero value if not set.
func (o *OneOfSingleBranchPositions) GetString() string {
	if o != nil {
		if v, ok := o.TypedBranch.(*OneOfSingleBranchPositions_String); ok {
			return v.String
		}
	}
	var zero string
	return zero
}

func (o *OneOfSingleBranchPositions) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	o.BoundBranch = nil
	o.ObjBranch = nil
	o.TypedBranch = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type OneOfSingleBranchPositions")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"constBranch",
			"emptyEnum",
			"falseBranch",
			"list",
			"map",
			"twoBranch",
			"boundBranch",
			"objBranch",
			"typedBranch",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfSingleBranchPositions
	aux := &struct {
		*Alias
		BoundBranch json.RawMessage `json:"boundBranch"`
		ObjBranch   json.RawMessage `json:"objBranch"`
		TypedBranch json.RawMessage `json:"typedBranch"`
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}

	{
		oneofData := aux.BoundBranch
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A branch whose Go type accepted the value but whose own
			// constraints rejected it is the near miss worth reporting, so it
			// outranks a decode failure from a branch of some other type.
			var oneofCheckErr error

			// Try variant: Integer
			{
				var candidate int64
				// Selection gates on whether the branch decodes, so an
				// integer branch has to read a number the same way the rest
				// of the file does -- otherwise a document this branch
				// accepts is reported as matching no branch at all.
				var _iv jsonInteger
				_ivErr := json.Unmarshal(oneofData, &_iv)
				if _ivErr == nil {
					candidate = int64(_iv)
				}
				if err := _ivErr; err == nil {
					// The branch's own constraints decide the match too: decoding
					// into the variant's Go type is not on its own a match.
					oneofOK := true
					if float64(candidate) < 5 {
						oneofOK = false
					}
					if oneofOK {
						o.BoundBranch = &OneOfSingleBranchPositions_Integer{Integer: candidate}
						oneofMatched++
					} else {
						oneofCheckErr = fmt.Errorf("variant Integer: value does not satisfy the variant's constraints")
					}
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				if oneofCheckErr != nil {
					oneofLastErr = oneofCheckErr
				}
				return fmt.Errorf("OneOfSingleBranchPositions.BoundBranch: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfSingleBranchPositions.BoundBranch: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}

	{
		oneofData := aux.ObjBranch
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isOneOfSingleBranchPositions_ObjBranch
			var oneofStrictErr error

			// Try variant: OneOfSingleBranchPositionsObjBranchOption0
			{
				if oneofHasRequiredFields(oneofData, "k") {
					var candidate *OneOfSingleBranchPositionsObjBranchOption0
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.ObjBranch = &OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0{OneOfSingleBranchPositionsObjBranchOption0: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0{OneOfSingleBranchPositionsObjBranchOption0: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfSingleBranchPositionsObjBranchOption0: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("OneOfSingleBranchPositions.ObjBranch: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					o.ObjBranch = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return fmt.Errorf("OneOfSingleBranchPositions.ObjBranch: no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfSingleBranchPositions.ObjBranch: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}

	{
		oneofData := aux.TypedBranch
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error

			// Try variant: String
			{
				var candidate string
				if err := json.Unmarshal(oneofData, &candidate); err == nil {
					o.TypedBranch = &OneOfSingleBranchPositions_String{String: candidate}
					oneofMatched++
				} else {
					oneofLastErr = err
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("OneOfSingleBranchPositions.TypedBranch: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfSingleBranchPositions.TypedBranch: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"boundBranch",
			"constBranch",
			"objBranch",
			"twoBranch",
			"typedBranch",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		if _v, ok := raw["list"]; ok {
			if err := checkJSONNulls(_v, "list", &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		if _v, ok := raw["map"]; ok {
			if err := checkJSONNulls(_v, "map", &jsonNullRule{Reject: true, IsMap: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"constBranch": true,
			"emptyEnum":   true,
			"falseBranch": true,
			"list":        true,
			"map":         true,
			"twoBranch":   true,
			"boundBranch": true,
			"objBranch":   true,
			"typedBranch": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfSingleBranchPositions) MarshalJSON() ([]byte, error) {
	type Alias OneOfSingleBranchPositions
	aux := struct {
		Alias
		BoundBranch json.RawMessage `json:"boundBranch,omitempty"`
		ObjBranch   json.RawMessage `json:"objBranch,omitempty"`
		TypedBranch json.RawMessage `json:"typedBranch,omitempty"`
	}{
		Alias: (Alias)(o),
	}

	if o.BoundBranch != nil {
		switch v := o.BoundBranch.(type) {
		case *OneOfSingleBranchPositions_Integer:
			raw, err := json.Marshal(v.Integer)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfSingleBranchPositions.BoundBranch: %w", err)
			}
			aux.BoundBranch = raw
		}
	}

	if o.ObjBranch != nil {
		switch v := o.ObjBranch.(type) {
		case *OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0:
			raw, err := json.Marshal(v.OneOfSingleBranchPositionsObjBranchOption0)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfSingleBranchPositions.ObjBranch: %w", err)
			}
			aux.ObjBranch = raw
		}
	}

	if o.TypedBranch != nil {
		switch v := o.TypedBranch.(type) {
		case *OneOfSingleBranchPositions_String:
			raw, err := json.Marshal(v.String)
			if err != nil {
				return nil, fmt.Errorf("marshaling OneOfSingleBranchPositions.TypedBranch: %w", err)
			}
			aux.TypedBranch = raw
		}
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfSingleBranchPositions against its JSON Schema constraints.
func (o OneOfSingleBranchPositions) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["constBranch"] {
		if err := o.ConstBranch.Validate(); err != nil {
			return fmt.Errorf("constBranch.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["emptyEnum"] {
		if err := o.EmptyEnum.Validate(); err != nil {
			return fmt.Errorf("emptyEnum.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["falseBranch"] {
		if err := o.FalseBranch.Validate(); err != nil {
			return fmt.Errorf("falseBranch.%w", err)
		}
	}
	for _i, _item := range o.List {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("list[%d].%w", _i, err)
		}
	}
	for _k, _val := range o.Map {
		if err := _val.Validate(); err != nil {
			return fmt.Errorf("map[%q].%w", _k, err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if o._jsonKeys == nil || o._jsonKeys["twoBranch"] {
		if err := o.TwoBranch.Validate(); err != nil {
			return fmt.Errorf("twoBranch.%w", err)
		}
	}
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := o.ObjBranch.(type) {
	case *OneOfSingleBranchPositions_OneOfSingleBranchPositionsObjBranchOption0:
		if _oneOfSel.OneOfSingleBranchPositionsObjBranchOption0 != nil {
			if err := _oneOfSel.OneOfSingleBranchPositionsObjBranchOption0.Validate(); err != nil {
				return fmt.Errorf("objBranch.%w", err)
			}
		}
	}
	return nil
}
