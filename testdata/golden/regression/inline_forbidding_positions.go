// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
)

// Never accepts any JSON value and validates a root-level "not" constraint.
type Never struct {
	_raw json.RawMessage
}

func (n *Never) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n Never) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n Never) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n Never) IsZero() bool { return len(n._raw) == 0 }

func (n Never) String() string { return string(n._raw) }

// Validate checks Never against its JSON Schema "not" constraint.
func (n Never) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// NeverEnum accepts any JSON value and validates a root-level "not" constraint.
type NeverEnum struct {
	_raw json.RawMessage
}

func (n *NeverEnum) UnmarshalJSON(data []byte) error {
	n._raw = append(n._raw[:0], data...)
	return nil
}

func (n NeverEnum) MarshalJSON() ([]byte, error) {
	if len(n._raw) == 0 {
		return []byte("null"), nil
	}
	return n._raw, nil
}

func (n NeverEnum) Raw() json.RawMessage { return n._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (n NeverEnum) IsZero() bool { return len(n._raw) == 0 }

func (n NeverEnum) String() string { return string(n._raw) }

// Validate checks NeverEnum against its JSON Schema "not" constraint.
func (n NeverEnum) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(n._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

type NeverWithK struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (n *NeverWithK) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type NeverWithK")
	}
	type Alias NeverWithK
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NeverWithK) MarshalJSON() ([]byte, error) {
	type Alias NeverWithK
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NeverWithK against its JSON Schema constraints.
func (n NeverWithK) Validate() error {
	// dependentSchemas: when a trigger key is present, validate against the dependent sub-schema.
	if n._jsonKeys["k"] {
		return fmt.Errorf("dependentSchema %q: schema is false, object with this property is always invalid", "k")
	}
	return nil
}

type NoNames struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (n *NoNames) UnmarshalJSON(data []byte) error {
	n.AdditionalProperties = nil
	n._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type NoNames")
	}
	type Alias NoNames
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(n),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		n._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			n._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if n.AdditionalProperties == nil {
				n.AdditionalProperties = make(map[string]json.RawMessage)
			}
			n.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (n NoNames) MarshalJSON() ([]byte, error) {
	type Alias NoNames
	aux := struct {
		Alias
	}{
		Alias: (Alias)(n),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range n.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks NoNames against its JSON Schema constraints.
func (n NoNames) Validate() error {
	// propertyNames: validate that all property names satisfy the constraint.
	for _pnKey := range n._jsonKeys {
		return fmt.Errorf("propertyNames: property %q is not allowed (schema is false)", _pnKey)
	}
	return nil
}

// InlineForbiddingPositionsEmptyEnumAllOf accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsEmptyEnumAllOf struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsEmptyEnumAllOf) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsEmptyEnumAllOf) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsEmptyEnumAllOf) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsEmptyEnumAllOf) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsEmptyEnumAllOf) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsEmptyEnumAllOf against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsEmptyEnumAllOf) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsEmptyEnumAnyOf accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsEmptyEnumAnyOf struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsEmptyEnumAnyOf) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsEmptyEnumAnyOf) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsEmptyEnumAnyOf) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsEmptyEnumAnyOf) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsEmptyEnumAnyOf) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsEmptyEnumAnyOf against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsEmptyEnumAnyOf) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsEmptyEnumBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type InlineForbiddingPositionsEmptyEnumBranch struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsEmptyEnumBranch) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsEmptyEnumBranch) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsEmptyEnumBranch) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsEmptyEnumBranch) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsEmptyEnumBranch) String() string { return string(i._raw) }

// InlineForbiddingPositionsEmptyEnumBranchSchema is the schema InlineForbiddingPositionsEmptyEnumBranch validates against.
var InlineForbiddingPositionsEmptyEnumBranchSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{Boolean: _boolPtr(false)},
		_schemaNode{
			Type: []string{"string"},
		},
	},
}

// Validate checks InlineForbiddingPositionsEmptyEnumBranch against its JSON Schema constraints.
func (i InlineForbiddingPositionsEmptyEnumBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&InlineForbiddingPositionsEmptyEnumBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// InlineForbiddingPositionsEmptyEnumItemsItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsEmptyEnumItemsItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsEmptyEnumItemsItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsEmptyEnumItemsItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsEmptyEnumItemsItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsEmptyEnumItemsItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsEmptyEnumItemsItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsEmptyEnumItemsItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsEmptyEnumItemsItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

type InlineForbiddingPositionsEmptyEnumPattern struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (i *InlineForbiddingPositionsEmptyEnumPattern) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i.PatternProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InlineForbiddingPositionsEmptyEnumPattern")
	}
	type Alias InlineForbiddingPositionsEmptyEnumPattern
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^a", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if i.PatternProperties == nil {
					i.PatternProperties = make(map[string]json.RawMessage)
				}
				i.PatternProperties[rawKey] = rawVal
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InlineForbiddingPositionsEmptyEnumPattern) MarshalJSON() ([]byte, error) {
	type Alias InlineForbiddingPositionsEmptyEnumPattern
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range i.PatternProperties {
		obj[k] = v
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InlineForbiddingPositionsEmptyEnumPattern against its JSON Schema constraints.
func (i InlineForbiddingPositionsEmptyEnumPattern) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(i.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^a", ecmaflags.Unicode),
		}
		for k, v := range i.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				return fmt.Errorf("patternProperties: key %q matches forbidden pattern %s", k, "^a")
			}
		}
	}
	return nil
}

type InlineForbiddingPositionsEmptyEnumUnevalProps struct {
	K                    any                        `json:"k,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonNulls           map[string]bool            // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (i *InlineForbiddingPositionsEmptyEnumUnevalProps) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonNulls = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InlineForbiddingPositionsEmptyEnumUnevalProps")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"k",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias InlineForbiddingPositionsEmptyEnumUnevalProps
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if i._jsonNulls == nil {
					i._jsonNulls = make(map[string]bool, 1)
				}
				i._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InlineForbiddingPositionsEmptyEnumUnevalProps) MarshalJSON() ([]byte, error) {
	type Alias InlineForbiddingPositionsEmptyEnumUnevalProps
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(i._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range i._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InlineForbiddingPositionsEmptyEnumUnevalProps against its JSON Schema constraints.
func (i InlineForbiddingPositionsEmptyEnumUnevalProps) Validate() error {
	// unevaluatedProperties: check overflow map for properties not covered by evaluated set.
	{
		evaluatedNames := map[string]bool{
			"k": true,
		}
		for k, v := range i.AdditionalProperties {
			evaluated := false
			_ = v
			if evaluatedNames[k] {
				evaluated = true
			}
			if !evaluated {
				return fmt.Errorf("unevaluated property %q is not allowed", k)
			}
		}
	}
	return nil
}

// InlineForbiddingPositionsEmptyEnumValuesValue accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsEmptyEnumValuesValue struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsEmptyEnumValuesValue) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsEmptyEnumValuesValue) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsEmptyEnumValuesValue) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsEmptyEnumValuesValue) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsEmptyEnumValuesValue) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsEmptyEnumValuesValue against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsEmptyEnumValuesValue) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsFalseItemsItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsFalseItemsItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsFalseItemsItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsFalseItemsItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsFalseItemsItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsFalseItemsItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsFalseItemsItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsFalseItemsItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsFalseItemsItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsInferredEmptyEnumItemsItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsInferredEmptyEnumItemsItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsInferredEmptyEnumItemsItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsInferredEmptyEnumItemsItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsInferredEmptyEnumItemsItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsInferredEmptyEnumItemsItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsInferredEmptyEnumItemsItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsInferredEmptyEnumItemsItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsInferredEmptyEnumItemsItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsInferredEmptyEnumItems accepts any JSON value. Constraints apply only when the value is array.
type InlineForbiddingPositionsInferredEmptyEnumItems struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InlineForbiddingPositionsInferredEmptyEnumItems) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InlineForbiddingPositionsInferredEmptyEnumItems) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InlineForbiddingPositionsInferredEmptyEnumItems) Slice() []any  { return i._value }
func (i InlineForbiddingPositionsInferredEmptyEnumItems) IsArray() bool { return !i._isRaw }
func (i InlineForbiddingPositionsInferredEmptyEnumItems) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InlineForbiddingPositionsInferredEmptyEnumItems) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InlineForbiddingPositionsInferredEmptyEnumItems against its JSON Schema constraints.
func (i InlineForbiddingPositionsInferredEmptyEnumItems) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	if len(i._value) > 0 {
		return fmt.Errorf("items: no items are allowed (got %d)", len(i._value))
	}
	return nil
}

// InlineForbiddingPositionsInferredEmptyEnumSlot accepts any JSON value. Constraints apply only when the value is array.
type InlineForbiddingPositionsInferredEmptyEnumSlot struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InlineForbiddingPositionsInferredEmptyEnumSlot) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InlineForbiddingPositionsInferredEmptyEnumSlot) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InlineForbiddingPositionsInferredEmptyEnumSlot) Slice() []any  { return i._value }
func (i InlineForbiddingPositionsInferredEmptyEnumSlot) IsArray() bool { return !i._isRaw }
func (i InlineForbiddingPositionsInferredEmptyEnumSlot) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InlineForbiddingPositionsInferredEmptyEnumSlot) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InlineForbiddingPositionsInferredEmptyEnumSlot against its JSON Schema constraints.
func (i InlineForbiddingPositionsInferredEmptyEnumSlot) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	{
		if len(i._value) > 0 {
			return fmt.Errorf("items[0]: value is not allowed (schema is false)")
		}
	}
	return nil
}

// InlineForbiddingPositionsInferredEmptyEnumTail accepts any JSON value. Constraints apply only when the value is array.
type InlineForbiddingPositionsInferredEmptyEnumTail struct {
	_value []any
	_raw   json.RawMessage
	_isRaw bool
}

func (i *InlineForbiddingPositionsInferredEmptyEnumTail) UnmarshalJSON(data []byte) error {
	// Null is a non-matching type for inferred schemas — store as raw.
	if string(data) == "null" {
		i._raw = append(i._raw[:0], data...)
		i._isRaw = true
		return nil
	}
	// Try typed unmarshal first.
	if _err := json.Unmarshal(data, &i._value); _err == nil {
		i._isRaw = false
		return nil
	}
	// Non-matching type — store raw bytes, accept silently per JSON Schema.
	i._raw = append(i._raw[:0], data...)
	i._isRaw = true
	return nil
}
func (i InlineForbiddingPositionsInferredEmptyEnumTail) MarshalJSON() ([]byte, error) {
	if i._isRaw {
		if len(i._raw) == 0 {
			return []byte("null"), nil
		}
		return i._raw, nil
	}
	return json.Marshal(i._value)
}
func (i InlineForbiddingPositionsInferredEmptyEnumTail) Slice() []any  { return i._value }
func (i InlineForbiddingPositionsInferredEmptyEnumTail) IsArray() bool { return !i._isRaw }
func (i InlineForbiddingPositionsInferredEmptyEnumTail) Raw() json.RawMessage {
	if i._isRaw {
		return i._raw
	}
	_b, _ := json.Marshal(i._value)
	return _b
}
func (i InlineForbiddingPositionsInferredEmptyEnumTail) String() string {
	if i._isRaw {
		return string(i._raw)
	}
	return fmt.Sprintf("%v", i._value)
}

// Validate checks InlineForbiddingPositionsInferredEmptyEnumTail against its JSON Schema constraints.
func (i InlineForbiddingPositionsInferredEmptyEnumTail) Validate() error {
	if i._isRaw {
		return nil // Constraints don't apply to non-matching types.
	}
	// Item-level validation for inferred array.
	{
	}
	if len(i._value) > 1 {
		return fmt.Errorf("additionalItems: %d extra items are not allowed", len(i._value)-1)
	}
	return nil
}

// InlineForbiddingPositionsNestedFalseItemsItemItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsNestedFalseItemsItemItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNestedFalseItemsItemItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNestedFalseItemsItemItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNestedFalseItemsItemItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNestedFalseItemsItemItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNestedFalseItemsItemItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsNestedFalseItemsItemItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsNestedFalseItemsItemItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsNotAnyOfEmptyEnum accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type InlineForbiddingPositionsNotAnyOfEmptyEnum struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNotAnyOfEmptyEnum) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNotAnyOfEmptyEnum) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNotAnyOfEmptyEnum) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNotAnyOfEmptyEnum) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNotAnyOfEmptyEnum) String() string { return string(i._raw) }

// InlineForbiddingPositionsNotAnyOfEmptyEnumSchema is the schema InlineForbiddingPositionsNotAnyOfEmptyEnum validates against.
var InlineForbiddingPositionsNotAnyOfEmptyEnumSchema = _schemaNode{
	Not: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks InlineForbiddingPositionsNotAnyOfEmptyEnum against its JSON Schema constraints.
func (i InlineForbiddingPositionsNotAnyOfEmptyEnum) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&InlineForbiddingPositionsNotAnyOfEmptyEnumSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// InlineForbiddingPositionsNotEmptyEnum accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type InlineForbiddingPositionsNotEmptyEnum struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNotEmptyEnum) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNotEmptyEnum) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNotEmptyEnum) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNotEmptyEnum) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNotEmptyEnum) String() string { return string(i._raw) }

// InlineForbiddingPositionsNotEmptyEnumSchema is the schema InlineForbiddingPositionsNotEmptyEnum validates against.
var InlineForbiddingPositionsNotEmptyEnumSchema = _schemaNode{
	Not: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks InlineForbiddingPositionsNotEmptyEnum against its JSON Schema constraints.
func (i InlineForbiddingPositionsNotEmptyEnum) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&InlineForbiddingPositionsNotEmptyEnumSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// InlineForbiddingPositionsNotEmptyEnumBound accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type InlineForbiddingPositionsNotEmptyEnumBound struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNotEmptyEnumBound) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNotEmptyEnumBound) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNotEmptyEnumBound) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNotEmptyEnumBound) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNotEmptyEnumBound) String() string { return string(i._raw) }

// InlineForbiddingPositionsNotEmptyEnumBoundSchema is the schema InlineForbiddingPositionsNotEmptyEnumBound validates against.
var InlineForbiddingPositionsNotEmptyEnumBoundSchema = _schemaNode{
	Not: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks InlineForbiddingPositionsNotEmptyEnumBound against its JSON Schema constraints.
func (i InlineForbiddingPositionsNotEmptyEnumBound) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&InlineForbiddingPositionsNotEmptyEnumBoundSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// InlineForbiddingPositionsNotEmptyItemsItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsNotEmptyItemsItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNotEmptyItemsItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNotEmptyItemsItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNotEmptyItemsItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNotEmptyItemsItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNotEmptyItemsItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsNotEmptyItemsItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsNotEmptyItemsItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsNotTypedConst accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type InlineForbiddingPositionsNotTypedConst struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNotTypedConst) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNotTypedConst) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNotTypedConst) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNotTypedConst) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNotTypedConst) String() string { return string(i._raw) }

// InlineForbiddingPositionsNotTypedConstSchema is the schema InlineForbiddingPositionsNotTypedConst validates against.
var InlineForbiddingPositionsNotTypedConstSchema = _schemaNode{
	Not: _node(_schemaNode{
		Const: _strPtr("\"x\""),
		Type:  []string{"string"},
	}),
}

// Validate checks InlineForbiddingPositionsNotTypedConst against its JSON Schema constraints.
func (i InlineForbiddingPositionsNotTypedConst) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&InlineForbiddingPositionsNotTypedConstSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// InlineForbiddingPositionsNotTypedEmptyEnum accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type InlineForbiddingPositionsNotTypedEmptyEnum struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNotTypedEmptyEnum) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNotTypedEmptyEnum) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNotTypedEmptyEnum) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNotTypedEmptyEnum) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNotTypedEmptyEnum) String() string { return string(i._raw) }

// InlineForbiddingPositionsNotTypedEmptyEnumSchema is the schema InlineForbiddingPositionsNotTypedEmptyEnum validates against.
var InlineForbiddingPositionsNotTypedEmptyEnumSchema = _schemaNode{
	Not: _node(_schemaNode{Boolean: _boolPtr(false)}),
}

// Validate checks InlineForbiddingPositionsNotTypedEmptyEnum against its JSON Schema constraints.
func (i InlineForbiddingPositionsNotTypedEmptyEnum) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(i._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&InlineForbiddingPositionsNotTypedEmptyEnumSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// InlineForbiddingPositionsNullableEmptyEnumValuesValue accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsNullableEmptyEnumValuesValue struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNullableEmptyEnumValuesValue) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNullableEmptyEnumValuesValue) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNullableEmptyEnumValuesValue) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNullableEmptyEnumValuesValue) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNullableEmptyEnumValuesValue) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsNullableEmptyEnumValuesValue against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsNullableEmptyEnumValuesValue) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsNullableFalseItemsItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsNullableFalseItemsItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsNullableFalseItemsItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsNullableFalseItemsItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsNullableFalseItemsItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsNullableFalseItemsItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsNullableFalseItemsItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsNullableFalseItemsItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsNullableFalseItemsItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

type InlineForbiddingPositionsOkEnumItemsItem string

const (
	InlineForbiddingPositionsOkEnumItemsItemA InlineForbiddingPositionsOkEnumItemsItem = "a"
	InlineForbiddingPositionsOkEnumItemsItemB InlineForbiddingPositionsOkEnumItemsItem = "b"
)

// Validate checks InlineForbiddingPositionsOkEnumItemsItem against its JSON Schema constraints.
func (i InlineForbiddingPositionsOkEnumItemsItem) Validate() error {
	switch i {
	case InlineForbiddingPositionsOkEnumItemsItemA, InlineForbiddingPositionsOkEnumItemsItemB:
		return nil
	default:
		return fmt.Errorf("invalid InlineForbiddingPositionsOkEnumItemsItem value: %v", i)
	}
}

// InlineForbiddingPositionsRefEmptyEnumAnyOf accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsRefEmptyEnumAnyOf struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsRefEmptyEnumAnyOf) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsRefEmptyEnumAnyOf) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsRefEmptyEnumAnyOf) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsRefEmptyEnumAnyOf) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsRefEmptyEnumAnyOf) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsRefEmptyEnumAnyOf against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsRefEmptyEnumAnyOf) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositionsTypedEmptyEnumItemsItem accepts any JSON value and validates a root-level "not" constraint.
type InlineForbiddingPositionsTypedEmptyEnumItemsItem struct {
	_raw json.RawMessage
}

func (i *InlineForbiddingPositionsTypedEmptyEnumItemsItem) UnmarshalJSON(data []byte) error {
	i._raw = append(i._raw[:0], data...)
	return nil
}

func (i InlineForbiddingPositionsTypedEmptyEnumItemsItem) MarshalJSON() ([]byte, error) {
	if len(i._raw) == 0 {
		return []byte("null"), nil
	}
	return i._raw, nil
}

func (i InlineForbiddingPositionsTypedEmptyEnumItemsItem) Raw() json.RawMessage { return i._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (i InlineForbiddingPositionsTypedEmptyEnumItemsItem) IsZero() bool { return len(i._raw) == 0 }

func (i InlineForbiddingPositionsTypedEmptyEnumItemsItem) String() string { return string(i._raw) }

// Validate checks InlineForbiddingPositionsTypedEmptyEnumItemsItem against its JSON Schema "not" constraint.
func (i InlineForbiddingPositionsTypedEmptyEnumItemsItem) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(i._raw) == 0 {
		return nil
	}
	return fmt.Errorf("not: value is forbidden (not schema matches all values)")
}

// InlineForbiddingPositions - Issue #142: an inline sub-schema that admits no instance at all, written at every position that resolves rather than names. `items: false` forbids every element and `{"enum":[]}` forbids every value, so the array, map, slot and branch holding one is invalid the moment it holds anything -- and each came out `[]any`, `map[string]any` or a Go type with no check. The two spellings are the same statement, which is why they sit here side by side; the accept-control beside every rejection is the container that holds nothing, which stays valid throughout.
type InlineForbiddingPositions struct {
	EmptyEnumAllOf          InlineForbiddingPositionsEmptyEnumAllOf                          `json:"emptyEnumAllOf,omitzero"`
	EmptyEnumAnyOf          InlineForbiddingPositionsEmptyEnumAnyOf                          `json:"emptyEnumAnyOf,omitzero"`
	EmptyEnumBranch         InlineForbiddingPositionsEmptyEnumBranch                         `json:"emptyEnumBranch,omitzero"`
	EmptyEnumContains       []any                                                            `json:"emptyEnumContains,omitzero"`
	EmptyEnumDependent      *NeverWithK                                                      `json:"emptyEnumDependent,omitempty"`
	EmptyEnumItems          []InlineForbiddingPositionsEmptyEnumItemsItem                    `json:"emptyEnumItems,omitzero"`
	EmptyEnumNames          *NoNames                                                         `json:"emptyEnumNames,omitempty"`
	EmptyEnumPattern        *InlineForbiddingPositionsEmptyEnumPattern                       `json:"emptyEnumPattern,omitempty"`
	EmptyEnumSlot           []any                                                            `json:"emptyEnumSlot,omitzero"`
	EmptyEnumUnevalItems    []any                                                            `json:"emptyEnumUnevalItems,omitzero"`
	EmptyEnumUnevalProps    *InlineForbiddingPositionsEmptyEnumUnevalProps                   `json:"emptyEnumUnevalProps,omitempty"`
	EmptyEnumValues         map[string]InlineForbiddingPositionsEmptyEnumValuesValue         `json:"emptyEnumValues,omitzero"`
	FalseItems              []InlineForbiddingPositionsFalseItemsItem                        `json:"falseItems,omitzero"`
	InferredEmptyEnumItems  *InlineForbiddingPositionsInferredEmptyEnumItems                 `json:"inferredEmptyEnumItems,omitempty"`
	InferredEmptyEnumSlot   *InlineForbiddingPositionsInferredEmptyEnumSlot                  `json:"inferredEmptyEnumSlot,omitempty"`
	InferredEmptyEnumTail   *InlineForbiddingPositionsInferredEmptyEnumTail                  `json:"inferredEmptyEnumTail,omitempty"`
	NestedFalseItems        [][]InlineForbiddingPositionsNestedFalseItemsItemItem            `json:"nestedFalseItems,omitzero"`
	NotAnyOfEmptyEnum       InlineForbiddingPositionsNotAnyOfEmptyEnum                       `json:"notAnyOfEmptyEnum,omitzero"`
	NotEmptyEnum            InlineForbiddingPositionsNotEmptyEnum                            `json:"notEmptyEnum,omitzero"`
	NotEmptyEnumBound       InlineForbiddingPositionsNotEmptyEnumBound                       `json:"notEmptyEnumBound,omitzero"`
	NotEmptyItems           []InlineForbiddingPositionsNotEmptyItemsItem                     `json:"notEmptyItems,omitzero"`
	NotTypedConst           InlineForbiddingPositionsNotTypedConst                           `json:"notTypedConst,omitzero"`
	NotTypedEmptyEnum       InlineForbiddingPositionsNotTypedEmptyEnum                       `json:"notTypedEmptyEnum,omitzero"`
	NullableEmptyEnumValues map[string]InlineForbiddingPositionsNullableEmptyEnumValuesValue `json:"nullableEmptyEnumValues,omitzero"`
	NullableFalseItems      []InlineForbiddingPositionsNullableFalseItemsItem                `json:"nullableFalseItems,omitzero"`
	OkEnumItems             []InlineForbiddingPositionsOkEnumItemsItem                       `json:"okEnumItems,omitzero"`
	PlainItems              []string                                                         `json:"plainItems,omitzero"`
	RefEmptyEnumAnyOf       InlineForbiddingPositionsRefEmptyEnumAnyOf                       `json:"refEmptyEnumAnyOf,omitzero"`
	TypedEmptyEnumItems     []InlineForbiddingPositionsTypedEmptyEnumItemsItem               `json:"typedEmptyEnumItems,omitzero"`
	ViaRefEmptyEnum         []NeverEnum                                                      `json:"viaRefEmptyEnum,omitzero"`
	ViaRefFalse             []Never                                                          `json:"viaRefFalse,omitzero"`
	AdditionalProperties    map[string]json.RawMessage                                       `json:"-"`
	_jsonKeys               map[string]bool                                                  // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls              map[string]bool                                                  // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (i *InlineForbiddingPositions) UnmarshalJSON(data []byte) error {
	i.AdditionalProperties = nil
	i._jsonKeys = nil
	i._jsonNulls = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type InlineForbiddingPositions")
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"emptyEnumAllOf",
			"emptyEnumAnyOf",
			"emptyEnumBranch",
			"emptyEnumContains",
			"emptyEnumDependent",
			"emptyEnumItems",
			"emptyEnumNames",
			"emptyEnumPattern",
			"emptyEnumSlot",
			"emptyEnumUnevalItems",
			"emptyEnumUnevalProps",
			"emptyEnumValues",
			"falseItems",
			"inferredEmptyEnumItems",
			"inferredEmptyEnumSlot",
			"inferredEmptyEnumTail",
			"nestedFalseItems",
			"notAnyOfEmptyEnum",
			"notEmptyEnum",
			"notEmptyEnumBound",
			"notEmptyItems",
			"notTypedConst",
			"notTypedEmptyEnum",
			"nullableEmptyEnumValues",
			"nullableFalseItems",
			"okEnumItems",
			"plainItems",
			"refEmptyEnumAnyOf",
			"typedEmptyEnumItems",
			"viaRefEmptyEnum",
			"viaRefFalse",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias InlineForbiddingPositions
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(i),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return err
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"emptyEnumAllOf",
			"emptyEnumContains",
			"emptyEnumDependent",
			"emptyEnumItems",
			"emptyEnumNames",
			"emptyEnumPattern",
			"emptyEnumSlot",
			"emptyEnumUnevalItems",
			"emptyEnumUnevalProps",
			"emptyEnumValues",
			"falseItems",
			"notEmptyItems",
			"viaRefEmptyEnum",
			"viaRefFalse",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		if _v, ok := raw["nestedFalseItems"]; ok {
			if err := checkJSONNulls(_v, "nestedFalseItems", &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		if _v, ok := raw["okEnumItems"]; ok {
			if err := checkJSONNulls(_v, "okEnumItems", &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		if _v, ok := raw["plainItems"]; ok {
			if err := checkJSONNulls(_v, "plainItems", &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		if _v, ok := raw["typedEmptyEnumItems"]; ok {
			if err := checkJSONNulls(_v, "typedEmptyEnumItems", &jsonNullRule{Reject: true, Elem: &jsonNullRule{Reject: true}}); err != nil {
				return err
			}
		}
		i._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			i._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"inferredEmptyEnumItems",
			"inferredEmptyEnumSlot",
			"inferredEmptyEnumTail",
			"nullableEmptyEnumValues",
			"nullableFalseItems",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if i._jsonNulls == nil {
					i._jsonNulls = make(map[string]bool, 1)
				}
				i._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"emptyEnumAllOf":          true,
			"emptyEnumAnyOf":          true,
			"emptyEnumBranch":         true,
			"emptyEnumContains":       true,
			"emptyEnumDependent":      true,
			"emptyEnumItems":          true,
			"emptyEnumNames":          true,
			"emptyEnumPattern":        true,
			"emptyEnumSlot":           true,
			"emptyEnumUnevalItems":    true,
			"emptyEnumUnevalProps":    true,
			"emptyEnumValues":         true,
			"falseItems":              true,
			"inferredEmptyEnumItems":  true,
			"inferredEmptyEnumSlot":   true,
			"inferredEmptyEnumTail":   true,
			"nestedFalseItems":        true,
			"notAnyOfEmptyEnum":       true,
			"notEmptyEnum":            true,
			"notEmptyEnumBound":       true,
			"notEmptyItems":           true,
			"notTypedConst":           true,
			"notTypedEmptyEnum":       true,
			"nullableEmptyEnumValues": true,
			"nullableFalseItems":      true,
			"okEnumItems":             true,
			"plainItems":              true,
			"refEmptyEnumAnyOf":       true,
			"typedEmptyEnumItems":     true,
			"viaRefEmptyEnum":         true,
			"viaRefFalse":             true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if i.AdditionalProperties == nil {
				i.AdditionalProperties = make(map[string]json.RawMessage)
			}
			i.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (i InlineForbiddingPositions) MarshalJSON() ([]byte, error) {
	type Alias InlineForbiddingPositions
	aux := struct {
		Alias
	}{
		Alias: (Alias)(i),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(i._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range i._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range i.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks InlineForbiddingPositions against its JSON Schema constraints.
func (i InlineForbiddingPositions) Validate() error {
	if i._jsonKeys["emptyEnumUnevalItems"] {
		if len(i.EmptyEnumUnevalItems) > 1 {
			return fmt.Errorf("emptyEnumUnevalItems: has %d items, maximum is 1", len(i.EmptyEnumUnevalItems))
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["emptyEnumAllOf"] {
		if err := i.EmptyEnumAllOf.Validate(); err != nil {
			return fmt.Errorf("emptyEnumAllOf.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["emptyEnumAnyOf"] {
		if err := i.EmptyEnumAnyOf.Validate(); err != nil {
			return fmt.Errorf("emptyEnumAnyOf.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["emptyEnumBranch"] {
		if err := i.EmptyEnumBranch.Validate(); err != nil {
			return fmt.Errorf("emptyEnumBranch.%w", err)
		}
	}
	if i.EmptyEnumDependent != nil {
		if err := i.EmptyEnumDependent.Validate(); err != nil {
			return fmt.Errorf("emptyEnumDependent.%w", err)
		}
	}
	for _i, _item := range i.EmptyEnumItems {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("emptyEnumItems[%d].%w", _i, err)
		}
	}
	if i.EmptyEnumNames != nil {
		if err := i.EmptyEnumNames.Validate(); err != nil {
			return fmt.Errorf("emptyEnumNames.%w", err)
		}
	}
	if i.EmptyEnumPattern != nil {
		if err := i.EmptyEnumPattern.Validate(); err != nil {
			return fmt.Errorf("emptyEnumPattern.%w", err)
		}
	}
	if i.EmptyEnumUnevalProps != nil {
		if err := i.EmptyEnumUnevalProps.Validate(); err != nil {
			return fmt.Errorf("emptyEnumUnevalProps.%w", err)
		}
	}
	for _k, _val := range i.EmptyEnumValues {
		if err := _val.Validate(); err != nil {
			return fmt.Errorf("emptyEnumValues[%q].%w", _k, err)
		}
	}
	for _i, _item := range i.FalseItems {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("falseItems[%d].%w", _i, err)
		}
	}
	if i.InferredEmptyEnumItems != nil {
		if err := i.InferredEmptyEnumItems.Validate(); err != nil {
			return fmt.Errorf("inferredEmptyEnumItems.%w", err)
		}
	}
	if i.InferredEmptyEnumSlot != nil {
		if err := i.InferredEmptyEnumSlot.Validate(); err != nil {
			return fmt.Errorf("inferredEmptyEnumSlot.%w", err)
		}
	}
	if i.InferredEmptyEnumTail != nil {
		if err := i.InferredEmptyEnumTail.Validate(); err != nil {
			return fmt.Errorf("inferredEmptyEnumTail.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["notAnyOfEmptyEnum"] {
		if err := i.NotAnyOfEmptyEnum.Validate(); err != nil {
			return fmt.Errorf("notAnyOfEmptyEnum.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["notEmptyEnum"] {
		if err := i.NotEmptyEnum.Validate(); err != nil {
			return fmt.Errorf("notEmptyEnum.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["notEmptyEnumBound"] {
		if err := i.NotEmptyEnumBound.Validate(); err != nil {
			return fmt.Errorf("notEmptyEnumBound.%w", err)
		}
	}
	for _i, _item := range i.NotEmptyItems {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("notEmptyItems[%d].%w", _i, err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["notTypedConst"] {
		if err := i.NotTypedConst.Validate(); err != nil {
			return fmt.Errorf("notTypedConst.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["notTypedEmptyEnum"] {
		if err := i.NotTypedEmptyEnum.Validate(); err != nil {
			return fmt.Errorf("notTypedEmptyEnum.%w", err)
		}
	}
	for _k, _val := range i.NullableEmptyEnumValues {
		if err := _val.Validate(); err != nil {
			return fmt.Errorf("nullableEmptyEnumValues[%q].%w", _k, err)
		}
	}
	for _i, _item := range i.NullableFalseItems {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("nullableFalseItems[%d].%w", _i, err)
		}
	}
	for _i, _item := range i.OkEnumItems {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("okEnumItems[%d].%w", _i, err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if i._jsonKeys == nil || i._jsonKeys["refEmptyEnumAnyOf"] {
		if err := i.RefEmptyEnumAnyOf.Validate(); err != nil {
			return fmt.Errorf("refEmptyEnumAnyOf.%w", err)
		}
	}
	for _i, _item := range i.TypedEmptyEnumItems {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("typedEmptyEnumItems[%d].%w", _i, err)
		}
	}
	for _i, _item := range i.ViaRefEmptyEnum {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("viaRefEmptyEnum[%d].%w", _i, err)
		}
	}
	for _i, _item := range i.ViaRefFalse {
		if err := _item.Validate(); err != nil {
			return fmt.Errorf("viaRefFalse[%d].%w", _i, err)
		}
	}
	for _i0, _e0 := range i.NestedFalseItems {
		for _i1, _e1 := range _e0 {
			if _err := _e1.Validate(); _err != nil {
				return fmt.Errorf("nestedFalseItems[%d][%d].%w", _i0, _i1, _err)
			}
		}
	}
	if i._jsonKeys["emptyEnumContains"] {
		// contains validation: count elements matching the contains sub-schema.
		{
			_containsCount := 0
			// contains: false — no element can ever match, so the count stays at 0.
			if _containsCount < 1 {
				return fmt.Errorf("emptyEnumContains: contains: no element matches the contains schema")
			}
		}
	}
	// Tuple items: validate each position against its schema type.
	for _idx, _elem := range i.EmptyEnumSlot {
		_ = _elem
		if _idx == 0 {
			return fmt.Errorf("emptyEnumSlot: items[%d]: value is not allowed (schema is false)", _idx)
		}
	}
	// unevaluatedItems validation
	{
		evaluatedCount := 1
		allEvaluated := false
		if !allEvaluated && len(i.EmptyEnumUnevalItems) > evaluatedCount {
			return fmt.Errorf("emptyEnumUnevalItems: unevaluatedItems: %d unevaluated items found (only %d items are evaluated)", len(i.EmptyEnumUnevalItems)-evaluatedCount, evaluatedCount)
		}
	}
	return nil
}
