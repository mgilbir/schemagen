// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
	ecma262 "github.com/mgilbir/goecma262"
	ecmaflags "github.com/mgilbir/goecma262/flags"
)

type AnyOfSummary struct {
	A                    *string                    `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AnyOfSummary) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnyOfSummary")
	}
	type Alias AnyOfSummary
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnyOfSummary) MarshalJSON() ([]byte, error) {
	type Alias AnyOfSummary
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnyOfSummary against its JSON Schema constraints.
func (a AnyOfSummary) Validate() error {
	// Keywords the generated checks cannot state in full, held as schema data and
	// evaluated against the document.
	if a._jsonRawProps != nil {
		_rbInstance := make(map[string]any, len(a._jsonRawProps))
		for _rbKey, _rbRaw := range a._jsonRawProps {
			var _rbVal any
			if _rbErr := json.Unmarshal(_rbRaw, &_rbVal); _rbErr != nil {
				return fmt.Errorf("cannot decode property %q: %w", _rbKey, _rbErr)
			}
			_rbInstance[_rbKey] = _rbVal
		}
		{
			_rbNode0 := _schemaNode{
				AnyOf: []_schemaNode{
					_schemaNode{
						Properties: []_schemaMember{
							{Key: "k", Node: _schemaNode{
								Type: []string{"string"},
							}},
						},
						Required: []string{"k"},
					},
					_schemaNode{Boolean: _boolPtr(false)},
				},
			}
			if _rbRes := _evalNode(&_rbNode0, _rbInstance); !_rbRes.ok {
				if _rbRes.reason == "" {
					return fmt.Errorf("anyOf: value does not satisfy the schema")
				}
				return fmt.Errorf("anyOf: %s", _rbRes.reason)
			}
		}
	}
	return nil
}

type AnyOfSummaryFalse struct {
	A                    *string                    `json:"a,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AnyOfSummaryFalse) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnyOfSummaryFalse")
	}
	type Alias AnyOfSummaryFalse
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnyOfSummaryFalse) MarshalJSON() ([]byte, error) {
	type Alias AnyOfSummaryFalse
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnyOfSummaryFalse against its JSON Schema constraints.
func (a AnyOfSummaryFalse) Validate() error {
	// Keywords the generated checks cannot state in full, held as schema data and
	// evaluated against the document.
	if a._jsonRawProps != nil {
		_rbInstance := make(map[string]any, len(a._jsonRawProps))
		for _rbKey, _rbRaw := range a._jsonRawProps {
			var _rbVal any
			if _rbErr := json.Unmarshal(_rbRaw, &_rbVal); _rbErr != nil {
				return fmt.Errorf("cannot decode property %q: %w", _rbKey, _rbErr)
			}
			_rbInstance[_rbKey] = _rbVal
		}
		{
			_rbNode0 := _schemaNode{
				AnyOf: []_schemaNode{
					_schemaNode{
						Properties: []_schemaMember{
							{Key: "k", Node: _schemaNode{
								Type: []string{"string"},
							}},
						},
						Required: []string{"k"},
					},
					_schemaNode{Boolean: _boolPtr(false)},
				},
			}
			if _rbRes := _evalNode(&_rbNode0, _rbInstance); !_rbRes.ok {
				if _rbRes.reason == "" {
					return fmt.Errorf("anyOf: value does not satisfy the schema")
				}
				return fmt.Errorf("anyOf: %s", _rbRes.reason)
			}
		}
	}
	return nil
}

// HiddenKeywordSpellingsConstNullBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type HiddenKeywordSpellingsConstNullBranch struct {
	_raw json.RawMessage
}

func (h *HiddenKeywordSpellingsConstNullBranch) UnmarshalJSON(data []byte) error {
	h._raw = append(h._raw[:0], data...)
	return nil
}

func (h HiddenKeywordSpellingsConstNullBranch) MarshalJSON() ([]byte, error) {
	if len(h._raw) == 0 {
		return []byte("null"), nil
	}
	return h._raw, nil
}

func (h HiddenKeywordSpellingsConstNullBranch) Raw() json.RawMessage { return h._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (h HiddenKeywordSpellingsConstNullBranch) IsZero() bool { return len(h._raw) == 0 }

func (h HiddenKeywordSpellingsConstNullBranch) String() string { return string(h._raw) }

// HiddenKeywordSpellingsConstNullBranchSchema is the schema HiddenKeywordSpellingsConstNullBranch validates against.
var HiddenKeywordSpellingsConstNullBranchSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("null"),
		},
	},
}

// Validate checks HiddenKeywordSpellingsConstNullBranch against its JSON Schema constraints.
func (h HiddenKeywordSpellingsConstNullBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(h._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(h._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&HiddenKeywordSpellingsConstNullBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// HiddenKeywordSpellingsConstStringBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type HiddenKeywordSpellingsConstStringBranch struct {
	_raw json.RawMessage
}

func (h *HiddenKeywordSpellingsConstStringBranch) UnmarshalJSON(data []byte) error {
	h._raw = append(h._raw[:0], data...)
	return nil
}

func (h HiddenKeywordSpellingsConstStringBranch) MarshalJSON() ([]byte, error) {
	if len(h._raw) == 0 {
		return []byte("null"), nil
	}
	return h._raw, nil
}

func (h HiddenKeywordSpellingsConstStringBranch) Raw() json.RawMessage { return h._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (h HiddenKeywordSpellingsConstStringBranch) IsZero() bool { return len(h._raw) == 0 }

func (h HiddenKeywordSpellingsConstStringBranch) String() string { return string(h._raw) }

// HiddenKeywordSpellingsConstStringBranchSchema is the schema HiddenKeywordSpellingsConstStringBranch validates against.
var HiddenKeywordSpellingsConstStringBranchSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("\"a\""),
		},
	},
}

// Validate checks HiddenKeywordSpellingsConstStringBranch against its JSON Schema constraints.
func (h HiddenKeywordSpellingsConstStringBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(h._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(h._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&HiddenKeywordSpellingsConstStringBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// HiddenKeywordSpellingsOneOfConstNull accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type HiddenKeywordSpellingsOneOfConstNull struct {
	_raw json.RawMessage
}

func (h *HiddenKeywordSpellingsOneOfConstNull) UnmarshalJSON(data []byte) error {
	h._raw = append(h._raw[:0], data...)
	return nil
}

func (h HiddenKeywordSpellingsOneOfConstNull) MarshalJSON() ([]byte, error) {
	if len(h._raw) == 0 {
		return []byte("null"), nil
	}
	return h._raw, nil
}

func (h HiddenKeywordSpellingsOneOfConstNull) Raw() json.RawMessage { return h._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (h HiddenKeywordSpellingsOneOfConstNull) IsZero() bool { return len(h._raw) == 0 }

func (h HiddenKeywordSpellingsOneOfConstNull) String() string { return string(h._raw) }

// HiddenKeywordSpellingsOneOfConstNullSchema is the schema HiddenKeywordSpellingsOneOfConstNull validates against.
var HiddenKeywordSpellingsOneOfConstNullSchema = _schemaNode{
	OneOf: []_schemaNode{
		_schemaNode{
			Const: _strPtr("null"),
		},
		_schemaNode{
			Type: []string{"string"},
		},
	},
}

// Validate checks HiddenKeywordSpellingsOneOfConstNull against its JSON Schema constraints.
func (h HiddenKeywordSpellingsOneOfConstNull) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(h._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(h._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&HiddenKeywordSpellingsOneOfConstNullSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type HiddenKeywordSpellingsPatternConstNullPattern0 json.RawMessage

var hiddenKeywordSpellingsPatternConstNullPattern0AllowedJSON = []string{
	"null",
}

func (h *HiddenKeywordSpellingsPatternConstNullPattern0) UnmarshalJSON(data []byte) error {
	*h = HiddenKeywordSpellingsPatternConstNullPattern0(data)
	return nil
}

func (h HiddenKeywordSpellingsPatternConstNullPattern0) MarshalJSON() ([]byte, error) {
	if len(h) == 0 {
		return []byte("null"), nil
	}
	return json.RawMessage(h).MarshalJSON()
}

// Validate checks HiddenKeywordSpellingsPatternConstNullPattern0 against its JSON Schema constraints.
func (h HiddenKeywordSpellingsPatternConstNullPattern0) Validate() error {
	// Normalize to compact JSON for comparison (handles whitespace, key order, number format).
	var tmp any
	if err := json.Unmarshal([]byte(h), &tmp); err != nil {
		return fmt.Errorf("invalid HiddenKeywordSpellingsPatternConstNullPattern0 value: %s", string(h))
	}
	canonical, err := json.Marshal(tmp)
	if err != nil {
		return fmt.Errorf("invalid HiddenKeywordSpellingsPatternConstNullPattern0 value: %s", string(h))
	}
	_canon := string(canonical)
	for _, allowed := range hiddenKeywordSpellingsPatternConstNullPattern0AllowedJSON {
		if _canon == allowed {
			return nil
		}
	}
	return fmt.Errorf("invalid HiddenKeywordSpellingsPatternConstNullPattern0 value: %s", _canon)
}

type HiddenKeywordSpellingsPatternConstNull struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (h *HiddenKeywordSpellingsPatternConstNull) UnmarshalJSON(data []byte) error {
	h.AdditionalProperties = nil
	h.PatternProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type HiddenKeywordSpellingsPatternConstNull")
	}
	type Alias HiddenKeywordSpellingsPatternConstNull
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(h),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^a", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if h.PatternProperties == nil {
					h.PatternProperties = make(map[string]json.RawMessage)
				}
				h.PatternProperties[rawKey] = rawVal
				continue
			}
			if h.AdditionalProperties == nil {
				h.AdditionalProperties = make(map[string]json.RawMessage)
			}
			h.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (h HiddenKeywordSpellingsPatternConstNull) MarshalJSON() ([]byte, error) {
	type Alias HiddenKeywordSpellingsPatternConstNull
	aux := struct {
		Alias
	}{
		Alias: (Alias)(h),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range h.PatternProperties {
		obj[k] = v
	}
	for k, v := range h.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks HiddenKeywordSpellingsPatternConstNull against its JSON Schema constraints.
func (h HiddenKeywordSpellingsPatternConstNull) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(h.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^a", ecmaflags.Unicode),
		}
		for k, v := range h.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv HiddenKeywordSpellingsPatternConstNullPattern0
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^a", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^a", k, _vErr)
					}
				}
			}
		}
	}
	return nil
}

type HiddenKeywordSpellingsPatternConstStringPattern0 string

const (
	HiddenKeywordSpellingsPatternConstStringPattern0A HiddenKeywordSpellingsPatternConstStringPattern0 = "a"
)

// Validate checks HiddenKeywordSpellingsPatternConstStringPattern0 against its JSON Schema constraints.
func (h HiddenKeywordSpellingsPatternConstStringPattern0) Validate() error {
	switch h {
	case HiddenKeywordSpellingsPatternConstStringPattern0A:
		return nil
	default:
		return fmt.Errorf("invalid HiddenKeywordSpellingsPatternConstStringPattern0 value: %v", h)
	}
}

type HiddenKeywordSpellingsPatternConstString struct {
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	PatternProperties    map[string]json.RawMessage `json:"-"`
}

func (h *HiddenKeywordSpellingsPatternConstString) UnmarshalJSON(data []byte) error {
	h.AdditionalProperties = nil
	h.PatternProperties = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type HiddenKeywordSpellingsPatternConstString")
	}
	type Alias HiddenKeywordSpellingsPatternConstString
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(h),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		knownFields := map[string]bool{}
		patternRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^b", ecmaflags.Unicode),
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			matchesPattern := false
			for _, re := range patternRegexps {
				if re.MatchString(rawKey) {
					matchesPattern = true
					break
				}
			}
			if matchesPattern {
				if h.PatternProperties == nil {
					h.PatternProperties = make(map[string]json.RawMessage)
				}
				h.PatternProperties[rawKey] = rawVal
				continue
			}
			if h.AdditionalProperties == nil {
				h.AdditionalProperties = make(map[string]json.RawMessage)
			}
			h.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (h HiddenKeywordSpellingsPatternConstString) MarshalJSON() ([]byte, error) {
	type Alias HiddenKeywordSpellingsPatternConstString
	aux := struct {
		Alias
	}{
		Alias: (Alias)(h),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range h.PatternProperties {
		obj[k] = v
	}
	for k, v := range h.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks HiddenKeywordSpellingsPatternConstString against its JSON Schema constraints.
func (h HiddenKeywordSpellingsPatternConstString) Validate() error {
	// patternProperties validation: check matched values against sub-schema constraints.
	if len(h.PatternProperties) > 0 {
		ppRegexps := []*ecma262.Regexp{
			ecma262.MustCompile("^b", ecmaflags.Unicode),
		}
		for k, v := range h.PatternProperties {
			_ = v
			if ppRegexps[0].MatchString(k) {
				{
					// The sub-schema's own type answers for it: the value is
					// decoded into it, so the decode enforces shape and the
					// Validate enforces everything beyond it.
					var _pv HiddenKeywordSpellingsPatternConstStringPattern0
					if _uErr := json.Unmarshal(v, &_pv); _uErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^b", k, _uErr)
					}
					if _vErr := _pv.Validate(); _vErr != nil {
						return fmt.Errorf("patternProperties %s: key %q: %w", "^b", k, _vErr)
					}
				}
			}
		}
	}
	return nil
}

type HiddenKeywordSpellingsPlainEnum string

const (
	HiddenKeywordSpellingsPlainEnumA HiddenKeywordSpellingsPlainEnum = "a"
	HiddenKeywordSpellingsPlainEnumB HiddenKeywordSpellingsPlainEnum = "b"
)

// Validate checks HiddenKeywordSpellingsPlainEnum against its JSON Schema constraints.
func (h HiddenKeywordSpellingsPlainEnum) Validate() error {
	switch h {
	case HiddenKeywordSpellingsPlainEnumA, HiddenKeywordSpellingsPlainEnumB:
		return nil
	default:
		return fmt.Errorf("invalid HiddenKeywordSpellingsPlainEnum value: %v", h)
	}
}

// HiddenKeywordSpellings - Issue #154. Five gates decide what a schema states by re-marshalling it and reading the keys, and marshalling drops two spellings: `enum` is tagged omitempty so "enum": [] leaves no key, and "const": null is held on a field tagged "-" because encoding/json cannot tell a null constant from an absent one. A schema stating only one of those read as a schema stating nothing, so each gate handed its position a type with no check on it. Every property here pairs the hidden spelling with the spelling that was always read -- `false` beside the empty enum, a string constant beside the null one -- and the two must now be answered alike. The accept row beside each rejection is the value the schema does admit; where a schema admits nothing at all, it is the document that leaves the property out.
type HiddenKeywordSpellings struct {
	AnyOfSummaryEmptyEnum *AnyOfSummary                             `json:"anyOfSummaryEmptyEnum,omitempty"`
	AnyOfSummaryFalse     *AnyOfSummaryFalse                        `json:"anyOfSummaryFalse,omitempty"`
	ConstNullBranch       HiddenKeywordSpellingsConstNullBranch     `json:"constNullBranch,omitzero"`
	ConstStringBranch     HiddenKeywordSpellingsConstStringBranch   `json:"constStringBranch,omitzero"`
	OneOfConstNull        HiddenKeywordSpellingsOneOfConstNull      `json:"oneOfConstNull,omitzero"`
	PatternConstNull      *HiddenKeywordSpellingsPatternConstNull   `json:"patternConstNull,omitempty"`
	PatternConstString    *HiddenKeywordSpellingsPatternConstString `json:"patternConstString,omitempty"`
	PlainEnum             *HiddenKeywordSpellingsPlainEnum          `json:"plainEnum,omitempty"`
	AdditionalProperties  map[string]json.RawMessage                `json:"-"`
	_jsonKeys             map[string]bool                           // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (h *HiddenKeywordSpellings) UnmarshalJSON(data []byte) error {
	h.AdditionalProperties = nil
	h._jsonKeys = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type HiddenKeywordSpellings")
	}
	type Alias HiddenKeywordSpellings
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(h),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"anyOfSummaryEmptyEnum",
			"anyOfSummaryFalse",
			"constStringBranch",
			"patternConstNull",
			"patternConstString",
			"plainEnum",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		h._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			h._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"anyOfSummaryEmptyEnum": true,
			"anyOfSummaryFalse":     true,
			"constNullBranch":       true,
			"constStringBranch":     true,
			"oneOfConstNull":        true,
			"patternConstNull":      true,
			"patternConstString":    true,
			"plainEnum":             true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if h.AdditionalProperties == nil {
				h.AdditionalProperties = make(map[string]json.RawMessage)
			}
			h.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (h HiddenKeywordSpellings) MarshalJSON() ([]byte, error) {
	type Alias HiddenKeywordSpellings
	aux := struct {
		Alias
	}{
		Alias: (Alias)(h),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range h.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks HiddenKeywordSpellings against its JSON Schema constraints.
func (h HiddenKeywordSpellings) Validate() error {
	if h.AnyOfSummaryEmptyEnum != nil {
		if err := h.AnyOfSummaryEmptyEnum.Validate(); err != nil {
			return fmt.Errorf("anyOfSummaryEmptyEnum.%w", err)
		}
	}
	if h.AnyOfSummaryFalse != nil {
		if err := h.AnyOfSummaryFalse.Validate(); err != nil {
			return fmt.Errorf("anyOfSummaryFalse.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if h._jsonKeys == nil || h._jsonKeys["constNullBranch"] {
		if err := h.ConstNullBranch.Validate(); err != nil {
			return fmt.Errorf("constNullBranch.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if h._jsonKeys == nil || h._jsonKeys["constStringBranch"] {
		if err := h.ConstStringBranch.Validate(); err != nil {
			return fmt.Errorf("constStringBranch.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if h._jsonKeys == nil || h._jsonKeys["oneOfConstNull"] {
		if err := h.OneOfConstNull.Validate(); err != nil {
			return fmt.Errorf("oneOfConstNull.%w", err)
		}
	}
	if h.PatternConstNull != nil {
		if err := h.PatternConstNull.Validate(); err != nil {
			return fmt.Errorf("patternConstNull.%w", err)
		}
	}
	if h.PatternConstString != nil {
		if err := h.PatternConstString.Validate(); err != nil {
			return fmt.Errorf("patternConstString.%w", err)
		}
	}
	if h.PlainEnum != nil {
		if err := h.PlainEnum.Validate(); err != nil {
			return fmt.Errorf("plainEnum.%w", err)
		}
	}
	return nil
}
