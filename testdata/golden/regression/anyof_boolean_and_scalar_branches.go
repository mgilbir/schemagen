// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"bytes"
	"encoding/json"
	"fmt"
)

// AnyOfBooleanAndScalarBranchesBareObjectBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type AnyOfBooleanAndScalarBranchesBareObjectBranch struct {
	_raw json.RawMessage
}

func (a *AnyOfBooleanAndScalarBranchesBareObjectBranch) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnyOfBooleanAndScalarBranchesBareObjectBranch) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnyOfBooleanAndScalarBranchesBareObjectBranch) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnyOfBooleanAndScalarBranchesBareObjectBranch) IsZero() bool { return len(a._raw) == 0 }

func (a AnyOfBooleanAndScalarBranchesBareObjectBranch) String() string { return string(a._raw) }

// AnyOfBooleanAndScalarBranchesBareObjectBranchSchema is the schema AnyOfBooleanAndScalarBranchesBareObjectBranch validates against.
var AnyOfBooleanAndScalarBranchesBareObjectBranchSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Type: []string{"object"},
		},
	},
}

// Validate checks AnyOfBooleanAndScalarBranchesBareObjectBranch against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesBareObjectBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnyOfBooleanAndScalarBranchesBareObjectBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// AnyOfBooleanAndScalarBranchesConstBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type AnyOfBooleanAndScalarBranchesConstBranch struct {
	_raw json.RawMessage
}

func (a *AnyOfBooleanAndScalarBranchesConstBranch) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnyOfBooleanAndScalarBranchesConstBranch) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnyOfBooleanAndScalarBranchesConstBranch) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnyOfBooleanAndScalarBranchesConstBranch) IsZero() bool { return len(a._raw) == 0 }

func (a AnyOfBooleanAndScalarBranchesConstBranch) String() string { return string(a._raw) }

// AnyOfBooleanAndScalarBranchesConstBranchSchema is the schema AnyOfBooleanAndScalarBranchesConstBranch validates against.
var AnyOfBooleanAndScalarBranchesConstBranchSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Const: _strPtr("\"x\""),
		},
	},
}

// Validate checks AnyOfBooleanAndScalarBranchesConstBranch against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesConstBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnyOfBooleanAndScalarBranchesConstBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type AnyOfBooleanAndScalarBranchesFalseBranch struct {
	K                    *string                    `json:"k,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AnyOfBooleanAndScalarBranchesFalseBranch) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnyOfBooleanAndScalarBranchesFalseBranch")
	}
	type Alias AnyOfBooleanAndScalarBranchesFalseBranch
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnyOfBooleanAndScalarBranchesFalseBranch) MarshalJSON() ([]byte, error) {
	type Alias AnyOfBooleanAndScalarBranchesFalseBranch
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnyOfBooleanAndScalarBranchesFalseBranch against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesFalseBranch) Validate() error {
	// anyOf/oneOf whose branch states unevaluatedProperties: which branch
	// contributes depends on the document, so the applicator is evaluated here.
	if a._jsonRawProps != nil {
		_rbInstance := make(map[string]any, len(a._jsonRawProps))
		for _rbKey, _rbRaw := range a._jsonRawProps {
			var _rbVal any
			if _rbErr := json.Unmarshal(_rbRaw, &_rbVal); _rbErr != nil {
				return fmt.Errorf("cannot decode property %q: %w", _rbKey, _rbErr)
			}
			_rbInstance[_rbKey] = _rbVal
		}
		{
			_rbNode0 := _schemaNode{
				AnyOf: []_schemaNode{
					_schemaNode{
						Properties: []_schemaMember{
							{Key: "k", Node: _schemaNode{
								Type: []string{"string"},
							}},
						},
						Required: []string{"k"},
						Type:     []string{"object"},
					},
					_schemaNode{Boolean: _boolPtr(false)},
				},
			}
			if _rbRes := _evalNode(&_rbNode0, _rbInstance); !_rbRes.ok {
				if _rbRes.reason == "" {
					return fmt.Errorf("anyOf: value does not satisfy the schema")
				}
				return fmt.Errorf("anyOf: %s", _rbRes.reason)
			}
		}
	}
	return nil
}

// AnyOfBooleanAndScalarBranchesMixed accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type AnyOfBooleanAndScalarBranchesMixed struct {
	_raw json.RawMessage
}

func (a *AnyOfBooleanAndScalarBranchesMixed) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnyOfBooleanAndScalarBranchesMixed) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnyOfBooleanAndScalarBranchesMixed) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnyOfBooleanAndScalarBranchesMixed) IsZero() bool { return len(a._raw) == 0 }

func (a AnyOfBooleanAndScalarBranchesMixed) String() string { return string(a._raw) }

// AnyOfBooleanAndScalarBranchesMixedSchema is the schema AnyOfBooleanAndScalarBranchesMixed validates against.
var AnyOfBooleanAndScalarBranchesMixedSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Type: []string{"string"},
		},
	},
}

// Validate checks AnyOfBooleanAndScalarBranchesMixed against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesMixed) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnyOfBooleanAndScalarBranchesMixedSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// AnyOfBooleanAndScalarBranchesNotBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type AnyOfBooleanAndScalarBranchesNotBranch struct {
	_raw json.RawMessage
}

func (a *AnyOfBooleanAndScalarBranchesNotBranch) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnyOfBooleanAndScalarBranchesNotBranch) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnyOfBooleanAndScalarBranchesNotBranch) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnyOfBooleanAndScalarBranchesNotBranch) IsZero() bool { return len(a._raw) == 0 }

func (a AnyOfBooleanAndScalarBranchesNotBranch) String() string { return string(a._raw) }

// AnyOfBooleanAndScalarBranchesNotBranchSchema is the schema AnyOfBooleanAndScalarBranchesNotBranch validates against.
var AnyOfBooleanAndScalarBranchesNotBranchSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{
			Not: _node(_schemaNode{
				Type: []string{"object"},
			}),
		},
	},
}

// Validate checks AnyOfBooleanAndScalarBranchesNotBranch against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesNotBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnyOfBooleanAndScalarBranchesNotBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

type AnyOfBooleanAndScalarBranchesObjectsOnly struct {
	J                    *string                    `json:"j,omitempty"`
	K                    *string                    `json:"k,omitempty"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonRawProps        map[string]json.RawMessage // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

func (a *AnyOfBooleanAndScalarBranchesObjectsOnly) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonRawProps = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnyOfBooleanAndScalarBranchesObjectsOnly")
	}
	type Alias AnyOfBooleanAndScalarBranchesObjectsOnly
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"j",
			"k",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		a._jsonRawProps = raw
		knownFields := map[string]bool{
			"j": true,
			"k": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnyOfBooleanAndScalarBranchesObjectsOnly) MarshalJSON() ([]byte, error) {
	type Alias AnyOfBooleanAndScalarBranchesObjectsOnly
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnyOfBooleanAndScalarBranchesObjectsOnly against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesObjectsOnly) Validate() error {
	// object-level anyOf: at least one flattened variant must match.
	// This check depends on JSON key presence (_jsonKeys), so it is skipped
	// for hand-constructed values (nil _jsonKeys), consistent with how the
	// required-property check above treats untracked presence.
	if a._jsonKeys != nil {
		_anyOfMatches := 0
		{
			_branchMatches := true
			if !a._jsonKeys["k"] {
				_branchMatches = false
			}
			if _branchMatches {
				if _raw, _ok := a._jsonRawProps["k"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "string" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				_anyOfMatches++
			}
		}
		{
			_branchMatches := true
			if !a._jsonKeys["j"] {
				_branchMatches = false
			}
			if _branchMatches {
				if _raw, _ok := a._jsonRawProps["j"]; _ok {
					_jt := "unknown"
					_b := bytes.TrimSpace(_raw)
					if len(_b) > 0 {
						switch _b[0] {
						case '"':
							_jt = "string"
						case '{':
							_jt = "object"
						case '[':
							_jt = "array"
						case 't', 'f':
							_jt = "boolean"
						case 'n':
							_jt = "null"
						default:
							_jt = "number"
							_isInt := true
							for _, _c := range _b {
								if _c == '.' || _c == 'e' || _c == 'E' {
									_isInt = false
									break
								}
							}
							if _isInt {
								_jt = "integer"
							}
						}
					}
					if _jt != "string" {
						_branchMatches = false
					}
				}
			}
			if _branchMatches {
				_anyOfMatches++
			}
		}
		if _anyOfMatches < 1 {
			return fmt.Errorf("anyOf: no variant matched")
		}
	}
	return nil
}

// AnyOfBooleanAndScalarBranchesTrueBranch accepts any JSON value. Its schema is held as data and interpreted
// at validation time, either because an unevaluatedItems constraint depends on
// which items the applicators evaluate for the value in hand, or because the
// schema gives the value no Go type of its own to hang a check on.
type AnyOfBooleanAndScalarBranchesTrueBranch struct {
	_raw json.RawMessage
}

func (a *AnyOfBooleanAndScalarBranchesTrueBranch) UnmarshalJSON(data []byte) error {
	a._raw = append(a._raw[:0], data...)
	return nil
}

func (a AnyOfBooleanAndScalarBranchesTrueBranch) MarshalJSON() ([]byte, error) {
	if len(a._raw) == 0 {
		return []byte("null"), nil
	}
	return a._raw, nil
}

func (a AnyOfBooleanAndScalarBranchesTrueBranch) Raw() json.RawMessage { return a._raw }

// IsZero reports whether no value was present, so an optional field tagged
// ",omitzero" is omitted when absent rather than marshalled as null.
func (a AnyOfBooleanAndScalarBranchesTrueBranch) IsZero() bool { return len(a._raw) == 0 }

func (a AnyOfBooleanAndScalarBranchesTrueBranch) String() string { return string(a._raw) }

// AnyOfBooleanAndScalarBranchesTrueBranchSchema is the schema AnyOfBooleanAndScalarBranchesTrueBranch validates against.
var AnyOfBooleanAndScalarBranchesTrueBranchSchema = _schemaNode{
	AnyOf: []_schemaNode{
		_schemaNode{
			Properties: []_schemaMember{
				{Key: "k", Node: _schemaNode{
					Type: []string{"string"},
				}},
			},
			Required: []string{"k"},
			Type:     []string{"object"},
		},
		_schemaNode{Boolean: _boolPtr(true)},
	},
}

// Validate checks AnyOfBooleanAndScalarBranchesTrueBranch against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranchesTrueBranch) Validate() error {
	// No value was present. A constraint applies to a value that is there; an
	// absent optional property is the parent's business (required), not this
	// type's.
	if len(a._raw) == 0 {
		return nil
	}
	var _v any
	if _err := json.Unmarshal(a._raw, &_v); _err != nil {
		return fmt.Errorf("cannot decode value: %w", _err)
	}
	if _res := _evalNode(&AnyOfBooleanAndScalarBranchesTrueBranchSchema, _v); !_res.ok {
		if _res.reason == "" {
			return fmt.Errorf("value does not satisfy the schema")
		}
		return fmt.Errorf("%s", _res.reason)
	}
	return nil
}

// AnyOfBooleanAndScalarBranches - An anyOf mixing an object branch with branches the merged struct cannot place, or that the object-shape summary cannot judge. The merge is a Go struct, so a branch admitting a scalar had nowhere to decode into and the document was refused before a branch was tried; a bare "type":"object" branch admits every object, including the ones the merged field types refuse; and the summary drops the whole "at least one branch matches" assertion as soon as one branch states neither a required key nor a property check, so a `false` branch -- which matches nothing -- was read as one matching everything. The all-object group is the control: it selects on required keys and must keep the merge it already judges correctly.
type AnyOfBooleanAndScalarBranches struct {
	BareObjectBranch     AnyOfBooleanAndScalarBranchesBareObjectBranch `json:"bareObjectBranch,omitzero"`
	ConstBranch          AnyOfBooleanAndScalarBranchesConstBranch      `json:"constBranch,omitzero"`
	FalseBranch          *AnyOfBooleanAndScalarBranchesFalseBranch     `json:"falseBranch,omitempty"`
	Mixed                AnyOfBooleanAndScalarBranchesMixed            `json:"mixed,omitzero"`
	NotBranch            AnyOfBooleanAndScalarBranchesNotBranch        `json:"notBranch,omitzero"`
	ObjectsOnly          *AnyOfBooleanAndScalarBranchesObjectsOnly     `json:"objectsOnly,omitempty"`
	TrueBranch           AnyOfBooleanAndScalarBranchesTrueBranch       `json:"trueBranch,omitzero"`
	AdditionalProperties map[string]json.RawMessage                    `json:"-"`
	_jsonKeys            map[string]bool                               // set by UnmarshalJSON for optional field / dependentSchemas validation
	_jsonNulls           map[string]bool                               // set by UnmarshalJSON for the properties written as null, which the decoded value cannot hold
}

func (a *AnyOfBooleanAndScalarBranches) UnmarshalJSON(data []byte) error {
	a.AdditionalProperties = nil
	a._jsonKeys = nil
	a._jsonNulls = nil
	if string(data) == "null" {
		return fmt.Errorf("null is not allowed for type AnyOfBooleanAndScalarBranches")
	}
	type Alias AnyOfBooleanAndScalarBranches
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(a),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return err
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"bareObjectBranch",
			"constBranch",
			"mixed",
			"objectsOnly",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return fmt.Errorf("%s: null is not allowed", _nullKey)
			}
		}
		a._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			a._jsonKeys[_k] = true
		}
		// The properties whose schema permits a null. The decode above has
		// already turned one into a nil pointer, a nil collection or an
		// untouched zero -- the same state an absent property leaves -- so the
		// document's own bytes are the only place the difference still exists.
		// Validate reads this to pass over the keywords a null satisfies
		// vacuously, and MarshalJSON to write the null back. See issue #110.
		for _, _nullKey := range []string{
			"falseBranch",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				if a._jsonNulls == nil {
					a._jsonNulls = make(map[string]bool, 1)
				}
				a._jsonNulls[_nullKey] = true
			}
		}
		knownFields := map[string]bool{
			"bareObjectBranch": true,
			"constBranch":      true,
			"falseBranch":      true,
			"mixed":            true,
			"notBranch":        true,
			"objectsOnly":      true,
			"trueBranch":       true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if a.AdditionalProperties == nil {
				a.AdditionalProperties = make(map[string]json.RawMessage)
			}
			a.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (a AnyOfBooleanAndScalarBranches) MarshalJSON() ([]byte, error) {
	type Alias AnyOfBooleanAndScalarBranches
	aux := struct {
		Alias
	}{
		Alias: (Alias)(a),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	// The properties the source document wrote as null. Nothing left in the
	// decoded value says they were there -- a null leaves the nil pointer or the
	// untouched zero an absent property leaves -- so writing them back has to
	// come from the record UnmarshalJSON kept. See issue #110.
	//
	// Only where the field still holds what the null left it holding. A caller
	// who decoded a null and then assigned a value has said something newer than
	// the document did, and writing the null over it would discard the
	// assignment; the record is about a value nobody has touched. What the
	// untouched state looks like is read off a zero of this very struct rather
	// than from a per-field literal, so a field type's own MarshalJSON decides
	// for itself and nothing here has to know how it spells "empty".
	if len(a._jsonNulls) > 0 {
		var _zero Alias
		if _zeroData, _zeroErr := json.Marshal(_zero); _zeroErr == nil {
			var _zeroObj map[string]json.RawMessage
			if json.Unmarshal(_zeroData, &_zeroObj) == nil {
				for _k := range a._jsonNulls {
					if _cur, _present := obj[_k]; !_present || string(_cur) == string(_zeroObj[_k]) {
						obj[_k] = json.RawMessage("null")
					}
				}
			}
		}
	}
	for k, v := range a.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks AnyOfBooleanAndScalarBranches against its JSON Schema constraints.
func (a AnyOfBooleanAndScalarBranches) Validate() error {
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["bareObjectBranch"] {
		if err := a.BareObjectBranch.Validate(); err != nil {
			return fmt.Errorf("bareObjectBranch.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["constBranch"] {
		if err := a.ConstBranch.Validate(); err != nil {
			return fmt.Errorf("constBranch.%w", err)
		}
	}
	if a.FalseBranch != nil {
		if err := a.FalseBranch.Validate(); err != nil {
			return fmt.Errorf("falseBranch.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["mixed"] {
		if err := a.Mixed.Validate(); err != nil {
			return fmt.Errorf("mixed.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["notBranch"] {
		if err := a.NotBranch.Validate(); err != nil {
			return fmt.Errorf("notBranch.%w", err)
		}
	}
	if a.ObjectsOnly != nil {
		if err := a.ObjectsOnly.Validate(); err != nil {
			return fmt.Errorf("objectsOnly.%w", err)
		}
	}
	// An optional property the source JSON did not carry left its Go zero
	// behind, which is not a value the schema ever saw. _jsonKeys records the
	// keys the JSON had; when nil the value was not built from JSON and
	// presence is unknowable, so the check still runs.
	if a._jsonKeys == nil || a._jsonKeys["trueBranch"] {
		if err := a.TrueBranch.Validate(); err != nil {
			return fmt.Errorf("trueBranch.%w", err)
		}
	}
	return nil
}
