// Code generated by schemagen. DO NOT EDIT.

package testpkg

import (
	"encoding/json"
	"fmt"
)

type OneOfBranchUnevaluatedPropertiesValueOption0 struct {
	B                    int64                      `json:"b"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_nonObject           bool                       // set by UnmarshalJSON when the JSON data is not an object
	_rawNonObject        json.RawMessage            // raw bytes of non-object data for lossless roundtrip
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfBranchUnevaluatedPropertiesValueOption0) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	o._nonObject = false
	o._rawNonObject = nil
	// The schema admits a document that is not an object, so object constraints
	// are type-conditional. Non-object JSON data is accepted here and judged by
	// Validate; raw bytes are preserved for roundtrip.
	if len(data) == 0 || data[0] != '{' {
		o._nonObject = true
		o._rawNonObject = append(o._rawNonObject[:0], data...)
		return nil
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"b",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfBranchUnevaluatedPropertiesValueOption0
	aux := &struct {
		*Alias
		B *jsonInteger `json:"b"`
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"b", jsonDecodeValue[jsonInteger]},
		})
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.B != nil {
		_iv := *aux.B
		o.B = int64(_iv)
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"b",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"b": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBranchUnevaluatedPropertiesValueOption0) MarshalJSON() ([]byte, error) {
	// Non-object data was silently accepted — return the original raw bytes.
	if o._nonObject {
		if len(o._rawNonObject) > 0 {
			return o._rawNonObject, nil
		}
		return []byte("null"), nil
	}
	type Alias OneOfBranchUnevaluatedPropertiesValueOption0
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfBranchUnevaluatedPropertiesValueOption0 against its JSON Schema constraints.
func (o OneOfBranchUnevaluatedPropertiesValueOption0) Validate() error {
	// Non-object data was silently accepted — validate non-object constraints if any.
	if o._nonObject {
		return nil
	}
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"b"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	// unevaluatedProperties: check overflow map for properties not covered by evaluated set.
	{
		evaluatedNames := map[string]bool{
			"b": true,
		}
		for k, v := range o.AdditionalProperties {
			evaluated := false
			_ = v
			if evaluatedNames[k] {
				evaluated = true
			}
			if !evaluated {
				return jsonValueErrorf("unevaluated property %q is not allowed", k)
			}
		}
	}
	return nil
}

type OneOfBranchUnevaluatedPropertiesValueOption1 struct {
	A                    int64                      `json:"a"`
	AdditionalProperties map[string]json.RawMessage `json:"-"`
	_nonObject           bool                       // set by UnmarshalJSON when the JSON data is not an object
	_rawNonObject        json.RawMessage            // raw bytes of non-object data for lossless roundtrip
	_jsonKeys            map[string]bool            // set by UnmarshalJSON for optional field / dependentSchemas validation
}

func (o *OneOfBranchUnevaluatedPropertiesValueOption1) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonKeys = nil
	o._nonObject = false
	o._rawNonObject = nil
	// The schema admits a document that is not an object, so object constraints
	// are type-conditional. Non-object JSON data is accepted here and judged by
	// Validate; raw bytes are preserved for roundtrip.
	if len(data) == 0 || data[0] != '{' {
		o._nonObject = true
		o._rawNonObject = append(o._rawNonObject[:0], data...)
		return nil
	}
	// The decode below is handed the document cut down to the properties this
	// schema declares, because encoding/json matches a key that matches no field
	// exactly a second time case-insensitively, and would fill "name" from a
	// "NAME" the schema never gave it. See jsonExactProperties and issue #245.
	//
	// The object is parsed once here and read again by the blocks below, so this
	// costs no parse that was not already being paid. Its error is held rather
	// than returned, so that a document which is not an object is still refused
	// by the decode that always refused it, in the words it always used.
	var raw map[string]json.RawMessage
	_rawErr := json.Unmarshal(data, &raw)
	_decodeData := data
	if _rawErr == nil {
		if _exact := jsonExactProperties(raw,
			"a",
		); _exact != nil {
			_decodeData = _exact
		}
	}
	type Alias OneOfBranchUnevaluatedPropertiesValueOption1
	aux := &struct {
		*Alias
		A *jsonInteger `json:"a"`
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(_decodeData, aux); err != nil {
		return jsonDecodeMemberError(data, err, []jsonMemberDecode{
			{"a", jsonDecodeValue[jsonInteger]},
		})
	}

	// A number written 1.0 is the integer 1 from draft 6 on, and the shadows
	// above are what let encoding/json see it. Each outer pointer is nil when
	// the property was absent or null, both of which leave the field as it was.
	if aux.A != nil {
		_iv := *aux.A
		o.A = int64(_iv)
	}
	{
		if _rawErr != nil {
			return _rawErr
		}
		// A property the schema gives a type to may not be written as null. By
		// the time the decode above has run there is nothing left to see: a null
		// leaves a nil pointer, a nil collection, or a scalar at its zero, which
		// is exactly what an absent property leaves, so the verdict has to be
		// taken from the document's own keys. See jsonNullRule for the nested
		// spelling of the same rule.
		for _, _nullKey := range []string{
			"a",
		} {
			if _v, ok := raw[_nullKey]; ok && string(_v) == "null" {
				return jsonPathf(jsonValueErrorf("null is not allowed"), "%s", _nullKey)
			}
		}
		o._jsonKeys = make(map[string]bool, len(raw))
		for _k := range raw {
			o._jsonKeys[_k] = true
		}
		knownFields := map[string]bool{
			"a": true,
		}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBranchUnevaluatedPropertiesValueOption1) MarshalJSON() ([]byte, error) {
	// Non-object data was silently accepted — return the original raw bytes.
	if o._nonObject {
		if len(o._rawNonObject) > 0 {
			return o._rawNonObject, nil
		}
		return []byte("null"), nil
	}
	type Alias OneOfBranchUnevaluatedPropertiesValueOption1
	aux := struct {
		Alias
	}{
		Alias: (Alias)(o),
	}
	data, err := json.Marshal(aux)
	if err != nil {
		return nil, err
	}
	var obj map[string]json.RawMessage
	if err := json.Unmarshal(data, &obj); err != nil {
		return nil, err
	}
	for k, v := range o.AdditionalProperties {
		obj[k] = v
	}
	return json.Marshal(obj)
}

// Validate checks OneOfBranchUnevaluatedPropertiesValueOption1 against its JSON Schema constraints.
func (o OneOfBranchUnevaluatedPropertiesValueOption1) Validate() error {
	// Non-object data was silently accepted — validate non-object constraints if any.
	if o._nonObject {
		return nil
	}
	// Required properties must be present in the source JSON. _jsonKeys is
	// populated by UnmarshalJSON; when nil (the value was not built from JSON)
	// presence is untracked and the check is skipped, consistent with how
	// optional-property validation below treats _jsonKeys.
	if o._jsonKeys != nil {
		for _, _req := range []string{"a"} {
			if !o._jsonKeys[_req] {
				return fmt.Errorf("%s: required property is missing", _req)
			}
		}
	}
	return nil
}

type OneOfBranchUnevaluatedProperties struct {
	Value                isOneOfBranchUnevaluatedProperties_Value `json:"-"`
	AdditionalProperties map[string]json.RawMessage               `json:"-"`
	_jsonRawProps        map[string]json.RawMessage               // set by UnmarshalJSON for runtime conditional evaluation (if/then/else, anyOf const checks)
}

// isOneOfBranchUnevaluatedProperties_Value is a sealed interface for the Value field of OneOfBranchUnevaluatedProperties.
// Exactly one variant must be set.
type isOneOfBranchUnevaluatedProperties_Value interface {
	isOneOfBranchUnevaluatedProperties_Value()
}

// OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0 wraps *OneOfBranchUnevaluatedPropertiesValueOption0 as a Value variant.
type OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0 struct {
	OneOfBranchUnevaluatedPropertiesValueOption0 *OneOfBranchUnevaluatedPropertiesValueOption0
}

func (*OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0) isOneOfBranchUnevaluatedProperties_Value() {
}

// OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1 wraps *OneOfBranchUnevaluatedPropertiesValueOption1 as a Value variant.
type OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1 struct {
	OneOfBranchUnevaluatedPropertiesValueOption1 *OneOfBranchUnevaluatedPropertiesValueOption1
}

func (*OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1) isOneOfBranchUnevaluatedProperties_Value() {
}

// GetOneOfBranchUnevaluatedPropertiesValueOption0 returns the OneOfBranchUnevaluatedPropertiesValueOption0 variant value, or the zero value if not set.
func (o *OneOfBranchUnevaluatedProperties) GetOneOfBranchUnevaluatedPropertiesValueOption0() *OneOfBranchUnevaluatedPropertiesValueOption0 {
	if o != nil {
		if v, ok := o.Value.(*OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0); ok {
			return v.OneOfBranchUnevaluatedPropertiesValueOption0
		}
	}
	var zero *OneOfBranchUnevaluatedPropertiesValueOption0
	return zero
}

// GetOneOfBranchUnevaluatedPropertiesValueOption1 returns the OneOfBranchUnevaluatedPropertiesValueOption1 variant value, or the zero value if not set.
func (o *OneOfBranchUnevaluatedProperties) GetOneOfBranchUnevaluatedPropertiesValueOption1() *OneOfBranchUnevaluatedPropertiesValueOption1 {
	if o != nil {
		if v, ok := o.Value.(*OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1); ok {
			return v.OneOfBranchUnevaluatedPropertiesValueOption1
		}
	}
	var zero *OneOfBranchUnevaluatedPropertiesValueOption1
	return zero
}

func (o *OneOfBranchUnevaluatedProperties) UnmarshalJSON(data []byte) error {
	o.AdditionalProperties = nil
	o._jsonRawProps = nil
	o.Value = nil
	if string(data) == "null" {
		return jsonValueErrorf("null is not allowed")
	}
	type Alias OneOfBranchUnevaluatedProperties
	aux := &struct {
		*Alias
	}{
		Alias: (*Alias)(o),
	}

	if err := json.Unmarshal(data, aux); err != nil {
		return jsonDecodeRefusal(err)
	}

	{
		// Top-level oneOf: the entire JSON object is the variant data.
		oneofData := json.RawMessage(data)
		if len(oneofData) > 0 && string(oneofData) != "null" {
			var oneofMatched int
			var oneofLastErr error
			// A second tally: branches actually satisfied, not merely decoded.
			// An object branch keeps its constraints inside the variant type, so
			// two branches whose required keys are both present both decode even
			// when only one holds. oneofOpaque counts branches neither this
			// tally nor the checks above can speak for. Read once, below.
			var oneofStrict int
			var oneofOpaque int
			var oneofStrictSel isOneOfBranchUnevaluatedProperties_Value
			var oneofStrictErr error

			// Try variant: OneOfBranchUnevaluatedPropertiesValueOption0
			{
				if oneofHasRequiredFields(oneofData, "b") {
					var candidate *OneOfBranchUnevaluatedPropertiesValueOption0
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.Value = &OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0{OneOfBranchUnevaluatedPropertiesValueOption0: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0{OneOfBranchUnevaluatedPropertiesValueOption0: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfBranchUnevaluatedPropertiesValueOption0: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			// Try variant: OneOfBranchUnevaluatedPropertiesValueOption1
			{
				if oneofHasRequiredFields(oneofData, "a") {
					var candidate *OneOfBranchUnevaluatedPropertiesValueOption1
					if err := json.Unmarshal(oneofData, &candidate); err == nil {
						o.Value = &OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1{OneOfBranchUnevaluatedPropertiesValueOption1: candidate}
						oneofMatched++
						if candidate != nil {
							if _vErr := candidate.Validate(); _vErr == nil {
								oneofStrict++
								oneofStrictSel = &OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1{OneOfBranchUnevaluatedPropertiesValueOption1: candidate}
							} else {
								oneofStrictErr = fmt.Errorf("variant OneOfBranchUnevaluatedPropertiesValueOption1: %w", _vErr)
							}
						} else {
							oneofOpaque++
						}
					} else {
						oneofLastErr = err
					}
				}
			}

			if oneofMatched == 0 {
				return fmt.Errorf("OneOfBranchUnevaluatedProperties.Value: no matching oneOf variant: %w", oneofLastErr)
			}
			if oneofMatched > 1 && oneofOpaque == 0 {
				// Several branches decoded and every one can be judged, so the
				// branches' own constraints settle which of them the value
				// really satisfies. Ambiguity is already a rejection here, so
				// this can only let through a document the schema allows.
				switch {
				case oneofStrict == 1:
					o.Value = oneofStrictSel
					oneofMatched = 1
				case oneofStrict > 1:
					oneofMatched = oneofStrict
				case oneofStrictErr != nil:
					// Not ambiguity but a value no branch accepts: report the
					// branch's own reason rather than a count.
					return fmt.Errorf("OneOfBranchUnevaluatedProperties.Value: no matching oneOf variant: %w", oneofStrictErr)
				}
			}
			if oneofMatched > 1 {
				return fmt.Errorf("OneOfBranchUnevaluatedProperties.Value: multiple oneOf variants matched (%d), expected exactly 1", oneofMatched)
			}
		}
	}
	{
		var raw map[string]json.RawMessage
		if err := json.Unmarshal(data, &raw); err != nil {
			return err
		}
		o._jsonRawProps = raw
		knownFields := map[string]bool{}
		for rawKey, rawVal := range raw {
			if knownFields[rawKey] {
				continue
			}
			if o.AdditionalProperties == nil {
				o.AdditionalProperties = make(map[string]json.RawMessage)
			}
			o.AdditionalProperties[rawKey] = rawVal
		}
	}

	return nil
}
func (o OneOfBranchUnevaluatedProperties) MarshalJSON() ([]byte, error) {
	// Top-level oneOf: marshal the selected variant directly as the root object.
	if o.Value != nil {
		switch v := o.Value.(type) {
		case *OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0:
			return json.Marshal(v.OneOfBranchUnevaluatedPropertiesValueOption0)
		case *OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1:
			return json.Marshal(v.OneOfBranchUnevaluatedPropertiesValueOption1)
		}
	}
	return []byte("null"), nil
}

// Validate checks OneOfBranchUnevaluatedProperties against its JSON Schema constraints.
func (o OneOfBranchUnevaluatedProperties) Validate() error {
	// oneOf union: the branch selection settled on one variant, whose own type
	// carries that branch's constraints. Nothing else applies them — selection
	// only decides which branch decodes — so the interior of an object variant
	// goes unchecked unless the parent descends here. A variant whose type has
	// no Validate (a scalar, or `any`) is absent from the switch: its branch
	// constraints ride on the selection checks in UnmarshalJSON instead.
	switch _oneOfSel := o.Value.(type) {
	case *OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption0:
		if _oneOfSel.OneOfBranchUnevaluatedPropertiesValueOption0 != nil {
			if err := _oneOfSel.OneOfBranchUnevaluatedPropertiesValueOption0.Validate(); err != nil {
				return err
			}
		}
	case *OneOfBranchUnevaluatedProperties_OneOfBranchUnevaluatedPropertiesValueOption1:
		if _oneOfSel.OneOfBranchUnevaluatedPropertiesValueOption1 != nil {
			if err := _oneOfSel.OneOfBranchUnevaluatedPropertiesValueOption1.Validate(); err != nil {
				return err
			}
		}
	}
	// Keywords the generated checks cannot state in full, held as schema data and
	// evaluated against the document.
	if o._jsonRawProps != nil {
		_rbInstance := make(map[string]any, len(o._jsonRawProps))
		for _rbKey, _rbRaw := range o._jsonRawProps {
			var _rbVal any
			if _rbErr := json.Unmarshal(_rbRaw, &_rbVal); _rbErr != nil {
				return jsonValueErrorf("cannot decode property %q: %w", _rbKey, _rbErr)
			}
			_rbInstance[_rbKey] = _rbVal
		}
		{
			_rbNode0 := _schemaNode{
				OneOf: []_schemaNode{
					_schemaNode{
						Properties: []_schemaMember{
							{Key: "b", Node: _schemaNode{
								Type: []string{"integer"},
							}},
						},
						Required:              []string{"b"},
						UnevaluatedProperties: _node(_schemaNode{Boolean: _boolPtr(false)}),
					},
					_schemaNode{
						Properties: []_schemaMember{
							{Key: "a", Node: _schemaNode{
								Type: []string{"integer"},
							}},
						},
						Required: []string{"a"},
					},
				},
			}
			if _rbRes := _evalNode(&_rbNode0, _rbInstance); !_rbRes.ok {
				if _rbRes.reason == "" {
					return fmt.Errorf("oneOf: value does not satisfy the schema")
				}
				return fmt.Errorf("oneOf: %s", _rbRes.reason)
			}
		}
	}
	return nil
}
