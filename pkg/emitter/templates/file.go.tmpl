// Code generated by schemagen. DO NOT EDIT.

package {{.PackageName}}
{{- if .UnresolvedRefs}}

// NOT VALIDATED: --lenient-refs generated this file with $refs that no resolver
// could serve:
//
{{- range .UnresolvedRefs}}
//	{{.}}
{{- end}}
//
// Whatever those references said is not in this file. A position that held one
// is `any` -- no Validate to call, no decode that can fail -- or names a type
// nothing here declares, so every document reaches your code unexamined at
// those places, including the ones the schema forbids. Supply the referenced
// documents, or drop --lenient-refs to have generation refuse instead.
{{- end}}
{{- if .Imports}}

import (
{{- range .Imports}}
	{{if .Alias}}{{.Alias}} {{end}}"{{.Path}}"
{{- end}}
)
{{- end}}
{{range .TypeDefs}}
{{template "typedef" .}}
{{end}}
{{- if .HasValidationCapability}}
{{template "validation_capability" .}}
{{- end}}
